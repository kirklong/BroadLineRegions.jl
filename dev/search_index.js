var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"Reference for BroadLineRegions.jl's public interface. \n\nnote: Note\nNo methods are exported by default into the global namespace to prevent overlap with other modules, and you must prepend the module name to all methods to access them. BroadLineRegions.jl exports itself as both BroadLineRegions and BLR, so both of these prefixes are equivalent, i.e. BroadLineRegions.model == BLR.model. If prepending this to function calls annoys you you can always manually import whatever you desire into the global space with syntax like: using BLR: DiskWindModel, cloudModel","title":"API"},{"category":"section","location":"api/#Performance-notes","page":"API","text":"Several internal optimizations affect how you should interact with models:\n\nResult caching. Each model memoizes the arrays that getVariable gathers from its rings (intensities, velocities, delays, …) in model.cache, so repeated profile/transfer-function calculations on the same model are much faster than the first. The package's own mutating functions (removeNaN!, raytrace!, zeroDiskObscuredClouds!, removeDiskObscuredClouds!, model combination with +) manage this automatically — but if you mutate ring fields directly (e.g. m.rings[1].I .= 0.0) you must call reset!(m) afterwards so subsequent calculations see the new values. Set m.cache = nothing to disable caching for a model.\nCached 3D coordinates. Every point's final system coordinates (x, y, z) are computed once at construction and stored on the ring (fields x, y, z; accessor getXYZ).  If you mutate a ring's geometry (r, ϕ₀, i, rot, θₒ, reflect), set ring.x = nothing; ring.y = nothing; ring.z = nothing to force recomputation. In performance-critical custom code prefer rotate3D_scalar (allocation-free tuple return) over rotate3D (allocates a Vector).\nMultithreading. Disk-wind model construction parallelizes across rings/pixels when Julia is started with multiple threads (julia -t N); results are bit-identical at any thread count. Custom I/v functions passed to the constructors must be thread-safe to benefit (the built-in ones are). Cloud model generation is currently deliberately not threaded so that seeded models (rng=...) reproduce identically.\nDelays in combined models. For combined (multi-submodel) models all time delays are computed with the general geometric formula t = η(r − x) (see tCloud) for every point to ensure the  most general formula is applied to be correct with all custom implementations. Thus it is critical in any combined  models for performance that ring.x is set properly for all submodels. ","title":"Performance notes"},{"category":"section","location":"api/#Full-documentation","page":"API","text":"","title":"Full documentation"},{"category":"method","location":"api/#BroadLineRegions.DiskWindModel-NTuple{4, Float64}","page":"API","text":"DiskWindModel(r̄::Float64, rFac::Float64, α::Float64, i::Float64; rot::Float64=0.0, \n        nr::Int=128, nϕ::Int=256, scale::Symbol=:log, kwargs...)\n\nUses the model constructor to create a DiskWind model of the BLR as detailed in Long+2023 and Long+2025.\n\nParameters\n\nr̄::Float64: Mean radius of model (in terms of r_s)\nrFac::Float64: Radius factor\nα::Float64: Power-law source function scaling\ni::Float64: Inclination angle in radians\nrot::Float64=0.0: Rotation of system plane about z-axis in radians\nnr::Int=128: Number of radial bins\nnϕ::Int=256: Number of azimuthal bins\nscale::Symbol=:log: Radial binning scale (:log or :linear)\nkwargs...: Extra keyword arguments for model constructor (see examples)\n\nReturns\n\nmodel object\n\nNote\n\nSimilar to another DiskWind model constructor but here we pass r̄, rFac, and α.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWindModel"},{"category":"method","location":"api/#BroadLineRegions.DiskWindModel-Tuple{Float64, Float64, Float64}","page":"API","text":"DiskWindModel(rMin::Float64, rMax::Float64, i::Float64; nr::Int=128, nϕ::Int=256, \n        I::Function=DiskWindIntensity, v::Function=vCircularDisk, scale::Symbol=:log, kwargs...)\n\nUses the model constructor to create a DiskWind model of the BLR as detailed in Long+2023 and Long+2025.\n\nParameters\n\nrMin::Float64: Minimum radius of model (in terms of r_s)\nrMax::Float64: Maximum radius of model (in terms of r_s)\ni::Float64: Inclination angle in radians (all rings have the same inclination)\nnr::Int=128: Number of radial bins\nnϕ::Int=256: Number of azimuthal bins\nI::Function=DiskWindIntensity: Intensity function\nv::Function=vCircularDisk: Velocity function\nscale::Symbol=:log: Radial binning scale (:log or :linear)\nkwargs...: Extra keyword arguments for I and v functions (see examples)\n\nReturns\n\nmodel object\n\nNote\n\nSimilar to other DiskWind model constructor but must explicitly pass rMin and rMax.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWindModel"},{"category":"method","location":"api/#BroadLineRegions.cloudModel-Tuple{Int64}","page":"API","text":"cloudModel(nClouds::Int64; μ::Float64=500., β::Float64=1.0, F::Float64=0.5, rₛ::Float64=1.0, θₒ::Float64=π/2, γ::Float64=1.0, ξ::Float64=1.0, i::Float64=0.0, I::Union{Function,Float64}=IsotropicIntensity, v::Union{Function,Float64}=vCircularCloud, rng::Union{AbstractRNG,Symbol}=Random.GLOBAL_RNG, seed::Union{Nothing,Integer}=nothing, parallel::Bool=true, kwargs...)\n\nUses the model constructor to create a cloud model of the BLR similar to Pancoast+ 2011 and 2014.\n\nParameters\n\nnClouds::Int64: Number of clouds\nμ::Float64=500.: Mean radius of model (in terms of r_s)\nβ::Float64=1.0: Shape parameter for radial distribution\nF::Float64=0.5: Beginning radius in units of μ where clouds can be placed\nrₛ::Float64=1.0: Scale radius (in terms of r_s)\nθₒ::Float64=π/2: Maximum opening angle of cloud distribution (rad)\nγ::Float64=1.0: Disk concentration parameter\nξ::Float64=1.0: Fraction of clouds in back side that have not been moved to the front (when ξ = 1.0 clouds equally distributed front - back and when ξ = 0.0 all clouds are on the front side) \ni::Float64=0.0: Inclination angle of system (rad)\nI::Union{Function,Float64}=IsotropicIntensity: Intensity function\nv::Union{Function,Float64}=vCircularCloud: Velocity function\nrng::Union{AbstractRNG,Symbol}=Random.GLOBAL_RNG: Random number generator. The default AbstractRNG path preserves the legacy sequential draw order; rng=:philox uses independent counter-based per-cloud streams.\nseed::Union{Nothing,Integer}=nothing: Required when rng=:philox. For legacy seeded models, pass rng=MersenneTwister(seed).\nparallel::Bool=true: Whether to use threaded cloud generation for the rng=:philox path.\nkwargs...: Extra keyword arguments for I and v functions (see examples)\n\nReturns\n\nmodel object\n\nNote\n\nSimilar to the other cloudModel methods, but here the per-cloud ϕ₀, rot, and θₒ values are drawn internally (one independent stream per cloud) while i is held constant for the system.\n\n\n\n\n\n","title":"BroadLineRegions.cloudModel"},{"category":"method","location":"api/#BroadLineRegions.cloudModel-Tuple{Vector{Float64}, Vector{Float64}, Vector{Float64}, Vector{Float64}, Float64, Float64}","page":"API","text":"cloudModel(ϕ₀::Vector{Float64}, i::Vector{Float64}, rot::Vector{Float64}, θₒ::Vector{Float64}, \n        θₒSystem::Float64, ξ::Float64; rₛ::Float64=1.0, μ::Float64=500., β::Float64=1.0, F::Float64=0.5, \n        I::Union{Function,Float64}=IsotropicIntensity, v::Union{Function,Float64}=vCircularCloud, kwargs...)\n\nUses the model constructor to create a cloud model of the BLR similar to Pancoast+ 2011 and 2014.\n\nParameters\n\nϕ₀::Vector{Float64}: Initial azimuthal angle of cloud (rad) \ni::Vector{Float64}: Inclination angle (rad) \nrot::Vector{Float64}: Random rotation of cloud about z axis (rad) \nθₒ::Vector{Float64}: Opening angle of cloud (rad) \nθₒSystem::Float64: Maximum opening angle of the system (rad) \nξ::Float64: Fraction of clouds in back side that have not been moved to the front (when ξ = 1.0 clouds equally distributed front - back and when ξ = 0.0 all clouds are on the front side) \nrₛ::Float64=1.0: Scale radius (in terms of r_s)\nμ::Float64=500.: Mean radius of model (in terms of r_s)\nβ::Float64=1.0: Shape parameter for radial distribution\nF::Float64=0.5: Beginning radius in units of μ where clouds can be placed. \nI::Union{Function,Float64}=IsotropicIntensity: Intensity function\nv::Union{Function,Float64}=vCircularCloud: Velocity function\nkwargs...: Extra keyword arguments for I and v functions (see examples)\n\nReturns\n\nmodel object\n\nNote\n\nSimilar to other cloudModel method but here you must explicitly pass ϕ₀, i, rot, and θₒ.\n\n\n\n\n\n","title":"BroadLineRegions.cloudModel"},{"category":"method","location":"api/#BroadLineRegions.fillDiskGrid!-Tuple{Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Matrix{Float64}, Float64, Float64, Float64, Matrix{Float64}, Matrix{Float64}, Vararg{Float64, 6}}","page":"API","text":"fillDiskGrid!(rSystem, ϕSystem, ϕ₀, η, xSystem, ySystem, zSystem, α, β,\n    i, rot, θₒ, M, r3D, rMin, rMax, ηₒ, η₁, αRM, rNorm)\n\nTyped inner pixel loop of the disk-wind model constructor (function barrier).\n\nThe constructor body is type-unstable (its locals come from kwargs), which would box every per-pixel value; routing the loop through this concretely-typed helper keeps the raytracing allocation-free. Fills the supplied matrices in place; out-of-range pixels (r < rMin or r > rMax, compared after rounding to 9 significant digits) are set to NaN. M and r3D are the precomputed matrices described in raytrace; ηₒ, η₁, αRM, rNorm are the response parameters (hoisted from kwargs once instead of splatted per pixel).\n\nRows are processed in parallel when Julia is started with multiple threads (julia -t N). Every pixel writes only its own matrix slots and the computation is deterministic, so results are bit-identical at any thread count.\n\n\n\n\n\n","title":"BroadLineRegions.fillDiskGrid!"},{"category":"type","location":"api/#BroadLineRegions.camera","page":"API","text":"camera\n\nCamera coordinates struct.\n\nFields\n\nα::Union{Vector{Float64}, Matrix{Float64}}: x values in the camera plane\nβ::Union{Vector{Float64}, Matrix{Float64}}: y values in the camera plane\nraytraced::Bool: whether the camera has been used to raytrace the model\n\n\n\n\n\n","title":"BroadLineRegions.camera"},{"category":"type","location":"api/#BroadLineRegions.model","page":"API","text":"model\n\nA mutable structure to hold many rings and their parameters that model the BLR.\n\nFields\n\nrings::Vector{ring}: List of ring objects, see ring struct\nprofiles::Union{Nothing,Dict{Symbol,profile}}: Dictionary of profiles (see profile struct) with keys as symbols; optional, usually initialized to empty dictionary and filled in with setProfile!\ncamera::Union{Nothing,camera}: Camera coordinates (α,β) corresponding to each ring used to generate images and in raytracing, see camera struct\nsubModelStartInds::Vector{Int}: Indices of start of each submodel in list of rings; used to separate out submodels for raytracing or for the recovery of individual models after being combined\ncache::Union{Nothing,Dict{Any,Array}}: Memoized getVariable results so repeated profile calculations don't re-gather data from the rings. Managed automatically by the package's own mutating functions; if you mutate ring fields directly, call reset!(m) afterwards to invalidate it. Set to nothing to disable caching for a model entirely.\nparams::Union{Nothing,NamedTuple}: Construction provenance recorded by the public constructors (DiskWindModel, cloudModel) and propagated recursively through + and raytrace!. Its first entry is always constructor=<Symbol> naming how the model was built; leaf records store the arguments that constructor was called with, :+ records left/right subtrees, and :raytrace! records the raytrace arguments plus a parent subtree. Used by rebuild to reconstruct (or re-parameterize) a model. nothing when the model was not built by a public constructor. This captures construction history ONLY – in-place mutators (removeNaN!, zeroDiskObscuredClouds!) are NOT recorded, so a caller who ran them must re-apply the same calls in the same order to a rebuilt model. removeNaN! is observable-neutral (every binned observable already skips non-finite points), so the only unrecorded mutator with a physical effect is zeroDiskObscuredClouds!, which is deterministic and cheap to re-run. params is metadata only and never affects numerics.\n\nConstructors\n\nmodel(rings::Vector{ring}, profiles::Union{Nothing,Dict{Symbol,profile}}, camera::Union{Nothing,camera}, subModelStartInds::Vector{Int}) \n\nthe most flexible constructor, takes in user supplied vector of ring objects, optional dictionary of profiles, optional camera coordinates, and vector of indices for submodels\n\nmodel(rings::Vector{ring{Vector{Float64},Float64}})\n\ncloud model constructor, takes in a vector of ring objects that are each points in space and returns a model object with camera coordinates calculated from the physical parameters of each ring\n\nmodel(rMin::Float64, rMax::Float64, i::Float64, nr::Int, nϕ::Int, I::Function, v::Function, scale::Symbol; kwargs...)\n\ndisk-wind model constructor, takes in minimum and maximum radius, inclination angle, number of radial and azimuthal bins, intensity and velocity functions, and scale for radial binning; returns a model object with rings generated from these parameters\n\nmodel(r̄::Float64, rFac::Float64, Sα::Float64, i::Float64, nr::Int, nϕ::Int, scale::Symbol; kwargs...)\n\ndisk-wind model constructor, takes in average radius, radius scaling factor, power law for source function, inclination angle, number of radial and azimuthal bins, and scale for radial binning; returns a model object with rings generated from these parameters\n\n\n\n\n\n","title":"BroadLineRegions.model"},{"category":"type","location":"api/#BroadLineRegions.profile","page":"API","text":"profile\n\nA struct to hold binned data, usually bound to model struct with profiles.jl#setProfile!.\n\nFields\n\nname::Symbol: Name of profile\nbinCenters::Vector{Float64}: Bin centers \nbinEdges::Vector{Float64}: Bin edges\nbinSums::Vector{Float64}: Sum of values in each bin (crude integral over bin)\n\n\n\n\n\n","title":"BroadLineRegions.profile"},{"category":"type","location":"api/#BroadLineRegions.ring","page":"API","text":"ring{V,F} <: AbstractRing{V,F}\n\nA mutable structure to hold parameters of each model ring, where the \"ring\" is a circle in the camera plane observing the BLR.\n\nFields\n\nr: Distance from central mass (in terms of r_s)\nUnion{Vector{Float64}, Float64, Function}\nCan be a single value for constant radius, or vector corresponding to azimuthal angles\nCan be a function returning Vector{Float64} or Float64\ni: Inclination angle in radians\nUnion{Vector{Float64}, Float64}\nMust be between 0 and pi2, with 0 being face-on and pi2 being edge-on\nrot: Rotation of system plane about z-axis in radians\nUnion{Vector{Float64}, Float64}\nθₒ: Opening angle of ring in radians\nUnion{Vector{Float64}, Float64}\nShould be between 0 and pi2\nv: Line of sight velocity\nUnion{Vector{Float64}, Float64, Function}\nCan be a function that calculates velocity from other parameters\nI: Intensity\nUnion{Vector{Float64}, Float64, Matrix{Float64}, Function}\nCan be a function that calculates intensity from other parameters\nϕ: Azimuthal angle in radians\nUnion{Vector{Float64}, Float64}\nϕ₀: Initial azimuthal angle before rotation in radians\nUnion{Vector{Float64}, Float64}\nDefaults to 0.0 if not provided or if rot is 0.0\nΔA: Projected area of each ring element in image\nUnion{Vector{Float64}, Float64}\nUsed in calculating profiles\nreflect: Whether cloud is reflected across disk mid-plane\nUnion{Bool, Array{Bool,}}\nτ: Optical depth\nUnion{Vector{Float64}, Float64, Function}\nη: Response parameter for reverberation\nUnion{Vector{Float64}, Float64, Function}\nΔr: Distance between camera pixels in r\nFloat64\nΔϕ: Distance between camera pixels in ϕ\nFloat64\nscale: Encoding for camera ring scaling\nUnion{Nothing, Symbol}\n:log or :linear scale\nx, y, z: Cached 3D system coordinates of each point (camera at +x), or nothing\nUnion{Vector{Float64}, Float64, Nothing}\nComputed once at construction (or lazily by getXYZ) so hot loops never recompute rotate3D\nValues are post-reflection: if reflect is true the stored coordinates already include it\nDefault nothing; access through getXYZ(ring) which fills the cache on first use\n\nConstructor\n\nring(; r, i, v, I, ϕ, rot=0.0, θₒ=0.0, ϕ₀=0.0, ΔA=1.0, reflect=false,\n    τ=0.0, η=1.0, Δr=1.0, Δϕ=1.0, scale=nothing, x=nothing, y=nothing, z=nothing, kwargs...)\n\nRequired parameters:\n\nr, i, v, I, ϕ\n\nOptional parameters with defaults:\n\nrot=0.0, θₒ=0.0, ϕ₀=0.0, ΔA=1.0, reflect=false, τ=0.0, η=1.0, Δr=1.0, Δϕ=1.0, scale=nothing\n\nAdditional keyword arguments are passed to the r, v, I, and τ functions if they are provided as functions.\n\n\n\n\n\n","title":"BroadLineRegions.ring"},{"category":"method","location":"api/#BroadLineRegions.drawCloud-Tuple{}","page":"API","text":"drawCloud(;μ::Float64=500.,β::Float64=1.0,F::Float64=0.5,\n        ϕ₀::Float64=0.0,i::Float64=π/4,rot::Float64=0.0,rₛ::Float64=1.0,\n        θₒ::Float64=0.0,θₒSystem::Float64=0.0,I::Union{Function,Float64}=IsotropicIntensity,\n        v::Union{Function,Float64}=vCircularCloud,ξ::Float64=1.0,\n        rng::AbstractRNG=Random.GLOBAL_RNG,kwargs...)\n\nGenerates a model ring struct for a single cloud drawn from a thick disk-like structure with parameters defined by the input arguments similar to Pancoast+ 2011 and 2014.\n\nArguments:\n\nμ::Float64=500.: Mean radius of model (in terms of r_s)\nβ::Float64=1.0: Shape parameter for radial distribution\nF::Float64=0.5: Beginning radius in units of μ where clouds can be placed. \nϕ₀::Float64=0.0: Initial azimuthal angle of cloud (rad) \ni::Float64=π/4: Inclination angle (rad) \nrot::Float64=0.0: Random rotation of cloud about z axis (rad) \nrₛ::Float64=1.0: Scale radius (in terms of r_s)\nθₒ::Float64=0.0: Opening angle of cloud (rad) \nθₒSystem::Float64=0.0: Maximum opening angle of the system (rad) \nξ::Float64=1.0: Fraction of clouds in back side that have not been moved to the front (when ξ = 1.0 clouds equally distributed front - back and when ξ = 0.0 all clouds are on the front side) \nI::Union{Function,Float64}=IsotropicIntensity: Intensity function/value\nv::Union{Function,Float64}=vCircularCloud: Velocity function/value\nkwargs...: Extra keyword arguments for I and v functions (see examples)\n\nReturns:\n\nA model ring struct representing the properties of the cloud. \n\n\n\n\n\n","title":"BroadLineRegions.drawCloud"},{"category":"method","location":"api/#BroadLineRegions.getG-Tuple{Float64}","page":"API","text":"getG(β::Float64)\n\nReturns a Gamma distribution with parameters derived from the input parameter β as in Pancoast+ 2014 equation 12.\n\n\n\n\n\n","title":"BroadLineRegions.getG"},{"category":"method","location":"api/#BroadLineRegions.getGamma-Tuple{}","page":"API","text":"getGamma(;μ::Float64,β::Float64,F::Float64)\n\nReturns a Gamma distribution with parameters derived from the input parameters as in Pancoast+ 2014 equations 7-10.\n\n\n\n\n\n","title":"BroadLineRegions.getGamma"},{"category":"function","location":"api/#BroadLineRegions.getR","page":"API","text":"getR(rₛ::Float64,γ::Gamma{Float64},rng::AbstractRNG=Random.GLOBAL_RNG)\n\nReturns a random number drawn from the Gamma distribution γ and shifted by rₛ.\n\n\n\n\n\n","title":"BroadLineRegions.getR"},{"category":"function","location":"api/#BroadLineRegions.getR-2","page":"API","text":"getR(rₛ::Float64,μ::Float64,β::Float64,F::Float64,\n    g::Gamma{Float64},rng::AbstractRNG=Random.GLOBAL_RNG)\n\nReturns the radius r calculated using Pancoast+ 2014 equation 12, where g is a Gamma distribution and a random number is drawn from it.\n\n\n\n\n\n","title":"BroadLineRegions.getR"},{"category":"method","location":"api/#BroadLineRegions.getR-NTuple{5, Float64}","page":"API","text":"getR(rₛ::Float64,μ::Float64,β::Float64,F::Float64,g::Float64)\n\nReturns the radius r calculated using Pancoast+ 2014 equation 12, where g is already set.\n\n\n\n\n\n","title":"BroadLineRegions.getR"},{"category":"method","location":"api/#Base.:+-Tuple{BroadLineRegions.model, BroadLineRegions.model}","page":"API","text":"Base.:+(m1::model, m2::model) combines two models by concatenating their rings and camera parameters.\n\nCreate a new model with the combined rings and camera parameters, and updates the subModelStartInds accordingly.\n\n\n\n\n\n","title":"Base.:+"},{"category":"method","location":"api/#Base.getindex-Tuple{BroadLineRegions.model, Int64}","page":"API","text":"Base.getindex(m::model, i::Int)\n\nRetrieves the i-th submodel from the model m.\n\nThe extracted submodel carries the matching side of a :+ params provenance tree when that side can be identified unambiguously (so e.g. (a+b)[2].params == b.params and the result is rebuild-able); otherwise its params is nothing – in particular for operands built without a public constructor and for submodels sliced out of a multi-slot raytrace!d model (re-raytracing a slice alone is not equivalent to slicing the raytraced whole, so no faithful construction record exists).\n\n\n\n\n\n","title":"Base.getindex"},{"category":"method","location":"api/#Base.getindex-Tuple{BroadLineRegions.CompositeModel, String}","page":"API","text":"Base.getindex(cm::CompositeModel, line::String) -> model\n\nRetrieve the model registered for line. Errors (listing the known line names) if line is not present.\n\n\n\n\n\n","title":"Base.getindex"},{"category":"method","location":"api/#Base.length-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"Base.length(cm::CompositeModel) -> Int\n\nNumber of lines registered in cm.\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#Base.show-Tuple{IO, BroadLineRegions.CompositeModel}","page":"API","text":"Base.show(io::IO, cm::CompositeModel)\n\nPrint one row per line: name, lineCenter, fluxRatio, number of rings, and whether profiles have been computed (setProfile!/getProfile) for that line's model.\n\n\n\n\n\n","title":"Base.show"},{"category":"method","location":"api/#BroadLineRegions._finiteVRange-Tuple{BroadLineRegions.model}","page":"API","text":"_finiteVRange(m::model) -> (vmin, vmax)\n\nInternal helper: the definition of a line's velocity range – the min/max of v over points where isfinite(v) && isfinite(I*ΔA) (the W4-T4 finiteness rule; finite-I alone would let a NaN v poison the range). Single non-allocating pass over the memoized flattened v/I/ΔA gathers. Shared by lineOverlap, getSpectrum (and Workstream 5) so the three cannot disagree.\n\nReturns (NaN, NaN) when the model has no finite points; callers treat that as \"this line contributes no range\" (skip it).\n\n\n\n\n\n","title":"BroadLineRegions._finiteVRange"},{"category":"method","location":"api/#BroadLineRegions._fluxWeights-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"_fluxWeights(cm::CompositeModel) -> Dict{String,Float64}\n\nInternal helper: per-line multiplicative weight fluxRatios[line] / totalFlux(line) that renormalizes each line's binned profile to unit integral before scaling by its fluxRatio – the integrated-flux semantics of decision 3. totalFlux(line) = Σ I[k]*ΔA[k] over points where isfinite(v[k]) && isfinite(I[k]*ΔA[k]) – both conditions, matching exactly what binAccumulate! counts (finite-I/NaN-position points occur in practice, so finite-I alone is not enough).\n\nUses the memoized flattened gathers (getVariable(m, …; flatten=true) hits model.cache) and one non-allocating loop. Deliberately not memoized itself – it recomputes per call so nothing new joins the model.cache invalidation contract; the O(N) sum is noise next to the binning it accompanies.\n\nErrors (naming the offending line) if any line's total flux is not positive: a zero-emission line cannot be normalized to unit integral, and the alternative – an Inf weight whose non-finite weighted fluxes the binning silently drops – would make the line quietly vanish from the spectrum.\n\n\n\n\n\n","title":"BroadLineRegions._fluxWeights"},{"category":"method","location":"api/#BroadLineRegions.addLine!-Tuple{BroadLineRegions.CompositeModel, BroadLineRegions.model}","page":"API","text":"addLine!(cm::CompositeModel, m::model; line::String, lineCenter::Float64, fluxRatio::Float64=1.0) -> cm\n\nRegister an independently built model (which may itself be a +-combined, raytraced multi-component model) as a new line in cm. Returns cm.\n\nParameters\n\nline::String: unique (case-sensitive) name for this line.\nlineCenter::Float64: central wavelength (same units convention as cm's other lines). Must be positive.\nfluxRatio::Float64=1.0: this line's velocity-integrated flux relative to cm.lines[1]. Must be positive.\n\n\n\n\n\n","title":"BroadLineRegions.addLine!"},{"category":"method","location":"api/#BroadLineRegions.addLine!-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"addLine!(cm::CompositeModel; line::String, lineCenter::Float64, fluxRatio::Float64=1.0,\n         from::String=cm.lines[1], overrides...) -> cm\n\nAdd a new line by parameter reuse: rebuild the recorded construction parameters (model.params, see the model docstring and Workstream 4-T1) of the from line, merged with overrides, and register the result under line. Because rebuild handles nested :+/:raytrace! records, this works for combined and raytraced from lines too – overrides broadcast to every leaf submodel that has a matching parameter (so e.g. overriding τ on a raytraced disk+cloud line reaches both submodels' intensity functions, if both accept τ).\n\nErrors with a clear message if the from model has no recorded params (params === nothing, i.e. it was not built by a public constructor) – pass an explicit model via the other addLine! method instead.\n\nOverride semantics – constructor semantics, no geometry invariance\n\nrebuild does exactly what calling the original public constructor with the merged arguments would do, nothing more – no geometry invariance is promised. Whether an override moves geometry depends on which parameterization was recorded:\n\n:DiskWindModelMean (i.e. DiskWindModel(r̄, rFac, α, i; ...)): α (and r̄, rFac) set rMin/rMax via get_rMinMaxDiskWind, so overriding any of them moves the whole grid.\n:DiskWindModel (i.e. explicit DiskWindModel(rMin, rMax, i; ...)): α reaches only the intensity function via kwargs – geometry (r, ϕ, v) is untouched, only I changes.\n\nCloud-model seed rule\n\nThe stored rng/seed live at the leaves of a (possibly nested) params tree, so this rule is applied per leaf, recursively:\n\nrng=:philox and the caller does not override seed -> the stored seed is reused automatically (unmentioned override keys are left as recorded), producing identical clouds.\nrng=:philox and the caller overrides seed -> fresh gas from the new seed. Overriding with seed=nothing is rejected with a clear error (cloudModel(...; rng=:philox) requires an explicit integer seed) – philox cannot draw without one.\nrng=:legacy (the model consumed Random.GLOBAL_RNG at construction time) -> a single @warn is emitted noting that gas geometry cannot be reproduced exactly and will be re-drawn (statistically equivalent, not identical) from GLOBAL_RNG.\n\nNot recorded\n\nparams captures construction history only. In-place mutators (removeNaN!, zeroDiskObscuredClouds!) are not recorded and are not re-applied by rebuild/ addLine! – see the rebuild docstring's \"Not recorded\" section. A caller who ran such mutators on the from model must re-apply the same calls, in the same order, to the newly added line's model.\n\nParameters\n\nline::String: unique (case-sensitive) name for the new line.\nlineCenter::Float64: central wavelength. Must be positive.\nfluxRatio::Float64=1.0: this line's flux relative to cm.lines[1]. Must be positive.\nfrom::String=cm.lines[1]: which registered line's params to reuse.\noverrides...: forwarded to rebuild (constructor-argument overrides).\n\n\n\n\n\n","title":"BroadLineRegions.addLine!"},{"category":"method","location":"api/#BroadLineRegions.getSpectrum-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"getSpectrum(cm::CompositeModel; bins=100, z=0.0, kwargs...)\n    -> (edges, centers, flux::Dict{String,Vector{Float64}}, total::Vector{Float64})\n\nCombined wavelength-space spectrum of all lines. Each line's flattened points are binned by wavelength(v, lineCenter)*(1+z) weighted by I*ΔA*_fluxWeights(cm)[line], over a shared wavelength grid spanning every line's finite range (from _finiteVRange) times (1+z). total is the elementwise sum of the per-line vectors. With the unit-integral weighting, sum(flux[line]) equals fluxRatios[line] (decision 3).\n\nArguments\n\nbins::Union{Int,Vector{Float64}}=100: bin count, or an explicit edge vector (passthrough as in binnedSum). For an integer count no edge vector is materialized – the same minX/maxX go to every line's binnedSum, whose deterministic constructBinEdges yields bit-identical edges and preserves the uniform direct-index fast path.\nz::Real=0.0: redshift; z=0 = rest frame, z>0 shifts to the observed frame.\nminX/maxX::Union{Real,Nothing}=nothing: pin one or both ends of the shared wavelength grid (observed frame, i.e. after the (1+z) shift) instead of auto-computing them from the lines' finite ranges – forwarded to every line's binnedSum like the other binning keywords the simple model methods forward. Ignored when bins is an edge vector (the edges pin the grid).\ncentered::Bool=false: forwarded to binnedSum/constructBinEdges. The default false builds edges at exactly [minX, maxX]; centered=true pads them half a bin so the extremes fall in bin centers, as in the getProfile default.\noverflow::Union{Bool,Nothing}=nothing: an explicit true/false is forwarded to binnedSum untouched. The default (nothing) resolves to true for integer bins and false for a user-supplied edge vector: non-centered internal edges built at exactly [λmin, λmax] place each line's extremal points on the boundary, which binnedSum otherwise drops (deliberate package-wide convention) – overflow=true is the sanctioned mechanism to keep them, is what makes the integrated flux exact, and (for a user-narrowed minX/maxX window) keeps out-of-window flux by accumulating it into the boundary bins. Pass overflow=false explicitly for drop-outside-the-window semantics; against a user edge vector (a data grid) out-of-range flux drops by default.\n\n\n\n\n\n","title":"BroadLineRegions.getSpectrum"},{"category":"method","location":"api/#BroadLineRegions.getVariable-Tuple{BroadLineRegions.CompositeModel, Union{Function, String, Symbol}}","page":"API","text":"getVariable(cm::CompositeModel, variable; line::String, flatten=false)\n\nPer-line forwarding of getVariable: gather variable from the model registered under line.\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"method","location":"api/#BroadLineRegions.gpu-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"gpu(cm::CompositeModel; T=Float32, kwargs...) -> ResidentCompositeModel\n\nMove every line's model onto the GPU with the per-model gpu (each line gets the same keyword arguments, e.g. T=Float32) and wrap the per-line ResidentModel handles as a ResidentCompositeModel. Requires CUDA.jl to be loaded so the package extension can provide the CUDA-backed gpu(::model) method — without it the gpu(::Any) fallback error fires per line.\n\n\n\n\n\n","title":"BroadLineRegions.gpu"},{"category":"function","location":"api/#BroadLineRegions.image","page":"API","text":"image(cm::CompositeModel, [variable]; line::String, kwargs...)\n\nPer-line forwarding of the image recipe: renders cm[line] exactly as image(cm[line], variable; kwargs...) would. line is required (no default) – unlike plot3d, an intensity image has no meaningful \"overlay all lines\" mode (each line's I is in its own arbitrary units, so mixing them in one color scale would be misleading).\n\n\n\n\n\n","title":"BroadLineRegions.image"},{"category":"function","location":"api/#BroadLineRegions.image-2","page":"API","text":"image(rcm::ResidentCompositeModel, [variable]; line::String, kwargs...)\n\nPer-line forwarding of the image recipe for a ResidentCompositeModel: renders rcm[line] exactly as image(rcm[line], variable; kwargs...) would (the ResidentModel recipe path – host copies of that line's device columns). As on the host composite, line is required (no default): an intensity image has no meaningful \"overlay all lines\" mode. variable must be a ModelArrays column name (custom Function variables need the host model path).\n\n\n\n\n\n","title":"BroadLineRegions.image"},{"category":"method","location":"api/#BroadLineRegions.lineOverlap-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"lineOverlap(cm::CompositeModel) -> Vector{NamedTuple}\n\nReport wavelength-space overlaps between lines. Each line spans [lineCenter*(1+vmin), lineCenter*(1+vmax)] from _finiteVRange; every pair whose intervals intersect yields an entry (lineA, lineB, λlo, λhi) (the overlap interval [λlo, λhi]). An empty vector means no lines overlap. Lines with no finite points (_finiteVRange = NaN) are skipped.\n\nA ResidentCompositeModel is supported through the same shared implementation (the ::ResidentCompositeModel method lives with its resident siblings in gpu_observables.jl, W4-G3); the only difference is that each line's velocity range then comes from the device reductions of _finiteVRange(::ResidentModel) instead of the host gathers.\n\n\n\n\n\n","title":"BroadLineRegions.lineOverlap"},{"category":"method","location":"api/#BroadLineRegions.lineRatio-Tuple{BroadLineRegions.CompositeModel, String, String}","page":"API","text":"lineRatio(cm::CompositeModel, a::String, b::String) -> Float64\n\nIntegrated flux ratio of line a to line b. By the integrated-flux semantics of fluxRatio (each line's profile is normalized to unit integral before scaling, see _fluxWeights) this is exactly cm.fluxRatios[a] / cm.fluxRatios[b] – the constant that the velocity-resolved getProfile(cm, :ratio; lines=(a, b)) profile integrates to (and equals bin-by-bin in the degenerate identical-geometry case). Errors (listing the known lines) if either name is not registered.\n\nA ResidentCompositeModel is supported through the same shared implementation (the ::ResidentCompositeModel method lives with its resident siblings in gpu_observables.jl, W5-G1) – fluxRatios is host metadata on both, so the two methods are identical.\n\n\n\n\n\n","title":"BroadLineRegions.lineRatio"},{"category":"method","location":"api/#BroadLineRegions.plot3d-Tuple{BroadLineRegions.CompositeModel, Vararg{Any}}","page":"API","text":"plot3d(cm::CompositeModel, [variable], [annotate]; line=nothing, kwargs...)\n\nPer-line forwarding of the plot3d recipe for a CompositeModel.\n\nline::String: identical to plot3d(cm[line], variable, annotate; kwargs...).\nline=nothing (default): overlay every line's geometry on one 3D plot, one solid color per line. Reuses _plot3d_submodels – the same per-submodel point gather the single-model plot3d recipe uses – for each line's (x,y,z) (and its NaN mask on variable); unlike the single-model recipe this does NOT color points by variable's value (a shared colormap across lines with independently-scaled intensities/variables would not be meaningful) – points are colored by line instead, and variable only selects which column supplies the per-point finite/NaN mask (default geometry, i.e. no masking, matching the single-model recipe's default).\n\n\n\n\n\n","title":"BroadLineRegions.plot3d"},{"category":"method","location":"api/#BroadLineRegions.plot3d-Tuple{BroadLineRegions.ResidentCompositeModel, Vararg{Any}}","page":"API","text":"plot3d(rcm::ResidentCompositeModel, [variable], [annotate]; line=nothing, kwargs...)\n\nPer-line forwarding of the plot3d recipe for a ResidentCompositeModel, matching plot3d(::CompositeModel, …):\n\nline::String: identical to plot3d(rcm[line], variable, annotate; kwargs...) (the ResidentModel recipe path).\nline=nothing (default): overlay every line's geometry on one 3D plot, one solid color per line, via the same shared implementation as the host composite overlay. The _plot3d_submodels(::ResidentModel, …) restrictions apply per line: a multi-submodel line must carry raytrace metadata (build the handle with raytrace=true), and custom-Function mask variables are not supported (pass a column Symbol, or use the host model).\n\n\n\n\n\n","title":"BroadLineRegions.plot3d"},{"category":"method","location":"api/#BroadLineRegions.raytrace!-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"raytrace!(cm::CompositeModel; line=nothing, kwargs...) -> cm\n\nPer-line forwarding of raytrace!. line=nothing (the default) raytraces every line; otherwise only the named line.\n\nraytrace! returns a new model (it does not mutate its argument), so this reassigns cm.models[line] = raytrace!(cm.models[line]; kwargs...). Lines that raytrace! would warn about and hand back unaltered – a single-submodel model (subModelStartInds == [1]) or an already-raytraced one (camera.raytraced) – are skipped silently so a multi-line loop does not warn-spam.\n\nRaytracing happens within each line's own submodels only. Cross-line occlusion is deliberately not modeled: different lines are at different wavelengths and each line's τ is that line's own optical depth (see the Workstream-4 plan, \"Out of scope\").\n\n\n\n\n\n","title":"BroadLineRegions.raytrace!"},{"category":"method","location":"api/#BroadLineRegions.removeNaN!-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"removeNaN!(cm::CompositeModel) -> cm\n\nPer-line forwarding of removeNaN!: drop NaN-sentinel points from every line's model.\n\n\n\n\n\n","title":"BroadLineRegions.removeNaN!"},{"category":"method","location":"api/#BroadLineRegions.reset!-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"reset!(cm::CompositeModel; line=nothing, profiles=true, img=false) -> cm\n\nPer-line forwarding of reset!. line=nothing (the default) resets every line; otherwise only the named line. Extra keywords (profiles, img) forward to the per-line model method.\n\n\n\n\n\n","title":"BroadLineRegions.reset!"},{"category":"method","location":"api/#BroadLineRegions.resident-Tuple{BroadLineRegions.CompositeModel}","page":"API","text":"resident(cm::CompositeModel; T=Float64, backend=KernelAbstractions.CPU(), raytrace=false)\n    -> ResidentCompositeModel\n\nFlatten every line's model with the per-model resident (each line gets the same keyword arguments) and wrap the per-line ResidentModel handles as a ResidentCompositeModel. The default CPU() backend keeps everything on the host — useful for testing the resident composite pipeline without a GPU. Use gpu(cm) (with CUDA.jl loaded) to build a device-resident composite.\n\n\n\n\n\n","title":"BroadLineRegions.resident"},{"category":"method","location":"api/#BroadLineRegions.setProfile!-Tuple{BroadLineRegions.CompositeModel, profile}","page":"API","text":"setProfile!(cm::CompositeModel, p::profile; line::String, overwrite=false) -> cm\n\nPer-line forwarding of setProfile!: store profile p on the model registered under line.\n\n\n\n\n\n","title":"BroadLineRegions.setProfile!"},{"category":"method","location":"api/#BroadLineRegions.spectrum!-Tuple","page":"API","text":"spectrum(cm::CompositeModel; z=0.0, bins=100, kwargs...)\nspectrum(rcm::ResidentCompositeModel; z=0.0, bins=100, kwargs...)\n\nCombined wavelength-space spectrum plot: one series per line plus a black \"total\" series (both from getSpectrum), with pairwise line overlaps (lineOverlap) shaded as gray bands behind the curves.\n\nA ResidentCompositeModel works through the same recipe body with no extra branch (W4-G3): getSpectrum and lineOverlap resolve at call time and dispatch to the resident methods (gpu_observables.jl), which run the per-line reductions/binning on the device. The resident getSpectrum restrictions apply (a user-supplied bins edge vector must be uniform).\n\nKeywords\n\nz::Real=0.0: redshift, forwarded to getSpectrum (z=0 = rest frame; matches getSpectrum's observed-frame convention).\nbins::Union{Int,Vector{Float64}}=100: bin count or explicit edge vector, forwarded to getSpectrum.\nother kwargs...: ordinary Plots attributes.\n\nlineOverlap reports rest-frame wavelength intervals; they are scaled by (1+z) here before shading so the bands line up with the (possibly redshifted) spectrum.\n\n\n\n\n\n","title":"BroadLineRegions.spectrum!"},{"category":"method","location":"api/#BroadLineRegions.spectrum!-Tuple{RecipesBase.AbstractPlot, Vararg{Any}}","page":"API","text":"spectrum(cm::CompositeModel; z=0.0, bins=100, kwargs...)\nspectrum(rcm::ResidentCompositeModel; z=0.0, bins=100, kwargs...)\n\nCombined wavelength-space spectrum plot: one series per line plus a black \"total\" series (both from getSpectrum), with pairwise line overlaps (lineOverlap) shaded as gray bands behind the curves.\n\nA ResidentCompositeModel works through the same recipe body with no extra branch (W4-G3): getSpectrum and lineOverlap resolve at call time and dispatch to the resident methods (gpu_observables.jl), which run the per-line reductions/binning on the device. The resident getSpectrum restrictions apply (a user-supplied bins edge vector must be uniform).\n\nKeywords\n\nz::Real=0.0: redshift, forwarded to getSpectrum (z=0 = rest frame; matches getSpectrum's observed-frame convention).\nbins::Union{Int,Vector{Float64}}=100: bin count or explicit edge vector, forwarded to getSpectrum.\nother kwargs...: ordinary Plots attributes.\n\nlineOverlap reports rest-frame wavelength intervals; they are scaled by (1+z) here before shading so the bands line up with the (possibly redshifted) spectrum.\n\n\n\n\n\n","title":"BroadLineRegions.spectrum!"},{"category":"method","location":"api/#BroadLineRegions.spectrum-Tuple","page":"API","text":"spectrum(cm::CompositeModel; z=0.0, bins=100, kwargs...)\nspectrum(rcm::ResidentCompositeModel; z=0.0, bins=100, kwargs...)\n\nCombined wavelength-space spectrum plot: one series per line plus a black \"total\" series (both from getSpectrum), with pairwise line overlaps (lineOverlap) shaded as gray bands behind the curves.\n\nA ResidentCompositeModel works through the same recipe body with no extra branch (W4-G3): getSpectrum and lineOverlap resolve at call time and dispatch to the resident methods (gpu_observables.jl), which run the per-line reductions/binning on the device. The resident getSpectrum restrictions apply (a user-supplied bins edge vector must be uniform).\n\nKeywords\n\nz::Real=0.0: redshift, forwarded to getSpectrum (z=0 = rest frame; matches getSpectrum's observed-frame convention).\nbins::Union{Int,Vector{Float64}}=100: bin count or explicit edge vector, forwarded to getSpectrum.\nother kwargs...: ordinary Plots attributes.\n\nlineOverlap reports rest-frame wavelength intervals; they are scaled by (1+z) here before shading so the bands line up with the (possibly redshifted) spectrum.\n\n\n\n\n\n","title":"BroadLineRegions.spectrum"},{"category":"method","location":"api/#BroadLineRegions.wavelength-Tuple{Any, Any}","page":"API","text":"wavelength(v, lineCenter) -> λ\n\nFirst-order velocity→wavelength map λ = lineCenter * (1 + v) (decision 4), with v the stored line-of-sight velocity in units of c. Works for scalar or vector v (broadcasts). Stored v is redshift-positive (positive v = receding), so (1 + v) is the correct sign – do not re-derive it.\n\n\n\n\n\n","title":"BroadLineRegions.wavelength"},{"category":"type","location":"api/#BroadLineRegions.CompositeModel","page":"API","text":"CompositeModel\n\nHolds an ordered collection of line => model pairs so that several broad emission lines (e.g. Hα + Hβ + CIV) can be modeled together. The existing model/ring/ raytrace!/cache/GPU code paths are unmodified and know nothing about lines – CompositeModel is purely a wrapper around independently valid model objects.\n\nFields\n\nlines::Vector{String}: unique line names, in creation order. lines[1] is the reference line (the implicit from default for addLine! parameter reuse, and the line every fluxRatio is relative to).\nmodels::Dict{String,model}: the per-line model object.\nlineCenters::Dict{String,Float64}: central wavelength per line, in any units – consistent units across lines in one CompositeModel are the caller's responsibility (only Δλ/λ matters downstream).\nfluxRatios::Dict{String,Float64}: each line's velocity-integrated line flux relative to lines[1] (so fluxRatios[lines[1]] == 1.0).\n\nThere is deliberately no lines(cm) accessor – cm.lines is the public API for the ordered name list (a lines(...) function would also collide with Workstream 5's lines::Tuple keyword).\n\n\n\n\n\n","title":"BroadLineRegions.CompositeModel"},{"category":"method","location":"api/#BroadLineRegions.CompositeModel-Tuple{BroadLineRegions.model}","page":"API","text":"CompositeModel(m::model; line::String, lineCenter::Float64)\n\nWrap an existing model as the first (reference) line of a new CompositeModel. The reference line's fluxRatio is fixed at 1.0 – every subsequent line's fluxRatio (see addLine!) is defined relative to it.\n\nParameters\n\nm::model: the model for this line (disk-wind, cloud, or a +-combined/raytraced model).\nline::String: unique name for this line (e.g. \"Hα\").\nlineCenter::Float64: central wavelength (any units, consistent across lines you plan to add to this CompositeModel). Must be positive.\n\nReturns\n\nCompositeModel with one registered line.\n\n\n\n\n\n","title":"BroadLineRegions.CompositeModel"},{"category":"method","location":"api/#BroadLineRegions.rebuild-Tuple{NamedTuple}","page":"API","text":"rebuild(params::NamedTuple; overrides...) -> model\nrebuild(m::model; overrides...) -> model\n\nReconstruct a model from the construction provenance recorded in params (see the model params field), optionally replacing individual constructor arguments via overrides.\n\nrebuild dispatches on params.constructor:\n\na leaf record (:DiskWindModel, :DiskWindModelMean, :cloudModel, :cloudModelVectors) calls the corresponding public constructor with its recorded arguments;\na :+ record rebuilds left and right and sums them;\na :raytrace! record rebuilds parent and re-raytraces it with the recorded raytrace arguments.\n\nOverrides\n\noverrides... broadcast to every leaf in which the key exists (so an override reaches all submodels of a combined/raytraced model that share that parameter). An override key that matches no leaf is an error (typo guard). Override semantics are exactly constructor semantics – whether an override moves geometry depends on the recorded parameterization. For example overriding α on a :DiskWindModelMean record moves the whole grid (α sets rMin/rMax via get_rMinMaxDiskWind), while overriding α on a :DiskWindModel (explicit rMin/rMax) record only reaches the intensity function – geometry is untouched. rebuild does exactly what calling the constructor with the merged arguments would do, nothing more.\n\nNode-level arguments (IRatios, τCutOff, raytraceFreeClouds) are not override targets in v1: to change them, rebuild the parent and re-raytrace manually.\n\nReproducibility\n\nDisk-wind construction is deterministic, so a rebuilt disk-wind line has identical geometry. Cloud models reproduce identical clouds only under rng=:philox with the recorded seed; a rng=:legacy record consumed the GLOBAL_RNG state, so its rebuild re-draws statistically-equivalent (different) gas from GLOBAL_RNG.\n\nNot recorded\n\nparams captures construction history only. In-place mutators (removeNaN!, zeroDiskObscuredClouds!) are not recorded; a caller who ran them must re-apply the same calls in the same order to the rebuilt model. removeNaN! is observable-neutral (binned observables skip non-finite points), so the only unrecorded mutator with a physical effect is zeroDiskObscuredClouds!, which is deterministic and cheap to re-run.\n\nrebuild(m::model; ...) is a convenience method forwarding to rebuild(m.params; ...); it errors when m.params === nothing (the model was not built by a public constructor).\n\n\n\n\n\n","title":"BroadLineRegions.rebuild"},{"category":"method","location":"api/#BroadLineRegions.DiskWindIntensity-Tuple{}","page":"API","text":"DiskWindIntensity(;r::Union{Vector{Float64},Float64}, i::Float64, ϕ::Union{Vector{Float64},Float64},\n        f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64, rMin::Float64 = 0.0, rMax::Float64 = Inf, _...)\n\nCalculates the intensity from a disk-wind model of the BLR following the prescription given in Long+ 2023 and 2025, similar to Chiang and Murray 1996 and 1997, assumes optically thick line emission limit (Sobolev).\n\nArguments:\n\nr::Union{Vector{Float64},Float64} - radius from central mass (in terms of rₛ)\ni::Float64 - inclination angle (rad)\nϕ::Union{Vector{Float64},Float64} - list of azimuthal angles (rad)\nf1::Float64 - strength of radial velocity gradient mathrmdv_rmathrmdr\nf2::Float64 - strength of Keplerian shear velocity gradient mathrmdv_phimathrmdr\nf3::Float64 - strength of radial lifting velocity gradient mathrmdv_thetamathrmdr\nf4::Float64 - strength of vertical lifting velocity gradient mathrmdv_thetamathrmdtheta (equivalent to isotropic emission)\nα::Float64 - power-law index of the source function S(r) propto r^-alpha\nrMin::Float64 - minimum radius to consider (default: 0.0)\nrMax::Float64 - maximum radius to consider (default: Inf)\n\nReturns:\n\nintensity (arbitrary units) as a Vector{Float64}\n\n\n\n\n\n","title":"BroadLineRegions.DiskWindIntensity"},{"category":"method","location":"api/#BroadLineRegions.DiskWind_I_-NTuple{8, Float64}","page":"API","text":"DiskWind_I_(r::Float64, ϕ::Float64, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64)\n\nSame as DiskWind_I_(r::Vector{Float64}, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64) but for a single radius r and a single azimuthal angle ϕ.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWind_I_"},{"category":"method","location":"api/#BroadLineRegions.DiskWind_I_-Tuple{Float64, Vector{Float64}, Vararg{Float64, 6}}","page":"API","text":"DiskWind_I_(r::Float64, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64)\n\nSame as DiskWind_I_(r::Vector{Float64}, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64) but for a single radius r and a vector of azimuthal angles ϕ.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWind_I_"},{"category":"method","location":"api/#BroadLineRegions.DiskWind_I_-Tuple{Vector{Float64}, Vararg{Float64, 7}}","page":"API","text":"DiskWind_I_(r::Vector{Float64}, ϕ::Float64, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64)\n\nSame as DiskWind_I_(r::Vector{Float64}, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64) but for a vector of radii r and a single azimuthal angle ϕ.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWind_I_"},{"category":"method","location":"api/#BroadLineRegions.DiskWind_I_-Tuple{Vector{Float64}, Vector{Float64}, Vararg{Float64, 6}}","page":"API","text":"DiskWind_I_(r::Vector{Float64}, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64)\n\nCalculates the intensity of the disk wind at radius r from the central mass and inclined at angle i (rad) over grid of azimuthal angles ϕ (rad)     Follows the prescription given in Long+ 2023 and 2025, similar to Chiang and Murray 1996 and 1997, assumes optically thick line emission limit\n\nArguments:\n\nr::Vector{Float64} - radius from central mass (in terms of rₛ)\nϕ::Vector{Float64} - list of azimuthal angles (rad)\ni::Float64 - inclination angle (rad)\nf1::Float64 - strength of radial velocity gradient dvᵣ/dr\nf2::Float64 - strength of Keplerian shear dvϕ/dr\nf3::Float64 - strength of velocity gradient dvθ/dr\nf4::Float64 - strength of velocity gradient dvθ/dθ (or isotropic emission)\nα::Float64 - power-law index of the source function S(r) ∝ r^(-α)\n\nReturns:\n\nintensity (arbitrary units) as a Vector{Float64}\n\n\n\n\n\n","title":"BroadLineRegions.DiskWind_I_"},{"category":"method","location":"api/#BroadLineRegions.DiskWind_I_grid-Tuple{Vector{Float64}, Vector{Float64}, Vararg{Float64, 8}}","page":"API","text":"DiskWind_I_grid(r::Vector{Float64}, ϕ::Vector{Float64}, i::Float64, f1::Float64, f2::Float64,\n    f3::Float64, f4::Float64, α::Float64, rMin::Float64, rMax::Float64)\n\nTyped helper (function barrier) for DiskWindIntensity's vector path. Fills a single preallocated vector with the scalar DiskWind_I_ per point, then applies the (rMin, rMax) mask with the bounds rounded to 9 significant digits once instead of per point.\n\nThe outer mask guard replicates the original semantics exactly: when r contains NaN (out-of-grid sentinels), minimum(r)/maximum(r) are NaN, the guard comparisons are false, and no masking pass runs — NaN points keep their NaN intensity rather than being zeroed.\n\n\n\n\n\n","title":"BroadLineRegions.DiskWind_I_grid"},{"category":"method","location":"api/#BroadLineRegions.IsotropicIntensity-Tuple{}","page":"API","text":"IsotropicIntensity(;r::Union{Vector{Float64},Float64}, ϕ::Union{Vector{Float64},Float64}, \n        rescale::Float64=1.0, rMin::Float64 = 0.0, rMax::Float64 = Inf, _...)\n\nReturns a constant intensity value at all radii and azimuthal angles, rescaled by rescale factor.\n\n\n\n\n\n","title":"BroadLineRegions.IsotropicIntensity"},{"category":"method","location":"api/#BroadLineRegions.IϕCloudMask-Tuple{}","page":"API","text":"IϕCloudMask(;r::Float64, ϕ::Float64, θₒ::Float64, ϕ₀::Float64, rot::Float64, i::Float64, κ::Float64=0.0, \n        ϕMin::Float64, ϕMax::Float64, overdense::Bool=false, _...)\n\nCalculates the intensity using cloudIntensity but with an extra layer of masking based on azimuthal angle ϕ and the specified range [ϕMin, ϕMax]. If ϕ is outside this range, the intensity is set to 0.0. If overdense is true, the intensity is multiplied by 2.0 to account for the \"lost\" cloud and thus preserve total intensity in the model.\n\n\n\n\n\n","title":"BroadLineRegions.IϕCloudMask"},{"category":"method","location":"api/#BroadLineRegions.IϕDiskWindMask-Tuple{}","page":"API","text":"IϕDiskWindMask(;r::Union{Vector{Float64},Float64}, ϕ::Union{Vector{Float64},Float64}, i::Float64, \n        f1::Float64, f2::Float64, f3::Float64, f4::Float64, α::Float64, ϕMin::Float64, ϕMax::Float64, _...)\n\nCalculates the intensity using DiskWindIntensity but with an extra layer of masking based on azimuthal angle ϕ and the specified range [ϕMin, ϕMax]. If ϕ is outside this range, the intensity is set to 0.0.\n\n\n\n\n\n","title":"BroadLineRegions.IϕDiskWindMask"},{"category":"method","location":"api/#BroadLineRegions.cloudIntensity-Tuple{}","page":"API","text":"cloudIntensity(;r::Float64, ϕ::Float64, θₒ::Float64, ϕ₀::Float64, rot::Float64, i::Float64, κ::Float64=0.0, _...)\n\nCalculate the intensity of the cloud at radius r from the central mass and inclined at angle i (rad) over grid of azimuthal angles ϕ (rad) following Pancoast+ 2011 and 2014.\n\nArguments\n\nr::Float64: radius from central mass (in terms of r₍ₛ₎)\nϕ::Float64: azimuthal angle (rad)\nθₒ::Float64: opening angle of cloud\nϕ₀::Float64: initial azimuthal angle\nrot::Float64: rotation angle\ni::Float64: inclination angle\nκ::Float64: anisotropy parameter\n\nReturns\n\nIntensity (arbitrary units) as a Float64\n\n\n\n\n\n","title":"BroadLineRegions.cloudIntensity"},{"category":"function","location":"api/#BroadLineRegions.vCirc","page":"API","text":"vCirc(r::Float64, rₛ::Float64=1.0)\n\nCalculate circular velocity at radius r from central mass, with Schwarzschild radius rₛ.\n\nDefaults to rₛ=1.0 for unitless calculations.\n\n\n\n\n\n","title":"BroadLineRegions.vCirc"},{"category":"method","location":"api/#BroadLineRegions.vCircularCloud-Tuple{}","page":"API","text":"vCircularCloud(;r::Float64, ϕ₀::Float64, i::Float64, rot::Float64, θₒ::Float64, rₛ::Float64=1.0, reflect::Bool=false, _...)\n\nCalculate line of sight velocity for cloud in 3D space.\n\nArguments\n\nr::Float64: radius from central mass (in terms of rₛ)\nϕ₀::Float64: starting azimuthal angle in ring plane (rad)\ni::Float64: inclination angle of ring plane (rad)\nrot::Float64: rotation of system plane about z axis (rad)\nθₒ::Float64: opening angle of point\nrₛ::Float64=1.0: Schwarzschild radius (optional, to convert to physical units)\nreflect::Bool=false: whether the point is reflected across the midplane of the disk\n_...: extra kwargs, ignored\n\nReturns\n\nLine of sight velocity (Float64)\n\n\n\n\n\n","title":"BroadLineRegions.vCircularCloud"},{"category":"method","location":"api/#BroadLineRegions.vCircularDisk-Tuple{}","page":"API","text":"vCircularDisk(;r::Union{Float64,Vector{Float64}}, i::Float64, ϕ::Union{Vector{Float64},Float64}, rₛ=1.0, _...)\n\nCalculate line of sight velocity for circular orbit at radius r from central mass and inclined at angle i (rad) over grid of azimuthal angles ϕ (rad).\n\n\n\n\n\n","title":"BroadLineRegions.vCircularDisk"},{"category":"method","location":"api/#BroadLineRegions.vCircularRadialDisk-Tuple{}","page":"API","text":"vCircularRadialDisk(;r::Union{Float64,Vector{Float64}}, i::Float64, ϕ::Union{Vector{Float64},Float64}, \n        vᵣFrac::Union{Vector{Float64},Float64}=0.0, inflow::Union{Vector{Bool},Bool}=true, rₛ=1.0, _...)\n\nCalculate line of sight velocity for circular orbit at radius r from central mass and inclined at angle i (rad) over grid of azimuthal angles ϕ (rad) with radial inflow/outflow.\n\n\n\n\n\n","title":"BroadLineRegions.vCircularRadialDisk"},{"category":"method","location":"api/#BroadLineRegions.vCloudTurbulentEllipticalFlow-Tuple{}","page":"API","text":"vCloudTurbulentEllipticalFlow(;σρᵣ::Float64, σρc::Float64, σΘᵣ::Float64, σΘc::Float64, \n        θₑ::Float64, fEllipse::Float64, fFlow::Float64, σₜ::Float64, r::Float64, \n        i::Float64, rot::Float64, θₒ::Float64, rₛ::Float64=1.0, ϕ₀::Float64=0.0, \n        reflect::Bool=false, rng::AbstractRNG=Random.GLOBAL_RNG, _...)\n\nCalculate line of sight velocity for cloud in 3D space with potential for elliptical orbital velocities, in/outflow, and turbulence as in Pancoast+14.\n\nArguments\n\nσρᵣ::Float64: Radial standard deviation around radial orbits\nσρc::Float64: Radial standard deviation around circular orbits\nσΘᵣ::Float64: Angular standard deviation around radial orbits\nσΘc::Float64: Angular standard deviation around circular orbits\nθₑ::Float64: Angle in vϕ-vr plane\nfEllipse::Float64: Fraction of elliptical orbits\nfFlow::Float64: If < 0.5, inflow, otherwise, outflow\nσₜ::Float64: Standard deviation of turbulent velocity \nr::Float64: Radius from central mass (in terms of rₛ)\ni::Float64: Inclination angle of ring plane (rad)\nrot::Float64: Rotation of system plane about z axis (rad)\nθₒ::Float64: Opening angle of point\nrₛ::Float64=1.0: Schwarzschild radius (optional, to convert to physical units)\nϕ₀::Float64=0.0: Starting azimuthal angle in ring plane (rad)\nreflect::Bool=false: Whether the point is reflected across the midplane of the disk\nrng::AbstractRNG=Random.GLOBAL_RNG: Random number generator\n_...: Extra kwargs, ignored\n\nReturns\n\nLine of sight velocity (Float64)\n\n\n\n\n\n","title":"BroadLineRegions.vCloudTurbulentEllipticalFlow"},{"category":"method","location":"api/#BroadLineRegions.binAccumulate!-Tuple{Vector{Float64}, Array{Float64}, Array{Float64}, Vector{Float64}, Bool, Bool}","page":"API","text":"binAccumulate!(result, x, y, binEdges, overflow::Bool, uniform::Bool)\n\nTyped accumulation loop for binnedSum (function barrier). When uniform is true the bin index is computed directly from the spacing (floor) and then corrected against the actual edges, which keeps the assignment bit-identical to searchsortedfirst — including the exact-edge convention (a point exactly on an interior edge belongs to the bin to its left) and any floating-point rounding of the direct computation. Non-uniform (user-supplied) edges use searchsortedfirst as before.\n\n\n\n\n\n","title":"BroadLineRegions.binAccumulate!"},{"category":"function","location":"api/#BroadLineRegions.binModel","page":"API","text":"binModel(bins::Union{Int,Vector{Float64}}=100; m::model, yVariable::Union{String,Symbol,Function}, \n        xVariable::Union{String,Symbol,Function}=:v, kwargs...)\nbinModel(bins::Vector{Float64}, dx::Array{Float64,}; m::model, yVariable::Union{String,Symbol,Function}, \n        xVariable::Union{String,Symbol,Function}=:v, kwargs...)\n\nBin the model into a histogram, where each bin is the integrated value of the yVariable as a function of the xVariable.\n\nArguments\n\nm::model: Model object to bin\nyVariable::Union{String,Symbol,Function}: Variable to bin\nMust be valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nbins::Union{Int,Vector{Float64}}: Number of bins or bin edges for binning\nIf Int: Number of bins with edges equally spaced between min/max of xVariable\nIf Vector{Float64}: Specific bin edges, with number of bins = length(bins)-1\nBins are left-edge exclusive, right-edge inclusive: a point exactly on an interior edge goes to the bin below it. Points at or below the first edge, or at or above the last edge, are only counted when overflow=true (into the first/last bin)\nxVariable::Union{String,Symbol,Function}=:v: Variable to bin over\nMust be a valid attribute of model.rings or a function that can be applied to model.rings\ndx::Array{Float64,}: Integration element for each bin\nIf provided, used as the associated integral element\nOtherwise defaults to ΔA in each ring struct\n\nReturns\n\nTuple{Vector{Float64},Vector{Float64},Vector{Float64}}: A tuple containing:\nbinEdges: Bin edges for the xVariable of the histogram\nbinCenters: Bin centers for the xVariable\nyBinned: Binned values of the yVariables\n\n\n\n\n\n","title":"BroadLineRegions.binModel"},{"category":"method","location":"api/#BroadLineRegions.binWeightedMean-Tuple{BroadLineRegions.model, Vararg{Any, 5}}","page":"API","text":"binWeightedMean(m::model, x, yNum, yDen, bins, dx; kwargs...)\n\nShared helper for the weighted-mean profile options of getProfile (:delay, :r, :ϕ): bins yNum.*dA and yDen.*dA over x and returns (edges, centers, num./den). Replicates binModel's ΔA shape fix-up (per-ring scalar ΔA broadcast across a matrix y). Operates on precomputed arrays so the gathers can come from the model's memoized cache.\n\n\n\n\n\n","title":"BroadLineRegions.binWeightedMean"},{"category":"method","location":"api/#BroadLineRegions.binnedSum-Tuple{Array{Float64}, Array{Float64}}","page":"API","text":"binnedSum(x::Array{Float64,}, y::Array{Float64, }; bins=100,\n        overflow=false, centered=true, minX=nothing, maxX=nothing)\n\nBin the x and y variables into a histogram, where each bin is the sum of the y values for the corresponding x values.\n\nArguments\n\nx::Array{Float64,}: x variable to bin over\ny::Array{Float64,}: y variable to bin\nbins::Union{Int,Vector{Float64}}=100: Number of bins or bin edges for binning\nIf Int: Number of bins with edges equally spaced between min/max of x\nIf Vector{Float64}: Specific bin edges, with number of bins = length(bins)-1\nBins are left-edge exclusive, right-edge inclusive: a point exactly on an interior edge goes to the bin below it. Points at or below the first edge, or at or above the last edge, are only counted when overflow=true (into the first/last bin)\noverflow::Bool=false: If true, include values outside bin range in the first/last bins\ncentered::Bool=true: If true, shift bin edges to center around middle value\nminX::Union{Float64,Nothing}=nothing: Minimum value of x for binning (defaults to minimum(x))\nmaxX::Union{Float64,Nothing}=nothing: Maximum value of x for binning (defaults to maximum(x))\n\nReturns\n\nTuple{Vector{Float64},Vector{Float64},Vector{Float64}}: A tuple containing:\nbinEdges: Bin edges for the x variable\nbinCenters: Bin centers for the x variable\nresult: Binned sums of the y variable\n\n\n\n\n\n","title":"BroadLineRegions.binnedSum"},{"category":"method","location":"api/#BroadLineRegions.binnedVariance-Tuple{Array{Float64}, Array{Float64}, Array{Float64}}","page":"API","text":"binnedVariance(x::Array{Float64,}, θ::Array{Float64,}, w::Array{Float64,};\n        bins=100, overflow=false, kwargs...)\n\nPer-bin weighted variance of θ: bin the x variable as in binnedSum, then in each bin compute the w-weighted variance sigma^2 = sum_k w_k(theta_k-mu)^2  sum_k w_k with μ the w-weighted mean of θ in that bin.\n\nThe variance is computed with a two-pass algorithm (pass 1: per-bin Σw and Σwθ → per-bin mean μ; pass 2: per-bin Σw(θ-μ)²/Σw) rather than the raw-moment identity σ² = ⟨θ²⟩ - ⟨θ⟩², which suffers catastrophic cancellation when the per-bin mean is large compared to the spread (e.g. a few clouds clustered tightly far from the bin mean). The two-pass form is also guaranteed non-negative. Bin membership in pass 2 follows the same rules as binnedSum, so the two can never disagree.\n\nArguments\n\nx::Array{Float64,}: x variable to bin over\nθ::Array{Float64,}: variable to take the variance of\nw::Array{Float64,}: weights (e.g. intensity × area)\nbins::Union{Int,Vector{Float64}}=100: number of bins or bin edges, as in binnedSum\noverflow::Bool=false: if true, include values outside the bin range in the first/last bins\nAdditional kwargs (centered, minX, maxX) are passed to binnedSum\n\nReturns\n\nTuple{Vector{Float64},Vector{Float64},Vector{Float64}}: A tuple containing:\nbinEdges: Bin edges for the x variable\nbinCenters: Bin centers for the x variable\nσ²: per-bin weighted variance of θ (NaN for empty bins)\n\n\n\n\n\n","title":"BroadLineRegions.binnedVariance"},{"category":"method","location":"api/#BroadLineRegions.constructBinEdges-Tuple{Int64, Float64, Float64, Bool}","page":"API","text":"constructBinEdges(bins::Int, minX::Float64, maxX::Float64, centered::Bool) -> Vector{Float64}\n\nBuild the uniform velocity-bin edges for an integer bin count, given the (NaN-resolved) data range minX/maxX. Shared by binnedSum and the GPU-resident profile path so the two cannot diverge on the centered edge convention.\n\n\n\n\n\n","title":"BroadLineRegions.constructBinEdges"},{"category":"method","location":"api/#BroadLineRegions.getProfile-Tuple{BroadLineRegions.model, Union{Function, String, Symbol}}","page":"API","text":"getProfile(m::model, name::Union{String,Symbol,Function};\n           bins::Union{Int,Vector{Float64}}=100,\n           dx::Union{Array{Float64,},Nothing}=nothing, kwargs...)\n\nReturn a profile for the model based on the specified name.\n\nArguments\n\nm: Model object to get the profile from\nname: Name of the profile to get. Options include:\n:line: Returns the line profile (integrated intensity as function of velocity)\n:delay: Returns the delay profile (mean delays weighted by intensity/responsivity vs. velocity)\n:r: Returns the mean radius (weighted by intensity) as function of velocity \n:ϕ: Returns the mean azimuthal angle (weighted by intensity) as function of velocity\n:phase: Returns the phase profile (integrated phase as function of velocity)\nRequires U [Mλ], V [Mλ], PA [rad], and BLRAng [rad] as keyword arguments\n:moment2: Returns the per-channel squared angular size of the image along the baseline direction(s) [rad²]\nRequires U [Mλ], V [Mλ], PA [rad], and BLRAng [rad] as keyword arguments\nUse secondMoment directly to also obtain the line-integrated size (a scalar per baseline, which does not fit in a profile struct)\nFunction: Returns the intensity weighted mean of this function vs. velocity\nbins: Number of bins or bin edges for binning\nIf Int: Number of bins with edges equally spaced between min/max velocity\nIf Vector{Float64}: Specific bin edges, with number of bins = length(bins)-1\nBins are left-edge exclusive, right-edge inclusive: a point exactly on an interior edge goes to the bin below it. Points at or below the first edge, or at or above the last edge, are only counted when overflow=true (into the first/last bin)\ndx: Integration element for each ring (defaults to ΔA in each ring struct if nothing)\nAdditional kwargs passed to binModel include:\noverflow=true: Include overflow bins\ncentered=true: Center bins around 0.0\nminX, maxX: Set min/max bin boundaries\n\nReturns\n\nprofile: A profile object containing bin edges, bin centers, and binned sums. Assign to a model object using setProfile! to store the profile in the model.\n\nA device-resident method (getProfile(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\n","title":"BroadLineRegions.getProfile"},{"category":"method","location":"api/#BroadLineRegions.phase-Tuple{BroadLineRegions.model}","page":"API","text":"phase(m::model; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, kwargs...)\n\nCalculate differential phase signal for a model based on specified baselines, model orientation, and BLR angular size.\n\nArguments\n\nm::model: Model object to calculate phase for\nU::Vector{Float64}: U component of complex visibility in [Mλ]\nV::Vector{Float64}: V component of complex visibility in [Mλ]\nPA::Float64: On-sky position angle of the model in radians\nBLRAng::Float64: Characteristic size of the BLR model in radians (conversion from r_s to radians)\nreturnAvg::Bool=false: If true, returns the average phase across all baselines\noffAxisInds::Union{Nothing,Vector{Int}}=nothing: If provided, only calculates phase for baselines at specified indices\n\nReturns\n\nIf returnAvg=true: Tuple{Vector{Float64},Vector{Float64},Vector{Float64}} containing:\nBin edges for velocity\nBin centers for velocity\nAverage differential phase (in radians)\nIf returnAvg=false: Vector{Tuple{Vector{Float64},Vector{Float64},Vector{Float64}}} containing:\nFor each baseline, a tuple of bin edges, bin centers, and differential phase\n\nThe differential phase is calculated by integrating the phase over the model at each velocity bin, weighted by the intensity and area of each ring element.\n\nA device-resident method (phase(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\n","title":"BroadLineRegions.phase"},{"category":"method","location":"api/#BroadLineRegions.secondMoment-Tuple{BroadLineRegions.model}","page":"API","text":"secondMoment(m::model; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, kwargs...)\n\nCalculate the second central moment (squared angular size) of the model image projected along interferometric baseline directions, both per velocity channel and integrated over the whole line.\n\nFor each baseline the per-channel size is the intensity-weighted variance of the projected position û⋅x in each velocity bin, sigma^2_theta(v hatu) = langle (hatucdot(x - barx_v))^2 rangle_v, i.e. the squared rms angular extent of the channel image along the baseline direction. In the marginally-resolved limit this predicts the differential visibility-amplitude dip via V_l(v) = 1 - 2pi^2 u^2 sigma^2_theta(v) with u = sqrtU^2+V^2times10^6 (baseline length in units of the observed wavelength).\n\nThe line-integrated size is the same variance taken over the whole line image (all velocities in a single channel), which by the law of total variance equals the flux-weighted mean of the per-channel sizes plus the flux-weighted spread of the per-channel photocenters — for a rotation-dominated BLR the photocenter-spread term is comparable to the mean per-channel size, so the line-integrated size is significantly larger than the flux-averaged channel size.\n\nNote that these are moments of the line image only: unlike phase, no continuum dilution factor f/(1+f) is applied.\n\nArguments\n\nm::model: Model object to calculate image second moments for\nU::Vector{Float64}: U component of complex visibility in [Mλ]\nV::Vector{Float64}: V component of complex visibility in [Mλ]\nPA::Float64: On-sky position angle of the model in radians\nBLRAng::Float64: Characteristic size of the BLR model in radians (conversion from r_s to radians)\nreturnAvg::Bool=false: If true, returns the average across all baselines\noffAxisInds::Union{Nothing,Vector{Int}}=nothing: If provided, only calculates moments for baselines at specified indices\nAdditional kwargs (bins, overflow, centered, minX, maxX) control the velocity binning as in binnedSum\n\nReturns\n\nIf returnAvg=true: Tuple{Vector{Float64},Vector{Float64},Vector{Float64},Float64} containing:\nBin edges for velocity\nBin centers for velocity\nAverage per-channel squared angular size σ²(v) (in rad², NaN for empty bins)\nAverage line-integrated squared angular size σ²tot (in rad²)\nIf returnAvg=false: Vector{Tuple{Vector{Float64},Vector{Float64},Vector{Float64},Float64}} containing:\nFor each baseline, a tuple of bin edges, bin centers, per-channel σ²(v), and line-integrated σ²tot\n\nA device-resident method (secondMoment(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\n","title":"BroadLineRegions.secondMoment"},{"category":"method","location":"api/#BroadLineRegions.setProfile!-Tuple{BroadLineRegions.model, profile}","page":"API","text":"setProfile!(m::model, p::profile; overwrite::Bool=false)\n\nSet a profile in the model's profiles dictionary. If the profile already exists and overwrite is false, a warning is issued.\n\n\n\n\n\n","title":"BroadLineRegions.setProfile!"},{"category":"method","location":"api/#BroadLineRegions.t-Tuple{BroadLineRegions.ring, Function}","page":"API","text":"t(ring::ring, subFxn::Function=tDisk)\n\nCalculate time delays for a point in a disk or cloud using a custom function subFxn that takes a ring struct.\n\nIt is more peformant to pass the function directly rather than figure it out on the fly if known ahead of time.\n\n\n\n\n\n\n\n","title":"BroadLineRegions.t"},{"category":"method","location":"api/#BroadLineRegions.t-Tuple{BroadLineRegions.ring}","page":"API","text":"t(ring::ring)\n\nCalculate time delays for a point in a disk as `` t = \\eta r \\left(1 + \\cos(\\phi) \\sin(i)\\right)`` or a cloud with opening angle ``\\theta_o``\nas the x-coordinate of the point subtracted from the radial distance of the point ``t = r - x``.\n\n\n\n\n\n","title":"BroadLineRegions.t"},{"category":"method","location":"api/#BroadLineRegions.tCloud-Tuple{BroadLineRegions.ring}","page":"API","text":"tCloud(ring::ring)\n\nCalculate time delays from the general geometric formula ``t = \\eta (r - x)``, where ``x`` is the\n3D system coordinate of the point towards the camera (cached on the ring, see `getXYZ`).\n\nDespite the name this is exact for *any* geometry: for a flat ring (``\\theta_o = 0``) it reduces\nalgebraically to the `tDisk` formula (``x = r\\cos\\phi\\sin i``), and unlike `tDisk` it also\naccounts for lifted (``\\theta_o \\neq 0``) and reflected points. Works on both point (scalar) and\ncontinuous (vector) rings.\n\n\n\n\n\n","title":"BroadLineRegions.tCloud"},{"category":"method","location":"api/#BroadLineRegions.tDisk-Tuple{BroadLineRegions.ring}","page":"API","text":"tDisk(ring::ring)\n\nCalculate time delays for a point in a disk as `` t = \\eta r \\left(1 - \\cos(\\phi) \\sin(i)\\right)``\n\n\n\n\n\n","title":"BroadLineRegions.tDisk"},{"category":"method","location":"api/#BroadLineRegions.getΨ-Tuple{BroadLineRegions.model, Array{Float64}, Array{Float64}}","page":"API","text":"getΨ(m::model,vEdges::Array{Float64},tEdges::Array{Float64})\n\nCalculate the 2D transfer function Ψ for a model m over specified velocity and time bins, whose edges are given by vEdges and tEdges.\n\nA device-resident method (getΨ(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\n","title":"BroadLineRegions.getΨ"},{"category":"method","location":"api/#BroadLineRegions.getΨ-Tuple{BroadLineRegions.model, Int64, Int64}","page":"API","text":"getΨ(m::model,vBins::Int64,tBins::Int64)\n\nCalculate the 2D transfer function Ψ for a model m over specified number of velocity bins vBins and time bins tBins. The velocity and time edges are automatically calculated based on the minimum and maximum values for velocity and delays in the model.\n\n\n\n\n\n","title":"BroadLineRegions.getΨ"},{"category":"function","location":"api/#BroadLineRegions.getΨt","page":"API","text":"getΨt(m::model,tBins::Int64,maxT::Float64=Inf,overflow::Bool=false)\n\nCalculate the 1D transfer function Ψ(t) for a model m over specified number of time bins tBins. The maxT parameter specifies the maximum time delay to consider, and overflow determines whether to include contributions from delays outside the specified edges in the edge bins.\n\n\n\n\n\n","title":"BroadLineRegions.getΨt"},{"category":"function","location":"api/#BroadLineRegions.getΨt-2","page":"API","text":"getΨt(m::model,tEdges::Array{Float64},overflow::Bool=false;)\n\nCalculate the 1D transfer function Ψ(t) for a model m over specified time edges tEdges. The overflow parameter determines whether to include contributions from delays outside the specified edges in the edge bins.\n\nA device-resident method (getΨt(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\n","title":"BroadLineRegions.getΨt"},{"category":"method","location":"api/#BroadLineRegions.response-NTuple{5, Float64}","page":"API","text":"response(r::Float64, ηₒ::Float64, η₁::Float64, αRM::Float64, rNorm::Float64)\n\nPositional-argument version of response for hot loops — avoids the per-call keyword splat.\n\n\n\n\n\n","title":"BroadLineRegions.response"},{"category":"method","location":"api/#BroadLineRegions.response-Tuple{Float64}","page":"API","text":"response(r::Float64; ηₒ::Float64=0.5, η₁::Float64=0.5, αRM::Float64=0.0, rNorm::Float64=1.0, _...)\n\nCalculate response function for use in reverberation mapping calculations.\n\n\n\n\n\n","title":"BroadLineRegions.response"},{"category":"method","location":"api/#BroadLineRegions.ΨAccumulate!-Tuple{Matrix{Float64}, Any, Any, Any, Any, AbstractVector{Float64}, AbstractVector{Float64}}","page":"API","text":"ΨAccumulate!(Ψ, v, delays, I, ΔA, vEdges, tEdges)\n\nSingle-pass accumulation for getΨ (function barrier): one sweep over all points, locating each point's (velocity, delay) bin instead of building a boolean mask per bin pair (the old approach was O(nbins² × npoints) in time and memory traffic).\n\nBin assignment uses the same convention as binnedSum (searchsortedfirst): interior edges are left-EXCLUSIVE / right-inclusive — a point exactly on an interior edge goes to the bin below it — and points at or below the first edge or at or above the last edge are dropped (Ψ has no overflow bins). NaN velocities/delays never land in a bin; NaN intensities/areas still accumulate into their bin so the final floor maps the poisoned bin to 1e-30. This matches the line profile and every other binnedSum-based observable, so a shared edge set bins identically everywhere.\n\n\n\n\n\n","title":"BroadLineRegions.ΨAccumulate!"},{"category":"method","location":"api/#BroadLineRegions.ΨtAccumulate!-Tuple{Vector{Float64}, Any, Any, Any, AbstractVector{Float64}}","page":"API","text":"ΨtAccumulate!(Ψt, delays, I, ΔA, tEdges) -> (sUnder, sOver)\n\nSingle-pass accumulation for getΨt (function barrier – the gathered arrays are not inferrable in the caller). Same binnedSum (searchsortedfirst, left-exclusive interior edge) convention as ΨAccumulate!; additionally collects the underflow/overflow sums (delays <= tEdges[1] / delays >= tEdges[end], NaN in neither) in the same sweep, matching binnedSum's overflow=true edge-bin folding.\n\n\n\n\n\n","title":"BroadLineRegions.ΨtAccumulate!"},{"category":"function","location":"api/#BroadLineRegions.photograph","page":"API","text":"photograph(r::Float64, ϕ₀::Float64, i::Float64, rot::Float64, θₒ::Float64, reflect::Bool=false)\n\nCalculate the image coordinates from system coordinates r, ϕ + inclination angle i.\n\nArguments\n\nr::Float64: radius from central mass (in terms of rₛ)\nϕ₀::Float64: unrotated azimuthal angle in ring plane (rad)\ni::Float64: inclination angle of ring plane (rad)\nrot::Float64: rotation of system plane about z axis (rad)\nθₒ::Float64: ring opening angle\nreflect::Bool=false: whether the point is reflected across the midplane of the disk\n\nReturns\n\nα::Float64: image x coordinate (in terms of rₛ)\nβ::Float64: image y coordinate (in terms of rₛ)\n\nNote\n\nThis function is coordinate photography only. To visualize models, see Image.`\n\n\n\n\n\n","title":"BroadLineRegions.photograph"},{"category":"method","location":"api/#BroadLineRegions.raytrace!-Tuple{BroadLineRegions.ResidentModel}","page":"API","text":"raytrace!(rm::ResidentModel; IRatios=1.0, τCutOff=1.0, raytraceFreeClouds=false) -> ResidentModel\n\nDevice-resident dispatch of raytrace!: run the full bin→sort→segmented-scan→ compact combine on rm's backend with no host round-trips, returning a freshly raytraced ResidentModel (drop-in for the observable methods). rm must carry raytrace metadata — build it with gpu(m) (CUDA loaded) or resident(m; raytrace=true).\n\nLike raytrace!(::model) this returns a new combined handle rather than mutating in place (the point count changes: one combined point per active output pixel + surviving free clouds), so write rm = raytrace!(rm). Semantics match the host raytrace!: IRatios is a global per-submodel weight, τCutOff the optical-depth cutoff, raytraceFreeClouds enables cloud–cloud attenuation.\n\n\n\n\n\n","title":"BroadLineRegions.raytrace!"},{"category":"method","location":"api/#BroadLineRegions.raytrace!-Tuple{BroadLineRegions.model}","page":"API","text":"raytrace!(m::model; IRatios::Union{Float64,Array{Float64,}}=1.0,\n        τCutOff::Float64=1.0, raytraceFreeClouds::Bool=false,\n        backend=nothing, T=Float64)\n\nPerform raytracing for a model, combining overlapping components along line of sight.\n\nThis function should be called after combining all relevant models (i.e. mCombined = m1 + m2 + m3...). Points are binned by pixel, sorted front-to-back along the line of sight, and combined with a segmented exp(-τ) scan: each point attenuates those behind it, points past τCutOff are dropped, and IRatios is applied as a global per-submodel weight. A new model object is returned with the extraneous points removed. Note that this function will mutate the input model objects.\n\nnote: Algorithm / performance\nThe bin→sort→segmented-scan implementation is far faster than the original brute-force raytracer (and can also run on a device via backend). If you only need simple disk obscuration removal rather than full attenuation, removeDiskObscuredClouds! is a lighter alternative.\n\nArguments\n\nm::model: Model to raytrace\nIRatios::Union{Float64,Array{Float64,}}=1.0: Global emissivity weights for each submodel\nIf Float64, applies to all submodels equally\nIf array, applies to each submodel individually (must match number of submodels)\nUsed when combining models with different intensity functions if they aren't properly normalized\nτCutOff::Float64=1.0: Maximum optical depth to raytrace to (stops when τ > τCutOff)\nraytraceFreeClouds::Bool=false: Whether to raytrace free clouds (cloud-cloud raytracing)\nIf false, clouds are only raytraced if they overlap with a continuous model\nIf true, clouds will be checked for overlap with other clouds and raytraced accordingly\nbackend=nothing: Optional KernelAbstractions backend to run the bin→sort→scan on a device.\nIf nothing, uses the CPU implementation.\nPass CUDABackend() (with CUDA.jl loaded) to run on the GPU; see also gpu.\nT=Float64: Element type for the device arrays when backend is set (use Float32 on GeForce GPUs for speed).\n\nReturns\n\nm::model: Model with raytraced points\n\n\n\n\n\n","title":"BroadLineRegions.raytrace!"},{"category":"method","location":"api/#BroadLineRegions.raytrace-NTuple{5, Float64}","page":"API","text":"raytrace(α::Float64, β::Float64, i::Float64, rot::Float64, θₒPoint::Float64)\n\nCalculate where ray traced back from camera coordinates α and β intersects the system (assumes circular geometry).\n\nArguments\n\nα::Float64: image x coordinate (in terms of rₛ)\nβ::Float64: image y coordinate (in terms of rₛ)\ni::Float64: inclination angle of system (rad)\nrot::Float64: how the point was rotated about z axis (rad)\nθₒPoint::Float64: opening angle of current point\n\nReturns\n\nr::Float64: distance from central mass (in terms of rₛ)\nϕ::Float64: azimuthal angle of system ring plane at intersection\nϕ₀::Float64: original azimuthal angle in ring plane (no rotation)\n\nNote\n\nThis function is coordinate raytracing only. To raytrace models and combine intensities, see raytrace!. \n\n\n\n\n\n","title":"BroadLineRegions.raytrace"},{"category":"method","location":"api/#BroadLineRegions.raytrace-Tuple{Float64, Float64, Float64, Float64, Float64, Matrix{Float64}, Matrix{Float64}}","page":"API","text":"raytrace(α::Float64, β::Float64, i::Float64, rot::Float64, θₒPoint::Float64, M::Matrix{Float64}, r3D::Matrix{Float64})\n\nSame as the 6-argument raytrace(α, β, i, rot, θₒPoint, M) method but additionally returns the 3D system coordinates of the intersection point (camera at +x), computed from the supplied r3D = get_r3D(i, rot, θₒPoint) matrix. Used at model construction to fill the per-point coordinate cache (ring.x/y/z) without a second rotation pass.\n\nReturns\n\nr, ϕ, ϕ₀, x, y, z (all Float64)\n\n\n\n\n\n","title":"BroadLineRegions.raytrace"},{"category":"method","location":"api/#BroadLineRegions.raytrace-Tuple{Float64, Float64, Float64, Float64, Float64, Matrix{Float64}, Vector{Float64}, Matrix{Float64}, Vector{Float64}}","page":"API","text":"raytrace(α::Float64, β::Float64, i::Float64, rot::Float64, θₒPoint::Float64, \n        r3D::Matrix{Float64}, xyz::Vector{Float64}, matBuff::Matrix{Float64}, \n        colBuff::Vector{Float64})\n\nPerformant version of raytrace function – calculate where ray traced back from camera coordinates α, β intersects the system (assumes circular geometry).\n\nArguments\n\nα::Float64: image x coordinate (in terms of rₛ)\nβ::Float64: image y coordinate (in terms of rₛ)\ni::Float64: inclination angle of system (rad)\nrot::Float64: rotation of current point about z axis (rad)\nθₒPoint::Float64: opening angle of current point\nr3D::Matrix{Float64}: matrix that rotates system plane into XY plane\nxyz::Vector{Float64}: preallocated xyz vector (but not precalculated)\nmatBuff::Matrix{Float64}: preallocated buffer matrix for storing result of 3x3 matrix multiplication\ncolBuff::Vector{Float64}: preallocated buffer vector for storing final matrix multiplication result\n\nReturns\n\nr::Float64: distance from central mass (in terms of rₛ)\nϕ::Float64: azimuthal angle of system ring plane at intersection\nϕ₀::Float64: original azimuthal angle in ring plane\n\nNote\n\nThis function is coordinate raytracing only. To raytrace models and combine intensities, see raytrace!.\n\nwarning: Deprecated\nPrefer the 6-argument method raytrace(α, β, i, rot, θₒPoint, M) with a single precomputed M = undo_tilt * get_r3D(i, rot, θₒ) — it is allocation-free and faster. This buffer-based method is retained for backwards compatibility only.\n\n\n\n\n\n","title":"BroadLineRegions.raytrace"},{"category":"method","location":"api/#BroadLineRegions.raytrace-Tuple{Float64, Float64, Float64, Float64, Float64, Matrix{Float64}}","page":"API","text":"raytrace(α::Float64, β::Float64, i::Float64, rot::Float64, θₒPoint::Float64, M::Matrix{Float64})\n\nAllocation-free version of raytrace — calculate where ray traced back from camera coordinates α, β intersects the system (assumes circular geometry), using a single precomputed rotation matrix.\n\nM must be undo_tilt * get_r3D(i, rot, θₒPoint) where undo_tilt = [sin(i) 0.0 -cos(i); 0.0 1.0 0.0; cos(i) 0.0 sin(i)] — both factors are constant for fixed (i, rot, θₒ) so M should be computed once outside any pixel loop.\n\nReturns\n\nr::Float64: distance from central mass (in terms of rₛ)\nϕ::Float64: azimuthal angle of system ring plane at intersection\nϕ₀::Float64: original azimuthal angle in ring plane\n\nNote\n\nSupersedes the buffer-based 9-argument method (which is retained for compatibility).\n\n\n\n\n\n","title":"BroadLineRegions.raytrace"},{"category":"function","location":"api/#BroadLineRegions.removeDiskObscuredClouds!","page":"API","text":"removeDiskObscuredClouds!(m::model, rotate3D::Function=rotate3D_scalar)\n\nRemove clouds that are obscured by the disk.\n\nPerforms simple raytracing for an optically thick obscuring disk. The function modifies the input model by removing cloud points that are obscured by the disk. Note that this is a mutating operation and the input model will be modified in place.\n\nArguments\n\nm::model: Model to remove disk obscured clouds. Should be a combined model consisting  of a disk component and a cloud component.\nrotate3D::Function=rotate3D_scalar: Function to rotate coordinates in 3D space (must return an indexable (x,y,z))\n\nReturns\n\nm::model: Model with disk obscured clouds removed\n\nSee also\n\nzeroDiskObscuredClouds!: Function to zero out disk obscured clouds instead of removing them\n\n\n\n\n\n","title":"BroadLineRegions.removeDiskObscuredClouds!"},{"category":"method","location":"api/#BroadLineRegions.zeroDiskObscuredClouds!-Tuple{BroadLineRegions.model}","page":"API","text":"zeroDiskObscuredClouds!(m::model; diskCloudIntensityRatio::Float64=1.0, rotate3D::Function=rotate3D_scalar)\n\nZero out the intensities of clouds that are obscured by the disk.\n\nPerforms simple raytracing for an optically thick obscuring disk. The function modifies the input model by setting the intensity of obscured cloud points to zero and adjusting the disk intensity according to the specified ratio.\n\nArguments\n\nm::model: Model to zero out disk obscured clouds. Should be a combined model consisting of a disk component and a cloud component. \ndiskCloudIntensityRatio::Float64=1.0: Ratio of disk to cloud intensity, used to scale  the disk intensities after zeroing out clouds\nrotate3D::Function=rotate3D_scalar: Function to rotate coordinates in 3D space (must return an indexable (x,y,z))\n\nReturns\n\nm::model: Model with disk obscured clouds zeroed out\n\nSee also\n\nremoveDiskObscuredClouds!: Function to remove disk obscured clouds instead of zeroing them out\n\n\n\n\n\n","title":"BroadLineRegions.zeroDiskObscuredClouds!"},{"category":"type","location":"api/#BroadLineRegions._RtGridBlock","page":"API","text":"RaytraceMeta\n\nDevice-resident metadata that raytrace!(::ResidentModel) needs beyond the flat ModelArrays: a list of output-grid blocks (grids, one per continuous submodel — each a _RtGridBlock with grid edges, per-output-pixel area/camera coords, and the block's inner radius for ordering) plus the per-point submodel index and discrete/cloud flag (submodel/discrete, length = number of input points). raytrace! assembles the blocks innermost-first into a single output grid at call time, so any number/kind of submodels combine generically (mirroring the host _rt_build_output). All array fields live on the same backend as the model columns.\n\n\n\n\n\n","title":"BroadLineRegions._RtGridBlock"},{"category":"function","location":"api/#BroadLineRegions.addGrid!","page":"API","text":"addGrid!(m::model, colors=nothing, nϕ::Int=64)\n\nAdd a grid to the model image plot - mostly a debugging tool to visualize grid cells of overlapping models.\n\nArguments\n\nm::model: Model object to add grid to\ncolors=nothing: Vector of colors for each submodel (if nothing, uses default colors)\nnϕ::Int=64: Number of azimuthal angles to use for the grid\n\nReturns\n\n::Plots.plot: Plot with grid added\n\n\n\n\n\n","title":"BroadLineRegions.addGrid!"},{"category":"method","location":"api/#BroadLineRegions.expandPerPoint-Tuple{BroadLineRegions.model, Symbol}","page":"API","text":"expandPerPoint(m::model, variable::Symbol)\n\nLike getVariable(m, variable; flatten=true) but per-ring scalar values are expanded to per-point length, so the result always aligns elementwise with getVariable(m, :I, flatten=true) — including for combined models where some rings store a field (e.g. η) as a single scalar while I is a vector.\n\nThe within-chunk ordering matches getVariable's flattening (vec(stack(chunk, dims=1)), i.e. point-major across the rings of each submodel), so elementwise products with other flattened gathers are valid. Results are memoized in m.cache under (variable, :perpoint).\n\nnote: Note\nraytrace! relies on getVariable's un-expanded behavior (it checks length(sub_η) < length(sub_I) to detect per-ring scalars) — that is why this is a separate function rather than a getVariable option.\n\n\n\n\n\n","title":"BroadLineRegions.expandPerPoint"},{"category":"method","location":"api/#BroadLineRegions.gatherStack-Tuple{Vector{BroadLineRegions.ring}, Symbol}","page":"API","text":"gatherStack(rings::Vector{ring}, variable::Symbol)\n\nTyped helper (function barrier) for getVariable: stacks getfield(ring, variable) over all rings into a Vector (one scalar per ring, e.g. cloud models) or a Matrix with one row per ring (vector-valued fields), without the intermediate vector-of-vectors that stack requires. Preserves the element type of the field (e.g. Bool for :reflect).\n\n\n\n\n\n","title":"BroadLineRegions.gatherStack"},{"category":"method","location":"api/#BroadLineRegions.getFlattenedCameraIndices-Tuple{BroadLineRegions.model}","page":"API","text":"getFlattenedCameraIndices(m::model)\n\nGet flattened camera indices corresponding to rings in model.\n\nArguments\n\nm::model: Model object to extract camera indices from\n\nReturns\n\ncamStartInds::Vector{Int}: Vector of camera starting indices with length equal to m.subModelStartInds\n\n\n\n\n\n","title":"BroadLineRegions.getFlattenedCameraIndices"},{"category":"method","location":"api/#BroadLineRegions.getRingFromFlattenedInd-Tuple{BroadLineRegions.model, Int64}","page":"API","text":"getRingFromFlattenedInd(m::model, flattenedInd::Int) -> Tuple{Int, Int}\n\nRetrieve the model ring index and subindex (if applicable) from flattened array index.\n\nArguments\n\nm::model: Model object with rings \nflattenedInd::Int: The index in the flattened array we need to work back from\n\nReturns\n\nrow::Int: The ring index in model.rings that the flattened index corresponds to\ncolumn::Int: The subindex that matches the flattened index passed to this function.\n\n\n\n\n\n","title":"BroadLineRegions.getRingFromFlattenedInd"},{"category":"method","location":"api/#BroadLineRegions.getVariable-Tuple{BroadLineRegions.model, Function}","page":"API","text":"getVariable(m::model, variable::Function; flatten=false)\n\nRetrieve model variable when specified as a Function. See main docstring for details.\n\nNote\n\nResults are memoized in m.cache only for the package's own pure delay functions (t, tDisk, tCloud) — user-supplied functions are always re-evaluated, since they may capture state the cache cannot see.\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"method","location":"api/#BroadLineRegions.getVariable-Tuple{BroadLineRegions.model, String}","page":"API","text":"getVariable(m::model, variable::Union{String,Symbol,Function}; flatten=false)\n\nRetrieve elements from model object and stack them into matrices for easy manipulation.\n\nArguments\n\nm::model: Model object to extract variables from\nvariable::Union{String,Symbol,Function}: Variable to extract from model\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nflatten::Bool=false: If true, flatten the result to a vector\n\nReturns\n\nArray{Float64,}: Matrix or vector of extracted variable from model.rings, created by stacking the output variable for each ring\nFor example, if variable given is :I, result will have shape (length(r), length(ϕ)) as at each r and ϕ there is a value of I\nIf flatten=true, result will be a flattened vector\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"method","location":"api/#BroadLineRegions.getVariable-Tuple{BroadLineRegions.model, Symbol}","page":"API","text":"getVariable(m::model, variable::Symbol; flatten=false)\n\nRetrieve model variable when specified as a Symbol. See main docstring for details.\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"method","location":"api/#BroadLineRegions.getXYZ-Tuple{BroadLineRegions.ring}","page":"API","text":"getXYZ(ring::ring)\n\nReturn the 3D system coordinates (x, y, z) of every point in ring (camera at +x).\n\nUses the cached ring.x/y/z fields when present; otherwise computes them with rotate3D_scalar and stores them on the (mutable) ring so the transform is only ever done once per point.\n\nReturns\n\nFor a cloud/point ring (scalar fields): NTuple{3,Float64}\nFor a continuous ring (vector fields): Tuple{Vector{Float64},Vector{Float64},Vector{Float64}}\n\nwarning: Reflection already applied\nCached coordinates are post-reflection — if ring.reflect is true the stored values already include the reflection across the disk midplane. Never apply reflect!/reflect_scalar to the result of getXYZ a second time.\n\nwarning: Geometry mutation\nThe cache is filled from r, ϕ₀, i, rot, θₒ at first access. If you mutate any of those fields afterwards, set ring.x = nothing; ring.y = nothing; ring.z = nothing to force a recompute.\n\n\n\n\n\n","title":"BroadLineRegions.getXYZ"},{"category":"method","location":"api/#BroadLineRegions.get_r3D-Tuple{Any, Any, Any}","page":"API","text":"get_r3D(i::Float64, rot::Float64, θₒ::Float64) -> Matrix{Float64}\n\nCalculate rotation matrix to transform from initial XY plane coordinates to 3D space.\n\nParameters\n\ni::Float64: Inclination angle of ring (rad)\nrot::Float64: Rotation of ring plane about z axis (rad)\nθₒ::Float64: Opening angle of point (rad)\n\nReturns\n\nmatrix::Matrix{Float64}: 3×3 rotation matrix\n\n\n\n\n\n","title":"BroadLineRegions.get_r3D"},{"category":"method","location":"api/#BroadLineRegions.get_rMinMaxDiskWind-Tuple{Float64, Float64, Float64}","page":"API","text":"get_rMinMaxDiskWind(r̄::Float64, rFac::Float64, α::Float64)\n\nCalculate the minimum and maximum radius of model given the (intensity weighted) mean radius r̄,  the radius factor rFac, and the power-law index α following Long+ 2023.\n\nParameters\n\nr̄::Float64: mean radius of model (in terms of rₛ)\nrFac::Float64: radius scaling factor \nα::Float64: power-law index of the source function S(r) propto r^-alpha (cannot be 1/2 or 3/2 as this divides by zero)\n\nReturns\n\nrMin::Float64: minimum radius of model (in terms of rₛ)\nrMax::Float64: maximum radius of model (in terms of rₛ)\n\n\n\n\n\n","title":"BroadLineRegions.get_rMinMaxDiskWind"},{"category":"method","location":"api/#BroadLineRegions.image!-Tuple","page":"API","text":"BLR.image(m::model, variable::Union{String,Symbol,Function}, kwargs...)\n\nGenerate an image of the model where the color of each point is determined by the variable provided.\n\nArguments\n\nm::model: Model object to extract variable from\nvariable::Union{String,Symbol,Function}: Variable to extract from model\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\n\nKeywords\n\nAdditional keyword arguments are passed to Plots.plot\n\nReturns\n\np::Plots.plot: Plot object representing the generated image\n\n\n\n\n\n","title":"BroadLineRegions.image!"},{"category":"method","location":"api/#BroadLineRegions.image!-Tuple{RecipesBase.AbstractPlot, Vararg{Any}}","page":"API","text":"BLR.image(m::model, variable::Union{String,Symbol,Function}, kwargs...)\n\nGenerate an image of the model where the color of each point is determined by the variable provided.\n\nArguments\n\nm::model: Model object to extract variable from\nvariable::Union{String,Symbol,Function}: Variable to extract from model\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\n\nKeywords\n\nAdditional keyword arguments are passed to Plots.plot\n\nReturns\n\np::Plots.plot: Plot object representing the generated image\n\n\n\n\n\n","title":"BroadLineRegions.image!"},{"category":"method","location":"api/#BroadLineRegions.image-Tuple","page":"API","text":"BLR.image(m::model, variable::Union{String,Symbol,Function}, kwargs...)\n\nGenerate an image of the model where the color of each point is determined by the variable provided.\n\nArguments\n\nm::model: Model object to extract variable from\nvariable::Union{String,Symbol,Function}: Variable to extract from model\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\n\nKeywords\n\nAdditional keyword arguments are passed to Plots.plot\n\nReturns\n\np::Plots.plot: Plot object representing the generated image\n\n\n\n\n\n","title":"BroadLineRegions.image"},{"category":"method","location":"api/#BroadLineRegions.midPlaneXZ-Tuple{Any, Any}","page":"API","text":"midPlaneXZ(x::Float64, i::Float64) -> Float64\n\nHeight of the disk mid-plane in the x–z plane at camera coordinate x for inclination i; used to decide cloud reflection.\n\n\n\n\n\n","title":"BroadLineRegions.midPlaneXZ"},{"category":"method","location":"api/#BroadLineRegions.plot3d!-Tuple","page":"API","text":"plot3d(m::model, [variable], [annotate], [kwargs...])\nplot3d(rm::ResidentModel, [variable], [annotate], [kwargs...])\n\nGenerate a 3D plot of the model geometry, optionally colored by a variable. Accepts a host model or a ResidentModel (its flat columns are copied to the host; the cached x/y/z are used directly).\n\nParameters\n\nm::model / rm::ResidentModel: Model object to plot\nvariable::Union{String,Symbol,Function}=nothing: Variable to color the points by\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g., :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nIf not provided, defaults to nothing (no coloring). For a ResidentModel, a Function variable is not supported (pass a column Symbol); a combined (multi-submodel) ResidentModel must carry raytrace metadata (build it with raytrace=true) so the series can be split per submodel.\nannotate::Bool=true: Whether to annotate the camera position and model orientation in the plot\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nReturns\n\np::Plots.plot: 3D plot of the model geometry, optionally colored by the variable provided\n\n\n\n\n\n","title":"BroadLineRegions.plot3d!"},{"category":"method","location":"api/#BroadLineRegions.plot3d!-Tuple{RecipesBase.AbstractPlot, Vararg{Any}}","page":"API","text":"plot3d(m::model, [variable], [annotate], [kwargs...])\nplot3d(rm::ResidentModel, [variable], [annotate], [kwargs...])\n\nGenerate a 3D plot of the model geometry, optionally colored by a variable. Accepts a host model or a ResidentModel (its flat columns are copied to the host; the cached x/y/z are used directly).\n\nParameters\n\nm::model / rm::ResidentModel: Model object to plot\nvariable::Union{String,Symbol,Function}=nothing: Variable to color the points by\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g., :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nIf not provided, defaults to nothing (no coloring). For a ResidentModel, a Function variable is not supported (pass a column Symbol); a combined (multi-submodel) ResidentModel must carry raytrace metadata (build it with raytrace=true) so the series can be split per submodel.\nannotate::Bool=true: Whether to annotate the camera position and model orientation in the plot\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nReturns\n\np::Plots.plot: 3D plot of the model geometry, optionally colored by the variable provided\n\n\n\n\n\n","title":"BroadLineRegions.plot3d!"},{"category":"method","location":"api/#BroadLineRegions.plot3d-Tuple","page":"API","text":"plot3d(m::model, [variable], [annotate], [kwargs...])\nplot3d(rm::ResidentModel, [variable], [annotate], [kwargs...])\n\nGenerate a 3D plot of the model geometry, optionally colored by a variable. Accepts a host model or a ResidentModel (its flat columns are copied to the host; the cached x/y/z are used directly).\n\nParameters\n\nm::model / rm::ResidentModel: Model object to plot\nvariable::Union{String,Symbol,Function}=nothing: Variable to color the points by\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g., :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nIf not provided, defaults to nothing (no coloring). For a ResidentModel, a Function variable is not supported (pass a column Symbol); a combined (multi-submodel) ResidentModel must carry raytrace metadata (build it with raytrace=true) so the series can be split per submodel.\nannotate::Bool=true: Whether to annotate the camera position and model orientation in the plot\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nReturns\n\np::Plots.plot: 3D plot of the model geometry, optionally colored by the variable provided\n\n\n\n\n\n","title":"BroadLineRegions.plot3d"},{"category":"method","location":"api/#BroadLineRegions.profile!-Tuple","page":"API","text":"profile(m::model, [variable], [kwargs...])\nprofile(rm::ResidentModel, variable, [kwargs...])\nprofile(cm::CompositeModel, [name], [kwargs...])\nprofile(rcm::ResidentCompositeModel, [name], [kwargs...])\nprofile(p::profile, [kwargs...])\n\nPlot line profiles, normalized to the maximum value of each profile.\n\nArguments\n\nmodel: A model object containing profile data, or a ResidentModel.\nvariable: A symbol or string (or list of symbols/strings) specifying which profile to plot. For a host model this is optional — if omitted, every profile already set on the model is plotted. A ResidentModel stores no preset profiles, so a variable is required there and the profile is computed on demand via getProfile (choose from :line, :delay, :r, :ϕ, :phase, :moment2).\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nCompositeModel and bare profile support (Workstream 4/5)\n\ncm::CompositeModel: plots the requested name profile (default :line, computed on demand via getProfile(cm, name; line=...)) for every line, one series per line labeled by the line name and normalized by that line's own max |binSums| (the same convention the multi-profile model case below uses) – so lines are shape-comparable regardless of fluxRatio. The one exception is profile(cm, :ratio, (a, b)) (W5): the velocity-resolved line ratio is computed from the PAIR of lines via getProfile(cm, :ratio; lines=(a, b)) and rendered as a single unnormalized series (the ratio's absolute scale is the point), identical to plotting the returned bare profile. The pair is a positional argument because Plots reserves lines as a magic line-attribute alias – a lines=... keyword would be consumed by the attribute preprocessing and never reach the recipe. Other :ratio keywords (bins, floor, ...) are not forwarded – for those, call getProfile yourself and plot the returned struct.\nrcm::ResidentCompositeModel: same as the CompositeModel case (one normalized series per line), except each line's profile is computed on its device-resident columns via getProfile(rcm, name; line=...) (W4-G3), with the resident restrictions (uniform bins, no custom Function profiles).\np::profile: plots a single bare profile struct directly (binCenters vs binSums, unnormalized). Needed so Workstream 5's :ratio profile (also a plain profile struct) is directly plottable. Note profile is both this struct's type and (via @userplot Profile above) the name of the generated plotting function – they're the same generic function, but the @kwdef constructor takes zero positional arguments (kwargs only), while this plotting method takes one-or-more positional arguments, so profile(p) is never ambiguous with profile(name=..., binCenters=..., ...).\n\n\n\n\n\n","title":"BroadLineRegions.profile!"},{"category":"method","location":"api/#BroadLineRegions.profile!-Tuple{RecipesBase.AbstractPlot, Vararg{Any}}","page":"API","text":"profile(m::model, [variable], [kwargs...])\nprofile(rm::ResidentModel, variable, [kwargs...])\nprofile(cm::CompositeModel, [name], [kwargs...])\nprofile(rcm::ResidentCompositeModel, [name], [kwargs...])\nprofile(p::profile, [kwargs...])\n\nPlot line profiles, normalized to the maximum value of each profile.\n\nArguments\n\nmodel: A model object containing profile data, or a ResidentModel.\nvariable: A symbol or string (or list of symbols/strings) specifying which profile to plot. For a host model this is optional — if omitted, every profile already set on the model is plotted. A ResidentModel stores no preset profiles, so a variable is required there and the profile is computed on demand via getProfile (choose from :line, :delay, :r, :ϕ, :phase, :moment2).\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nCompositeModel and bare profile support (Workstream 4/5)\n\ncm::CompositeModel: plots the requested name profile (default :line, computed on demand via getProfile(cm, name; line=...)) for every line, one series per line labeled by the line name and normalized by that line's own max |binSums| (the same convention the multi-profile model case below uses) – so lines are shape-comparable regardless of fluxRatio. The one exception is profile(cm, :ratio, (a, b)) (W5): the velocity-resolved line ratio is computed from the PAIR of lines via getProfile(cm, :ratio; lines=(a, b)) and rendered as a single unnormalized series (the ratio's absolute scale is the point), identical to plotting the returned bare profile. The pair is a positional argument because Plots reserves lines as a magic line-attribute alias – a lines=... keyword would be consumed by the attribute preprocessing and never reach the recipe. Other :ratio keywords (bins, floor, ...) are not forwarded – for those, call getProfile yourself and plot the returned struct.\nrcm::ResidentCompositeModel: same as the CompositeModel case (one normalized series per line), except each line's profile is computed on its device-resident columns via getProfile(rcm, name; line=...) (W4-G3), with the resident restrictions (uniform bins, no custom Function profiles).\np::profile: plots a single bare profile struct directly (binCenters vs binSums, unnormalized). Needed so Workstream 5's :ratio profile (also a plain profile struct) is directly plottable. Note profile is both this struct's type and (via @userplot Profile above) the name of the generated plotting function – they're the same generic function, but the @kwdef constructor takes zero positional arguments (kwargs only), while this plotting method takes one-or-more positional arguments, so profile(p) is never ambiguous with profile(name=..., binCenters=..., ...).\n\n\n\n\n\n","title":"BroadLineRegions.profile!"},{"category":"method","location":"api/#BroadLineRegions.reflect!-Tuple{Any, Any}","page":"API","text":"reflect!(xyzSys::Vector{Float64}, i::Float64) -> Vector{Float64}\n\nReflect coordinates in 3D space across the ring plane.\n\nParameters\n\nxyzSys::Vector{Float64}: [x;y;z] coordinates in 3D space\ni::Float64}: Inclination angle of ring plane (rad)\n\nReturns\n\nxyzSys::Vector{Float64}: [x';y';z'] coordinates in 3D space after reflection\n\n\n\n\n\n","title":"BroadLineRegions.reflect!"},{"category":"method","location":"api/#BroadLineRegions.reflect_scalar-NTuple{4, Float64}","page":"API","text":"reflect_scalar(x::Float64, y::Float64, z::Float64, i::Float64) -> NTuple{3,Float64}\n\nAllocation-free scalar version of reflect! — reflect coordinates in 3D space across the ring plane. Same math as reflect! but takes/returns plain scalars instead of mutating a Vector.\n\n\n\n\n\n","title":"BroadLineRegions.reflect_scalar"},{"category":"method","location":"api/#BroadLineRegions.removeNaN!-Tuple{BroadLineRegions.model}","page":"API","text":"removeNaN!(m::model)\n\nRemove points with I = NaN from model.\n\nParameters\n\nm::model: Model to remove points from\n\nReturns\n\nm::model: Model with NaN points removed\n\n\n\n\n\n","title":"BroadLineRegions.removeNaN!"},{"category":"method","location":"api/#BroadLineRegions.reset!-Tuple{BroadLineRegions.model}","page":"API","text":"reset!(m::model; profiles=true, img=false)\n\nErase existing profiles/raytrace status and invalidate the model's getVariable cache.\n\nCall this after mutating ring fields directly (e.g. m.rings[1].I .= ...) so that subsequent getVariable/getProfile calls see the new values instead of memoized ones.\n\nParameters\n\nm::model: Model object to reset\nprofiles::Bool=true: If true, reset profiles\nimg::Bool=false: If true, reset raytracing boolean (does not change existing model but allows model to be raytraced again after combining other new models)\n\n\n\n\n\n","title":"BroadLineRegions.reset!"},{"category":"function","location":"api/#BroadLineRegions.rotate3D","page":"API","text":"rotate3D(r::Float64, ϕ₀::Float64, i::Float64, rot::Float64, θₒ::Float64, reflect::Bool=false)\n\nTransform from ring coordinates to 3D coordinates where camera is at +x.\n\nParameters\n\nr::Float64: Radius from central mass (in terms of rₛ)\nϕ₀::Float64: Starting azimuthal angle in ring plane (rad)\ni::Float64: Inclination angle of ring plane (rad)\nrot::Float64: Rotation of system plane about z axis (rad)\nθₒ::Float64: Opening angle of point (rad)\nreflect::Bool=false: Whether to reflect across the ring plane\n\nReturns\n\nVector{Float64}: [x; y; z] coordinates in 3D space\n\nNote\n\nThis method allocates its result; in performance-critical loops use rotate3D_scalar which returns a stack-allocated tuple instead.\n\n\n\n\n\n","title":"BroadLineRegions.rotate3D"},{"category":"function","location":"api/#BroadLineRegions.rotate3D-2","page":"API","text":"rotate3D(r::Float64, ϕ₀::Float64, i::Float64, matrix::Matrix{Float64}, reflect::Bool=false)\n\nTransform from ring coordinates to 3D coordinates where camera is at +x using a precomputed rotation matrix.\n\nParameters\n\nr::Float64: Radius from central mass (in terms of rₛ)\nϕ₀::Float64: Starting azimuthal angle in ring plane (rad)\ni::Float64: Inclination angle of ring plane (rad)\nmatrix::Matrix{Float64}: Precomputed rotation matrix for the ring\nreflect::Bool=false: Whether to reflect across the ring plane\n\nReturns\n\nVector{Float64}: [x; y; z] coordinates in 3D space\n\n\n\n\n\n","title":"BroadLineRegions.rotate3D"},{"category":"function","location":"api/#BroadLineRegions.rotate3D_scalar","page":"API","text":"rotate3D_scalar(r::Float64, ϕ₀::Float64, i::Float64, rot::Float64, θₒ::Float64, reflect::Bool=false) -> NTuple{3,Float64}\n\nAllocation-free version of rotate3D: transform from ring coordinates to 3D coordinates where camera is at +x, returning an (x, y, z) tuple instead of a heap-allocated Vector. Prefer this in hot loops.\n\n\n\n\n\n","title":"BroadLineRegions.rotate3D_scalar"},{"category":"function","location":"api/#BroadLineRegions.rotate3D_vector_scalar","page":"API","text":"rotate3D_vector_scalar(vx::Float64, vy::Float64, vz::Float64, i::Float64, rot::Float64, θₒ::Float64, reflect::Bool=false) -> NTuple{3,Float64}\n\nAllocation-free rotation of an arbitrary vector (vx,vy,vz) from initial XY-plane coordinates into 3D space (camera at +x), with optional reflection across the ring plane. The matrix entries are those of get_r3D(i,rot,θₒ) inlined; used for rotating velocity vectors as well as positions.\n\n\n\n\n\n","title":"BroadLineRegions.rotate3D_vector_scalar"},{"category":"method","location":"api/#BroadLineRegions.profile-Tuple","page":"API","text":"profile(m::model, [variable], [kwargs...])\nprofile(rm::ResidentModel, variable, [kwargs...])\nprofile(cm::CompositeModel, [name], [kwargs...])\nprofile(rcm::ResidentCompositeModel, [name], [kwargs...])\nprofile(p::profile, [kwargs...])\n\nPlot line profiles, normalized to the maximum value of each profile.\n\nArguments\n\nmodel: A model object containing profile data, or a ResidentModel.\nvariable: A symbol or string (or list of symbols/strings) specifying which profile to plot. For a host model this is optional — if omitted, every profile already set on the model is plotted. A ResidentModel stores no preset profiles, so a variable is required there and the profile is computed on demand via getProfile (choose from :line, :delay, :r, :ϕ, :phase, :moment2).\nkwargs...: Additional keyword arguments passed to Plots.plot\n\nCompositeModel and bare profile support (Workstream 4/5)\n\ncm::CompositeModel: plots the requested name profile (default :line, computed on demand via getProfile(cm, name; line=...)) for every line, one series per line labeled by the line name and normalized by that line's own max |binSums| (the same convention the multi-profile model case below uses) – so lines are shape-comparable regardless of fluxRatio. The one exception is profile(cm, :ratio, (a, b)) (W5): the velocity-resolved line ratio is computed from the PAIR of lines via getProfile(cm, :ratio; lines=(a, b)) and rendered as a single unnormalized series (the ratio's absolute scale is the point), identical to plotting the returned bare profile. The pair is a positional argument because Plots reserves lines as a magic line-attribute alias – a lines=... keyword would be consumed by the attribute preprocessing and never reach the recipe. Other :ratio keywords (bins, floor, ...) are not forwarded – for those, call getProfile yourself and plot the returned struct.\nrcm::ResidentCompositeModel: same as the CompositeModel case (one normalized series per line), except each line's profile is computed on its device-resident columns via getProfile(rcm, name; line=...) (W4-G3), with the resident restrictions (uniform bins, no custom Function profiles).\np::profile: plots a single bare profile struct directly (binCenters vs binSums, unnormalized). Needed so Workstream 5's :ratio profile (also a plain profile struct) is directly plottable. Note profile is both this struct's type and (via @userplot Profile above) the name of the generated plotting function – they're the same generic function, but the @kwdef constructor takes zero positional arguments (kwargs only), while this plotting method takes one-or-more positional arguments, so profile(p) is never ambiguous with profile(name=..., binCenters=..., ...).\n\n\n\n\n\n","title":"BroadLineRegions.profile"},{"category":"method","location":"api/#Base.:+-Tuple{BroadLineRegions.ResidentModel, BroadLineRegions.ResidentModel}","page":"API","text":"+(rm1::ResidentModel, rm2::ResidentModel) -> ResidentModel\n\nCombine two device-resident models by concatenating their columns on their existing backend — when both live on the GPU the vcats stay on the device, so this is the fast path for gpu(m1) + gpu(m2) (or for merging submodels built on-device with residentDiskWindModel/residentCloudModel) without a host round-trip. Mirrors +(::model, ::model): submodels are stacked and nSubModels adds. Both handles must share the same backend type and element type.\n\nLike the host +, this stacks submodels without raytracing, but it does preserve raytrace metadata when both operands carry it (merged via _rt_merge_meta). Use raytrace!(rm) for the device-resident τ-scan, or host raytrace!(m1 + m2; backend=…) for the one-shot host-orchestrated path.\n\n\n\n\n\n","title":"Base.:+"},{"category":"method","location":"api/#Base.getindex-Tuple{BroadLineRegions.ResidentCompositeModel, String}","page":"API","text":"Base.getindex(rcm::ResidentCompositeModel, line::String) -> ResidentModel\n\nRetrieve the ResidentModel registered for line. Errors (listing the known line names) if line is not present, matching the host CompositeModel indexing behavior.\n\n\n\n\n\n","title":"Base.getindex"},{"category":"method","location":"api/#Base.length-Tuple{BroadLineRegions.ResidentCompositeModel}","page":"API","text":"Base.length(rcm::ResidentCompositeModel) -> Int\n\nNumber of lines registered in rcm.\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#Base.show-Tuple{IO, BroadLineRegions.ResidentCompositeModel}","page":"API","text":"Base.show(io::IO, rcm::ResidentCompositeModel)\n\nPrint one row per line — name, lineCenter, fluxRatio, number of flattened points, element type, and backend — mirroring the host CompositeModel show.\n\n\n\n\n\n","title":"Base.show"},{"category":"method","location":"api/#BroadLineRegions.flatten-Tuple{BroadLineRegions.model}","page":"API","text":"flatten(m::model; T=Float64) -> ModelArrays{T,Vector{T},Vector{Bool}}\n\nBuild a flat structure-of-arrays snapshot of m, one entry per (ring, point), as the host-side bridge to the GPU (gpu(m) adapts the result to CuArray).\n\nEach column is gathered through the package's existing, audited expansion machinery rather than a GPU-specific copy: per-point fields and per-ring scalars are aligned elementwise by expandPerPoint (so e.g. a scalar η is broadcast to every point of its ring), the 3D coordinates come from the ring getXYZ cache, and the camera columns from m.camera. The point ordering matches getVariable(m, sym, flatten=true).\n\n\n\n\n\n","title":"BroadLineRegions.flatten"},{"category":"method","location":"api/#BroadLineRegions.getVariable-Tuple{BroadLineRegions.ResidentModel, Union{String, Symbol}}","page":"API","text":"getVariable(rm::ResidentModel, variable::Union{String,Symbol}) -> Vector\n\nHost copy of a single flat ModelArrays column by name (e.g. :I, :v, :α). This is the recipe-facing counterpart of getVariable(::model, …); the columns are already flat so it takes no flatten keyword, and it does not accept a Function (compute custom quantities on the host model). When the columns live on the GPU the result is copied back to the host.\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"method","location":"api/#BroadLineRegions.gpu-Tuple{Any}","page":"API","text":"gpu(m::model; T=Float32) -> ResidentModel\ngpu(ma::ModelArrays) -> ModelArrays\n\nMove a host model or flat ModelArrays snapshot onto the GPU and return a device-resident handle for repeated observable calls. Requires CUDA.jl to be loaded so the package extension can provide the CUDA-backed methods.\n\n\n\n\n\n","title":"BroadLineRegions.gpu"},{"category":"method","location":"api/#BroadLineRegions.resident-Tuple{BroadLineRegions.model}","page":"API","text":"resident(m::model; T=Float64, backend=KernelAbstractions.CPU(), raytrace=false) -> ResidentModel\n\nFlatten m and wrap it as a ResidentModel on backend. The default CPU() backend keeps everything on the host — useful for testing the resident pipeline without a GPU. Use gpu(m) (with CUDA.jl loaded) to build a device-resident handle.\n\nPass raytrace=true to attach the metadata that raytrace!(::ResidentModel) needs. It is only built for host models with more than one submodel (single-submodel handles keep rt === nothing); on-device constructors carry their own metadata for later combination via +.\n\n\n\n\n\n","title":"BroadLineRegions.resident"},{"category":"type","location":"api/#BroadLineRegions.ModelArrays","page":"API","text":"ModelArrays{T,V,B}\n\nFlat structure-of-arrays representation of a model, with one entry per model point for geometry, velocity, intensity, response, camera coordinates, and reflection state. This is the common host/device payload used by resident, gpu, and the resident observable kernels.\n\n\n\n\n\n","title":"BroadLineRegions.ModelArrays"},{"category":"type","location":"api/#BroadLineRegions.ResidentCompositeModel","page":"API","text":"ResidentCompositeModel\n\nHost-side mirror of CompositeModel for device-resident multi-line work: the same lines/lineCenters/fluxRatios bookkeeping, but each line's model is a ResidentModel handle instead of a host model. Each line stays its own ResidentModel — there is no device-side composite kernel work, only orchestration (the resident _fluxWeights/getSpectrum/getProfile/ lineOverlap methods in gpu_observables.jl run per-line device reductions/kernels and combine the small results on the host; the image/plot3d forwarding and the profile/spectrum recipe support mirror the host composite's — see composite.jl W4-G3 and the recipe docstrings).\n\nBuilt by resident(cm::CompositeModel) (CPU backend) or gpu(cm::CompositeModel) (device) — both of those constructors live in src/composite.jl, NOT here: this file is included before composite.jl (see src/BroadLineRegions.jl), so a method signature annotated ::CompositeModel would UndefVarError at load time. The struct itself only references ResidentModel and can live here with the rest of the resident machinery.\n\nFields\n\nlines::Vector{String}: unique line names, in creation order (lines[1] is the reference line, as on the host CompositeModel).\nmodels::Dict{String,ResidentModel}: the per-line device-resident handle.\nlineCenters::Dict{String,Float64}: central wavelength per line, any consistent units.\nfluxRatios::Dict{String,Float64}: each line's velocity-integrated flux relative to lines[1].\n\n\n\n\n\n","title":"BroadLineRegions.ResidentCompositeModel"},{"category":"type","location":"api/#BroadLineRegions.ResidentModel","page":"API","text":"ResidentModel(ma::ModelArrays, backend, nSubModels::Int [, rt])\n\nA model held resident on a compute device: a flat ModelArrays snapshot plus the KernelAbstractions backend its columns live on. Built once (gpu(m) on the device, or resident on the CPU) and reused across many observable calls — getProfile, getΨ, getΨt, phase, secondMoment have methods on it that run the GPU kernels without re-flattening or re-transferring the model each call.\n\nThe optional rt field carries device-resident raytrace metadata (output grid + per-point submodel / discrete info; see RaytraceMeta) so raytrace!(::ResidentModel) can run the bin→sort→scan→compact entirely on backend. It is nothing for handles that were not built for raytracing; the observable methods ignore it.\n\n\n\n\n\n","title":"BroadLineRegions.ResidentModel"},{"category":"method","location":"api/#BroadLineRegions._build_cloud_modelarrays-Tuple{Int64, Integer}","page":"API","text":"_build_cloud_modelarrays(nClouds, seed; μ, β, F, rₛ, θₒ, γ, ξ, i, rescale,\n    ηₒ, η₁, αRM, rNorm, backend, T) -> ModelArrays\n\nAllocate the device columns on backend and run _rt_build_cloud_kernel! to draw nClouds clouds (one thread each) into a ModelArrays.\n\n\n\n\n\n","title":"BroadLineRegions._build_cloud_modelarrays"},{"category":"method","location":"api/#BroadLineRegions._build_diskwind_modelarrays-Tuple{Real, Real, Real, Int64, Int64, Symbol, Real, Real, Any, Any}","page":"API","text":"_build_diskwind_modelarrays(rMin, rMax, inc, nr, nϕ, scale, rot, θₒ, Ifun, vfun;\n    ηₒ, η₁, αRM, rNorm, backend, T) -> ModelArrays\n\nAllocate the device columns on backend and run _rt_build_disk_kernel! to fill them, then wrap as a ModelArrays. Ifun/vfun are GPU-safe scalar callables (r, ϕ, inc) -> value.\n\n\n\n\n\n","title":"BroadLineRegions._build_diskwind_modelarrays"},{"category":"method","location":"api/#BroadLineRegions._rt_disk_intensity_fn-Union{Tuple{T}, Tuple{Type{T}, Vararg{Any, 5}}} where T<:Real","page":"API","text":"_rt_disk_intensity_fn(::Type{T}, f1, f2, f3, f4, α) -> closure\n\nGPU-safe scalar intensity closure for the built-in DiskWindIntensity, capturing the (T-typed) shape factors so the fused construction kernel can call Ifun(r, ϕ, inc). Mirrors DiskWind_I_(r, ϕ, i, f1, f2, f3, f4, α).\n\n\n\n\n\n","title":"BroadLineRegions._rt_disk_intensity_fn"},{"category":"method","location":"api/#BroadLineRegions._rt_disk_radial_velocity_fn-Union{Tuple{T}, Tuple{Type{T}, Any, Bool, Any}} where T<:Real","page":"API","text":"_rt_disk_radial_velocity_fn(::Type{T}, vᵣFrac, inflow, rₛ) -> closure\n\nGPU-safe scalar velocity closure for the built-in vCircularRadialDisk (Keplerian + radial in/outflow). With vᵣFrac = 0 it is identical to vCircularDisk.\n\n\n\n\n\n","title":"BroadLineRegions._rt_disk_radial_velocity_fn"},{"category":"method","location":"api/#BroadLineRegions._rt_disk_velocity_fn-Union{Tuple{T}, Tuple{Type{T}, Any}} where T<:Real","page":"API","text":"_rt_disk_velocity_fn(::Type{T}, rₛ) -> closure\n\nGPU-safe scalar velocity closure for the built-in vCircularDisk, capturing the (T-typed) Schwarzschild radius so the fused construction kernel can call vfun(r, ϕ, inc).\n\n\n\n\n\n","title":"BroadLineRegions._rt_disk_velocity_fn"},{"category":"function","location":"api/#BroadLineRegions.gpuCloudModel","page":"API","text":"gpuCloudModel(nClouds, seed; T=Float32, kwargs...) -> ResidentModel\n\nBuild a cloud model directly on the GPU (device-resident ResidentModel) — the CUDA counterpart of residentCloudModel, defaulting to Float32. Requires CUDA.jl loaded.\n\n\n\n\n\n","title":"BroadLineRegions.gpuCloudModel"},{"category":"function","location":"api/#BroadLineRegions.gpuDiskWindModel","page":"API","text":"gpuDiskWindModel(rMin, rMax, i; T=Float32, kwargs...) -> ResidentModel\ngpuDiskWindModel(r̄, rFac, α, i; T=Float32, kwargs...) -> ResidentModel\n\nBuild a DiskWind model directly on the GPU (device-resident ResidentModel) — the CUDA counterpart of residentDiskWindModel, defaulting to Float32 (GeForce FP64 is ~1/64 rate). Requires CUDA.jl loaded (defined by the CUDA package extension); errors otherwise.\n\n\n\n\n\n","title":"BroadLineRegions.gpuDiskWindModel"},{"category":"method","location":"api/#BroadLineRegions.residentCloudModel-Tuple{Int64, Integer}","page":"API","text":"residentCloudModel(nClouds, seed; μ=500.0, β=1.0, F=0.5, rₛ=1.0, θₒ=π/2, γ=1.0, ξ=1.0, i=0.0,\n    rescale=1.0, ηₒ=0.5, η₁=0.5, αRM=0.0, rNorm=1.0,\n    backend=KernelAbstractions.CPU(), T=Float64) -> ResidentModel\n\nBuild a Pancoast-style cloud model directly on backend as a device-resident ResidentModel — the on-device counterpart of cloudModel(nClouds; rng=:philox, seed=…). Each cloud is drawn from an independent counter-based (Philox4x32) substream keyed by (seed, cloud index), so the realization is deterministic, seed-reproducible, and identical across CPU/CUDA backends.\n\nnote: Not bit-identical to the host\nThe host Gamma radius draw uses Distributions.jl's rejection sampler, which cannot be reproduced bitwise in a kernel. This path samples the same target distributions on-device, so it is statistically — not bitwise — equal to cloudModel(...; rng=:philox, seed). Use the host path + gpu(m) if you need the exact legacy realization.\n\nseed is required (the stream is counter-based).\n\nTwo built-in physics options are supported on-device (selected per call):\n\nintensity — isotropic (intensity=:isotropic, scaled by rescale) or the anisotropic cloudIntensity (intensity=:cloud, parameter κ).\nvelocity — vCircularCloud (velocity=:circular) or the Pancoast vCloudTurbulentEllipticalFlow (velocity=:turbulent, parameters σρᵣ, σρc, σΘᵣ, σΘc, θₑ, fEllipse, fFlow, σₜ). The turbulent path draws extra normals/uniform per cloud from the same substream.\n\nFor physics beyond these built-ins, build on the host and call gpu(m).\n\n\n\n\n\n","title":"BroadLineRegions.residentCloudModel"},{"category":"method","location":"api/#BroadLineRegions.residentDiskWindModel-NTuple{4, Real}","page":"API","text":"residentDiskWindModel(r̄, rFac, α, i; rot=0.0, nr=128, nϕ=256, scale=:log, kwargs...) -> ResidentModel\n\nr̄/rFac/α parameterization (matching DiskWindModel(r̄, rFac, α, i; ...)): derives rMin/rMax via get_rMinMaxDiskWind and sets the intensity power-law to α.\n\n\n\n\n\n","title":"BroadLineRegions.residentDiskWindModel"},{"category":"method","location":"api/#BroadLineRegions.residentDiskWindModel-Tuple{Real, Real, Real}","page":"API","text":"residentDiskWindModel(rMin, rMax, i; nr=128, nϕ=256, scale=:log, rot=0.0, θₒ=0.0,\n    f1, f2, f3, f4, α, ηₒ=0.5, η₁=0.5, αRM=0.0, rNorm=1.0, rₛ=1.0,\n    intensity=nothing, velocity=nothing, backend=KernelAbstractions.CPU(), T=Float64) -> ResidentModel\n\nBuild a DiskWind model directly on backend as a device-resident ResidentModel, without constructing host rings — the on-device counterpart of DiskWindModel(...) |> resident. One fused kernel generates the camera grid and computes geometry, velocity, intensity, and reverberation response per pixel. Reproduces flatten(DiskWindModel(rMin, rMax, i; ...)) to floating-point rounding (see the boundary-masking note in src/gpu_construct.jl).\n\nThe default physics is the built-in DiskWindIntensity (f1–f4, α) and vCircularDisk (rₛ). Pass vᵣFrac/inflow to switch the velocity to the built-in vCircularRadialDisk (Keplerian + radial in/outflow; vᵣFrac = 0 is vCircularDisk). Advanced users may instead pass intensity/velocity as GPU-safe scalar callables (r, ϕ, inc) -> value (e.g. a let-closure over isbits parameters) to run custom physics in the same fused kernel; generic custom Functions that cannot run in a kernel should use host construction + gpu(m) instead.\n\nUse gpu-backed construction (CUDA loaded) via gpuDiskWindModel; the default CPU() backend builds on the host and is mainly for testing the on-device pipeline without a GPU.\n\n\n\n\n\n","title":"BroadLineRegions.residentDiskWindModel"},{"category":"method","location":"api/#BroadLineRegions._finiteVRange-Tuple{BroadLineRegions.ResidentModel}","page":"API","text":"_finiteVRange(rm::ResidentModel) -> (vmin, vmax)\n\nResident counterpart of _finiteVRange(::model): min/max of the device v column over points where isfinite(v) && isfinite(I*ΔA) (the W4-T4 finiteness rule — finite I*ΔA alone would let a NaN v poison the range, and vice versa), computed as two device reductions with ±Inf sentinels in the style of _rt_device_nanmin/_rt_device_nanmax. Returns host Float64s; (NaN, NaN) when the model has no finite points, exactly like the host helper.\n\n\n\n\n\n","title":"BroadLineRegions._finiteVRange"},{"category":"method","location":"api/#BroadLineRegions._fluxWeights-Tuple{BroadLineRegions.ResidentCompositeModel}","page":"API","text":"_fluxWeights(rcm::ResidentCompositeModel) -> Dict{String,Float64}\n\nResident counterpart of _fluxWeights(::CompositeModel): per-line Σ I*ΔA as a single device reduction (result to host), counting only points where isfinite(v) && isfinite(I*ΔA) — the same finiteness rule as the CPU helper (both conditions, matching exactly what the histogram kernels count; a plain sum would return NaN the moment a sentinel survives). Returns fluxRatios[line] / totalFlux(line) per line.\n\nErrors (naming the offending line) if any line's total flux is not positive, matching the CPU zero-flux guard: a zero-emission line cannot be normalized to unit integral, and an Inf weight would make the line silently vanish from the spectrum instead.\n\n\n\n\n\n","title":"BroadLineRegions._fluxWeights"},{"category":"method","location":"api/#BroadLineRegions.getProfile-Tuple{BroadLineRegions.ResidentCompositeModel, Union{Function, String, Symbol}}","page":"API","text":"getProfile(rcm::ResidentCompositeModel, name; line=nothing, lines=nothing, kwargs...) -> profile\n\nPer-line forwarding of the resident getProfile: compute name's profile for the ResidentModel registered under line (getProfile(rcm[line], name; kwargs...)), running the device kernels on that line's resident columns.\n\nMirrors getProfile(::CompositeModel, …) exactly – a single method covering the whole positional signature, branching at runtime (Julia cannot dispatch on keywords):\n\nname === :ratio (velocity-resolved line ratio, W5-G1) requires lines::Tuple{String,String} and computes the ratio of the two lines' device-binned velocity profiles with identical keyword semantics to the host method (see the :ratio section of getProfile(::CompositeModel, …) for the full argument docs): shared bins from the union of the two lines' _finiteVRange device reductions, one device binned sum per line, the division and NaN/floor handling on the small host vectors via the shared _ratioDivide. The resident restriction applies: a user-supplied bins edge vector must be uniform (_rt_resident_edges).\nevery other name requires the line keyword and forwards to the per-line ResidentModel method, whose restrictions apply (uniform bins only, no custom dx, no custom-Function profiles).\n\n\n\n\n\n","title":"BroadLineRegions.getProfile"},{"category":"method","location":"api/#BroadLineRegions.getProfile-Tuple{BroadLineRegions.ResidentModel, Union{Function, String, Symbol}}","page":"API","text":"getProfile(rm::ResidentModel, name; bins=100, dx=nothing, kwargs...) -> profile\n\nGPU-resident getProfile. Supports :line, :delay, :r, :ϕ, :phase, and :moment2, matching the host getProfile(::model, …) semantics (binnedSum's left-exclusive bins). Custom-Function profiles and a custom dx integration element are not supported on the resident path (the model's ΔA is always used); pass a host model for either.\n\n\n\n\n\n","title":"BroadLineRegions.getProfile"},{"category":"method","location":"api/#BroadLineRegions.getSpectrum-Tuple{BroadLineRegions.ResidentCompositeModel}","page":"API","text":"getSpectrum(rcm::ResidentCompositeModel; bins=100, z=0.0, minX=nothing, maxX=nothing,\n            centered=false, overflow=nothing, kwargs...)\n    -> (edges, centers, flux::Dict{String,Vector{Float64}}, total::Vector{Float64})\n\nResident counterpart of getSpectrum(::CompositeModel), with an identical return shape and identical keyword semantics (see the CPU method for the full argument docs). The shared wavelength range is computed on the host from per-line device min/max reductions of v (_finiteVRange, NaN-aware) unless pinned by the caller via minX/maxX; each line is then binned on its own device via the existing resident binned-sum path with the shared bins/minX/maxX (constructBinEdges is deterministic, so every line gets bit-identical edges), and the per-line host vectors are combined into total.\n\nAs on the CPU, the binning keywords (minX/maxX/centered/overflow) forward to every line's binned sum, and overflow defaults to true for integer bins (flux-preserving – non-centered edges built at exactly [λmin, λmax] place each line's extremal points on the boundary, which the binning otherwise drops; a user-narrowed window accumulates out-of-window flux into the boundary bins) and false for a user-supplied edge vector; an explicit overflow always wins. The resident path additionally requires a user edge vector to be uniform (the device histogram kernels assume uniform spacing; see _rt_resident_edges).\n\n\n\n\n\n","title":"BroadLineRegions.getSpectrum"},{"category":"method","location":"api/#BroadLineRegions.getΨ-Tuple{BroadLineRegions.ResidentModel, AbstractVector, AbstractVector}","page":"API","text":"getΨ(rm::ResidentModel, vEdges::AbstractVector, tEdges::AbstractVector) -> Ψ\n\nGPU-resident 2D transfer function Ψ(v, t); uniform edges only. Matches the host getΨ.\n\n\n\n\n\n","title":"BroadLineRegions.getΨ"},{"category":"function","location":"api/#BroadLineRegions.getΨt-3","page":"API","text":"getΨt(rm::ResidentModel, tEdges::AbstractVector, overflow::Bool=false) -> Ψt\n\nGPU-resident 1D transfer function Ψ(t); uniform tEdges only. Matches the host getΨt (binnedSum's left-exclusive interior-edge convention, empty/NaN-poisoned bins floored to 1e-30, optional overflow into the edge bins).\n\n\n\n\n\n","title":"BroadLineRegions.getΨt"},{"category":"method","location":"api/#BroadLineRegions.lineOverlap-Tuple{BroadLineRegions.ResidentCompositeModel}","page":"API","text":"lineOverlap(rcm::ResidentCompositeModel) -> Vector{NamedTuple}\n\nResident counterpart of lineOverlap with identical pairwise-overlap semantics, via the shared _lineOverlap implementation (src/composite.jl); the only difference is that each line's velocity range comes from the device reductions of _finiteVRange(::ResidentModel) (W4-G2) instead of the host gathers. This is also what lets the spectrum plot recipe accept a ResidentCompositeModel with no recipe changes.\n\n\n\n\n\n","title":"BroadLineRegions.lineOverlap"},{"category":"method","location":"api/#BroadLineRegions.lineRatio-Tuple{BroadLineRegions.ResidentCompositeModel, String, String}","page":"API","text":"lineRatio(rcm::ResidentCompositeModel, a::String, b::String) -> Float64\n\nResident counterpart of lineRatio via the shared _lineRatio implementation (src/composite.jl) – fluxRatios is host metadata on both composite types, so no device work is involved.\n\n\n\n\n\n","title":"BroadLineRegions.lineRatio"},{"category":"method","location":"api/#BroadLineRegions.phase-Tuple{BroadLineRegions.ResidentModel}","page":"API","text":"phase(rm::ResidentModel; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, bins=100, kwargs...)\n\nGPU-resident differential phase, matching phase(::model, …).\n\n\n\n\n\n","title":"BroadLineRegions.phase"},{"category":"method","location":"api/#BroadLineRegions.secondMoment-Tuple{BroadLineRegions.ResidentModel}","page":"API","text":"secondMoment(rm::ResidentModel; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, bins=100, kwargs...)\n\nGPU-resident image second moment, matching secondMoment(::model, …).\n\n\n\n\n\n","title":"BroadLineRegions.secondMoment"},{"category":"section","location":"usage_examples/#Usage-and-Examples","page":"Usage and Examples","text":"This guide explores a few more complicated tasks one might wish to use this code for. \n\nPages = [\"usage_examples.md\"]\nDepth = 2:3","title":"Usage and Examples"},{"category":"section","location":"usage_examples/#Reproducing-the-line-profile-and-transfer-function-shown-in-[CM96](https://ui.adsabs.harvard.edu/abs/1996ApJ...466..704C/abstract)","page":"Usage and Examples","text":"CM96 showed that disk-wind models of the BLR could produce single-peaked line profiles, and as part of their results they generated a hypothetical 2D transfer function for a disk-wind BLR as applied to NGC 5548. We can reproduce such a map with just a few lines of code using BroadLineRegions.jl. \n\nnote: Note\nBroadLineRegions.jl exports itself as both BroadLineRegions and the shorter BLR. We will use the shorter BLR throughout these examples but know that BLR is equivalent to BroadLineRegions should you prefer the longer version.\n\nFirst, let's define the model using DiskWindModel: \n\nusing BroadLineRegions\nmCM96 = BLR.DiskWindModel(3000.,100.,1.,75/180*π,\n        nr=2048,nϕ=1024,scale=:log,f1=1.0,f2=1.0,f3=0.0,f4=0.0,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,τ=5.0,reflect=false)\n\nWe use the parameters from CM96 for barr = 3000rmr_s, r_rmfac = 10^2, alpha = 1, and i = 75^circ. CM96 considered the case of radial and Keplerian shear only, so we turn these gradients on by setting f_1 = 1 and f_2 = 1 (radial and Keplerian shear, respectively) and turn off the remaining velocity gradients by setting f_3 = 0 and f_4 = 0. \n\nTo have a well sampled model grid we pick nr=5096 and nϕ=1024 with scale=:log, which will result in the returned model object having 5096 \"rings\" in our circular camera aperture with each ring holding 1024 grid cells evenly divided in phi space. \n\nWhile this is the default, for the sake of this demonstration we also set I=BLR.DiskWindIntensity to explicitly tell the model to use the intensity formulation first proposed in CM96 (and using the \"f\" parameter language introduced in Long+2023). We also tell the model to calculate the velocities according to the built in function BLR.vCircularDisk, which simply calculates the projected line of sight velocity as v = sqrtfracrmr_s2rsin i sinphi. \n\nFinally, while not required, we set τ = 5.0 as we want our disk to be optically thick and reflect = false as we do not want to move any points from the back to the front. \n\nNow that we have a model, let's get the line profile and store it in our model:\n\np = BLR.getProfile(mCM96,:line,bins=101) #get the line profile using 101 bins in velocity-space, otherwise using default params\nBLR.setProfile!(mCM96,p) #store the line profile in our model structure\n\nWe can do a quick visualization with the built-in profile macro:\n\nBLR.profile(mCM96) #plot all profiles stored in model object, here we only have set :line so only :line will be plotted\n\nWhich should return a plot like this (left panel, right panel is Figure 2 in CM96):\n\n(Image: result of BLR.profile)\n\nNote that the y-axis units are arbitrary and the x-axis is in units of velocity [c], not lambda as published in Fig 2 of CM96, but the shape clearly matches their result that the line profile for such a model should be single-peaked. This is also showing only the full model line profile (thei solid line), not the data or other models. We could reproduce their plot exactly by rescaling our flux and converting from velocity-space to wavelength space as well as plotting the data and a model line profile with azimuthally isotropic emission, but we will leave that as an exercise for the motivated reader...\n\nIt is similarly easy to use BroadLineRegions.jl to generate transfer functions for any user-defined model. In looking at Figure 4 of CM96 we see that their y-axis (delays) spans 0-20 days and the x-axis (projected velocity) -12-12 (10^8 rmcm rms^-1). To match our model result to theirs, we first need to convert our units of rmr_s to the more physical unit of days. CM96 used M_rmNGC5548 approx 10^8 rmM_odot, which means we can calculate the conversion factor as:\n\nM = 1e8*2e30 #kg\nrs = 2*M*6.67e-11/9e16 #2GM/c^2\nrsDay = rs/3e8/3600/24 #days\n\nWe only need to obtain the transfer function Psi over the range shown in CM96, so let's define the tEdges and vEdges we want to use to match their result and then generate the transfer function:\n\ntEdges = collect(range(0.0,stop=20.0/rsDay,length=101)) #will generate 100 bins, convert 20 days upper limit to units of rs\nvEdges = collect(range(-0.04,stop=0.04,length=101)) #0.04c ~12*1e8 cm/s\nΨ = BLR.getΨ(mCM96,vEdges,tEdges) #obtain transfer function with our supplied bin edges \n\nNote that in Fig. 4 of CM96 the edges between the colors are sharp, indicating that they used discrete bins in their image to better showcase structure. While these levels are not explicitly supplied in the paper, we can make a good guess that they are somewhat evenly spaced logarithmic bins. To transform our Psi to match their binning, we can do something like: \n\nfunction getΨMatch(ΨBinned,levels=[-0.4*i for i=0:11]) #generate constant log10 bins of Ψ\n    #fills intervals that are closed at the top;\n    #that is, for regions z1 and z2 the filled region is z1 < Z <= z2\"\n\n    logΨ = log10.(ΨBinned)\n    res = zeros(size(logΨ))\n    mask = (logΨ .<= levels[2]) .& (logΨ .>= levels[1])\n    res[mask] .= (levels[1]+levels[2])/2\n    for i=2:length(levels)-1\n        mask = (logΨ .< levels[i]) .& (logΨ .>= levels[i+1])\n        res[mask] .= (levels[i]+levels[i+1])/2\n    end\n    mask = (logΨ .< levels[end])\n    res[mask] = logΨ[mask]\n    return res\nend\nΨDiscrete = getΨMatch(Ψ./maximum(Ψ)) #normalize and generate discrete bins\n\nAll that's left to do now is visualize the result, which we will do with the heatmap function from Julia's Plots.jl:\n\nusing Plots\n\nheatmap(vEdges.*3e10/1e8,tEdges.*rsDay,ΨDiscrete',\n    ylims=(0,20),xlims=(-12,12),clims=(-4.4,0.0),cbar=false,\n    xlabel=\"v (10⁸ cm/s)\",ylabel=\"τ (days)\",minorticks=true,\n    tickdirection=:out,widen=false,size=(500,500))\n\nWhich should return something like this (left plot, right plot is Fig 4. in CM96):\n\n(Image: 2D Ψ map)\n\nWhile our binning is a little off/coarser than in CM96, this is clearly a pretty good match for a quick and dirty calculation. \n\nA few final notes: \n\nNote that when plotting we use ΨDiscrete' because heatmap expects the shape of the image variable to be flipped from how BroadLineRegions.jl calculates it (the shape of Ψ when returned is (number of velocity bins, number of t bins)). \nIf you wanted to generate just the 1D response function as shown in CM96 Figure 5, BroadLineRegions.jl has syntax for that too: tCenters, Ψt = BLR.getΨt(mCM96,101,10/rsDay). ","title":"Reproducing the line profile and transfer function shown in CM96"},{"category":"section","location":"usage_examples/#Reproducing-sample-cloud-model-results-from-[Pancoast2014](https://ui.adsabs.harvard.edu/abs/2014MNRAS.445.3055P/abstract)","page":"Usage and Examples","text":"We can also generate \"cloud\"/thick-disk models of the BLR similar to Pancoast+2014 easily with BroadLineRegions.jl. In this section we will reproduce part of Figure 4 in Pancoast+2014, which shows five different model BLRs and their 1D transfer functions with the following parameters: \n\nparameter model 1 model 2\nr_rmmean [days] 4.0 4.0\nF 0.25 0.25\nbeta 1.0 0.8\ntheta_o [deg] 40 30\ni [deg] 20 20\nkappa -0.4 -0.4\ngamma 5.0 5.0\nxi 0.3 0.1\nf_rmellipse 0.0 1.0\nf_rmflow 0.0 -\ntheta_e [deg] 0.0 -\nsigma_rmturb 0.0 0.0\n\nFirst, we initialize model structs for both of these cases:\n\nM = 10^(6.5)*2e30 #kg, from Pancoast+2014\nrs = 2*6.67e-11*M/9e16 #2GM/c^2, m\nrsDay = rs/3e8/3600/24 #days^C #days -- model expects μ in units of rₛ\nnClouds = 100_000 #small number of clouds just for demonstration\nmP1 = BLR.cloudModel(nClouds,μ=4/rsDay,F=0.25,β=1.0,θₒ=40/180*π,i=20/180*π,\n        κ=-0.4,γ=5.0,ξ=0.3,fEllipse=0.0,fFlow=0.0,θₑ=0.0,σₜ=0.0,\n        σρc=0.0,σΘᵣ=0.0,σΘc=0.0,σρᵣ=0.0,\n        I=BLR.cloudIntensity,v=BLR.vCloudTurbulentEllipticalFlow,τ=0.0)\nmP2 = BLR.cloudModel(nClouds,μ=4/rsDay,F=0.25,β=0.8,θₒ=30/180*π,i=20/180*π,\n        κ=-0.4,γ=5.0,ξ=0.1,fEllipse=1.0,σₜ=0.0,\n        fFlow=0.0,θₑ=0.0,σρc=0.0,σΘᵣ=0.0,σΘc=0.0,σρᵣ=0.0,\n        I=BLR.cloudIntensity,v=BLR.vCloudTurbulentEllipticalFlow,τ=0.0)\n\nWhere the first argument passed to cloudModel is the number of clouds to generate (here we use a relatively small number just for demonstration purposes), and the keyword arguments supplied mostly correspond to the values in the table above and are fully described in Pancoast+2014. The extra keyword arguments set to 0 but not in the table are options not used in this calculation, and the final line specifies that we would like the intensity calculated with the cloudIntensity function, the velocities calculated with vCloudTurbulentEllipticalFlow, and that the optical depth of the cloud is 0.\n\nTo compare to Figure 4 in Pancoast+2014 we can again generate transfer functions for both models:\n\nusing Plots\n\nvEdges = collect(range(-0.015,stop=0.015,length=51)) #Δλ/λ ~ v/c\ntEdges = collect(range(0.0,stop=20.0/rsDay,length=51))\nΨ1 = BLR.getΨ(mP1,vEdges,tEdges)\nΨ2 = BLR.getΨ(mP2,vEdges,tEdges)\np1 = heatmap(vEdges.*3e5,tEdges.*rsDay,(Ψ1'./maximum(Ψ1)).^(1/2),ylims=(0,20),cbar=false,\n    xlabel=\"Δv (km/s)\",ylabel=\"Lag (days)\",minorticks=true,tickdirection=:out,\n    widen=false,size=(500,500),guidefontsize=18,tickfontsize=16)\np2 = heatmap(vEdges.*3e5,tEdges.*rsDay,(Ψ2'./maximum(Ψ2)).^(1/2),ylims=(0,20),cbar=false,\n    xlabel=\"Δv (km/s)\",ylabel=\"Lag (days)\",minorticks=true,tickdirection=:out,\n    widen=false,size=(500,500),guidefontsize=18,tickfontsize=16)\n\nWhich should produce something like the left column of plots in the comparison below (top plot = p1 and bottom = p2):\n\n(Image: 2D cloud Ψ maps)\n\nNote that again we are just trying to roughly match the color scale by eye, but the general shape and morphology looks good.\n\nWe can also quickly visualize the geometry of the system from any angle (i.e. to compare to the rightmost panel of Fig. 4 in Pancoast+2014) using the built-in plot3d macro:\n\nBLR.plot3d(mP1,:I,cam=(10,10)) #visualize the system with points colored by their intensity with a camera at 10 deg azimuth and altitude\n\nIterating this over a wide variety of camera angles one can produce fun 3D visualizations of the BLR models like the gifs below:\n\nmodel 1 model 2\n(Image: gif of model 1 geometry) (Image: gif of model 2 geometry)\n\nComparing to the stills in Figure 4 of Pancoast+2014 again shows good agreement (note that in generating this animation the number of clouds was set to be just 2,000 to better show the structure/agreement with Pancoast+2014). ","title":"Reproducing sample cloud model results from Pancoast+2014"},{"category":"section","location":"usage_examples/#Reproducing-the-line-and-phase-profiles-shown-in-[Long2023](https://dx.doi.org/10.3847/1538-4357/ace4bb)","page":"Usage and Examples","text":"While reverberation mapping has historically been the primary way to constrain the properties of the BLR, advances in interferometry have recently allowed for the BLR to be directly \"imaged\" by GRAVITY on the VLTI, up to zsim 25! \n\nHere we will demonstrate how to generate line and phase profiles for model BLRs, reproducing the results shown in the left panel in Figure 2 of Long+2023 in the process. To start, we define disk-wind model objects for each case shown in the figure: \n\nmLAll = BLR.DiskWindModel(8e3,45.,1.,45/180*π,nr=2048,nϕ=1024,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,f1=1.0,f2=1.0,\n        f3=1.0,f4=1.0,τ=5.,reflect=false)\nmLf1 = BLR.DiskWindModel(8e3,45.,1.,45/180*π,nr=2048,nϕ=1024,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,f1=1.0,f2=0.0,\n        f3=0.0,f4=0.0,τ=5.,reflect=false)\nmLf2 = BLR.DiskWindModel(8e3,45.,1.,45/180*π,nr=2048,nϕ=1024,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,f1=0.0,f2=1.0,\n        f3=0.0,f4=0.0,τ=5.,reflect=false)\nmLf3 = BLR.DiskWindModel(8e3,45.,1.,45/180*π,nr=2048,nϕ=1024,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,f1=0.0,f2=0.0,\n        f3=1.0,f4=0.0,τ=5.,reflect=false)\nmLf4 = BLR.DiskWindModel(8e3,45.,1.,45/180*π,nr=2048,nϕ=1024,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,f1=0.0,f2=0.0,\n        f3=0.0,f4=1.0,τ=5.,reflect=false)\n\nSetting the values in DiskWindModel to approximately match those in the paper. \n\nWe can then generate the line profiles for each just as we did in the CM96 example above:\n\nLPAll = BLR.getProfile(mLAll,:line,bins=101,centered=true)\nLPf1 = BLR.getProfile(mLf1,:line,bins=101,centered=true)\nLPf2 = BLR.getProfile(mLf2,:line,bins=101,centered=true)\nLPf3 = BLR.getProfile(mLf3,:line,bins=101,centered=true)\nLPf4 = BLR.getProfile(mLf4,:line,bins=101,centered=true)\n\nNote that this time we don't store the profiles in their respective models (although we could!) because we are going to plot all of the models on top of each other by hand. \n\nTo generate the average phase profile we need to pass a few bits of additional information to the phase function. As the documenation there shows, we must supply the U-V baseline information (in units of rmMlambda), the position angle, and the angular scale size of the BLR. Using the published information in the paper we can set these quantities and generate the average phase profiles like so:\n\n#\"off\" axis U-V baselines as in Gravity+2019, Long+2023\nU = [-14.942664391893409, -29.796334354450384, -14.76368897621181, -7.76620715016356, -11.58052554955926, -4.073062007808528, -14.957054767646966, -28.352905965208993, -13.459074151241555, -13.168981507518257, -23.464313904842403, -10.912122036718138] #Mλ\nV = [-15.04195936592504, -36.26778925081391, -21.229760064196043, -14.479367253742678, -35.24862063439095, -20.773150186942956, -14.816925731069297, -35.8588243819711, -21.043799197907198, -15.401140293812933, -37.58326121804998, -21.630778375611328] #Mλ\nBLRAng = 2*8e7*2e33*6.67e-8/9e20/548/3.09e24 #rₛ in radians for BLR distance of 548 Mpc\nPA = 300/180*π #rad\n\nphaseAll = BLR.getProfile(mLAll,:phase,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\nphasef1 = BLR.getProfile(mLf1,:phase,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\nphasef2 = BLR.getProfile(mLf2,:phase,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\nphasef3 = BLR.getProfile(mLf3,:phase,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\nphasef4 = BLR.getProfile(mLf4,:phase,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\n\nWe can then plot the line and phase profiles for all the models and compare to Figure 2 in Long+2023:\n\nusing Plots\n\npLP = plot(title=\"line profiles\",xlabel=\"Δv [Mm/s]\",ylabel=\"flux [normalized]\") #initialize LP plot\nLPList = [LPAll,LPf1,LPf2,LPf3,LPf4]\nnorm = maximum(LPAll.binSums)\nfor p in LPList\n    pLP = plot!(p.binCenters.*3e2,p.binSums./norm,label=\"\",lw=2) #label them in the phase plot only\nend\n\nphaseList = [phaseAll,phasef1,phasef2,phasef3,phasef4]\nweightedPhase = [p.binSums.*maximum(lp.binSums)./norm for (p,lp) in zip(phaseList,LPList)] #just for this figure\n\npPhase = plot(title=\"average phase profiles\",xlabel=\"Δv [Mm/s]\",ylabel=\"Δϕ [deg]\",legend=:topleft) #initialize phase plot\nlabels = [\"All terms equal\",\"f₁ only\",\"f₂ only\",\"f₃ only\",\"f₄ only\"]\nfor (l,p,Δϕ) in zip(labels,phaseList,weightedPhase)\n    pPhase = plot!(p.binCenters.*3e2,Δϕ.*180/π,label=l,lw=2)\nend\n\nplot(pLP,pPhase,layout=@layout([a;b]),size=(500,800),margins=5*Plots.Measures.mm)\n\nWhich should return something similar to the left panel below. Compared to the original plot (right panel) we can again see that we have well reproduced the result (although not quite exactly as seen in the height of the blue phase profile, as I no longer remember the exact set of parameters I used). \n\n(Image: line and phase profiles for disk-wind models)","title":"Reproducing the line and phase profiles shown in Long+2023"},{"category":"section","location":"usage_examples/#Predicting-BLR-sizes-from-differential-visibility-amplitudes","page":"Usage and Examples","text":"The differential phase profiles above encode the photocenter (first image moment) of each velocity channel, but at second order the interferometric signal also contains the size (second image moment) of the emission: in the marginally-resolved limit the line visibility amplitude dips below the continuum by 1 - V_l = 2pi^2u^2sigma_theta^2, where sigma_theta is the rms angular extent of the channel image projected along the baseline direction. While  this has not yet been measured experimentally, we include the ability to predict this quantity for various models via the secondMoment function, both per velocity channel and integrated over the entire line.\n\nContinuing with the disk-wind model and baselines from the example above:\n\nres = BLR.secondMoment(mLAll,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng) #list of (edges, centers, σ², σ²tot) per baseline\nedges, centers, σ², σ²tot = res[1] #σ² [rad²] per velocity channel and integrated over the line, first baseline\n\nrad2μas = 180/π*3600*1e6\nplot(centers.*3e2, sqrt.(σ²).*rad2μas, lw=2, label=\"\",\n    xlabel=\"Δv [Mm/s]\", ylabel=\"rms size along baseline [μas]\")\n\nThe per-channel sizes can also be generated through the usual getProfile interface with :moment2 (returning the average across baselines, like :phase does), which plugs into setProfile! and the plotting recipes:\n\nm2All = BLR.getProfile(mLAll,:moment2,bins=101,centered=true,U=U,V=V,PA=PA,BLRAng=BLRAng)\n\nThe predicted amplitude signal on a given baseline follows directly, e.g. for the first baseline:\n\nabsu = hypot(U[1],V[1])*1e6 #baseline length in units of λ\nVl = 1 .- 2π^2*absu^2 .* σ² #per-channel line visibility amplitude\n\nNote that the line-integrated size σ²tot is not the flux-weighted average of the per-channel sizes: by the law of total variance it also contains the spread of the per-channel photocenters about the global centroid, which for a rotation-dominated BLR is comparable to the mean per-channel size. secondMoment computes it by placing the whole line in a single channel (the moment analog of averaging the complex visibilities before taking the modulus), so both terms are included automatically.","title":"Predicting BLR sizes from differential visibility amplitudes"},{"category":"section","location":"usage_examples/#Reproducing-the-combined-model-line-and-delay-profiles-shown-in-[Long2025](https://doi.org/10.3847/1538-4357/adda38)","page":"Usage and Examples","text":"The real utility of BroadLineRegions.jl is not in its ability to model certain prescriptions for the BLR, but instead the ability to flexibly combine them. To demonstrate this we will reproduce the hybrid disk + cloud model line and delay profiles shown in Figure 4 of Long+2025. As described in the paper, this model is a combination of a Pancoast style \"cloud\" model and a simple azimuthally isotropic disk model with a bit of radial inflow. We can generate both submodels and then combine them with simple syntax: \n\nmDisk = BLR.DiskWindModel(300.,40.,1.0,30/180*π,nr=512,nϕ=1024,f1=0.0,f2=0.0,f3=0.0,f4=1.0,\n        reflect=false,I=BLR.DiskWindIntensity,τ=5.,v=BLR.vCircularRadialDisk,vᵣFrac=0.33,inflow=true) \n        #setting only f4 = azimuthally isotropic disk, 2/3 Keplerian 1/3 inflow velocity field\nmClouds = BLR.cloudModel(1_000_000; I=BLR.cloudIntensity, v=BLR.vCloudTurbulentEllipticalFlow, i=30/180*π, \n        θₒ=15/180*π, β=1.25, F=0.5, μ=300., κ=0.4, ξ=0.6, γ=0.65, fEllipse=0.8, fFlow=0.0, θₑ=35/180*π, σρᵣ=0.2, \n        σρc=0.04, σΘᵣ=0.4, σΘc=0.1, σₜ=0.05,τ=0.0)\n        #parameters from Long+2025\n\n#in Long+2025 the ratio between the sum of intensity values in the cloud submodel and disk submodel is ~1:1\n#so we need to rescale one of the models to ensure this is true\n#note that scaling things this way is arbitrary -- a more physical way would be to specify the ratio of I*ΔA\nIDisk = BLR.getVariable(mDisk,:I)\nIClouds = BLR.getVariable(mClouds,:I)\nratio = sum(IDisk[.!isnan.(IDisk)])/sum(IClouds[.!isnan.(IClouds)])\nfor ring in mDisk.rings\n    ring.I .*= 1/ratio\nend\nBLR.reset!(mDisk) #after mutating ring fields directly, reset! invalidates the model's cached arrays (see Performance notes in the API docs)\n\nmCombined = mDisk+mClouds #all we have to do to combine models is \"add\" them!\n\nWe can again easily generate profiles for both the combined model and the submodels. This time we will generate line and delay profiles for each component and the combined model to compare to Figure 4 of Long+2025:\n\n#get line profiles for each model\nLPCombined = BLR.getProfile(mCombined,:line,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\nLPDisk = BLR.getProfile(mDisk,:line,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\nLPClouds = BLR.getProfile(mClouds,:line,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\n#get delay profiles for each model\nDPCombined = BLR.getProfile(mCombined,:delay,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\nDPDisk = BLR.getProfile(mDisk,:delay,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\nDPClouds = BLR.getProfile(mClouds,:delay,minX=-1e4/3e5,maxX=1e4/3e5,bins=101)\n\n#plot the results\nusing Plots\npLP = plot(LPCombined.binCenters.*3e5,LPCombined.binSums./maximum(LPCombined.binSums),c=:purple,lw=2,label=\"\")\npLP = plot!(LPDisk.binCenters.*3e5,LPDisk.binSums./maximum(LPDisk.binSums),c=:dodgerblue,lw=2,label=\"\",ls=:dash)\npLP = plot!(LPClouds.binCenters.*3e5,LPClouds.binSums./maximum(LPClouds.binSums),c=:crimson,lw=2,label=\"\",ls=:dash)\npLP = plot!(xlabel=\"v [km/s]\",ylabel=\"flux [normalized]\",title=\"line profiles\",xticks=[0.5e4*i for i=-2:2],xlims=(-1e4,1e4))\n\nM = 1.7e8*2e30 #kg\nrs = 2*6.67e-11*M/9e16 #2GM/c^2, m\nrsDay = rs/3e8/3600/24 #days\npDP = plot(DPCombined.binCenters.*3e5,DPCombined.binSums.*rsDay,c=:purple,lw=2,label=\"combined\")\npDP = plot!(DPDisk.binCenters.*3e5,DPDisk.binSums.*rsDay,c=:dodgerblue,lw=2,label=\"thin-disk component\",ls=:dash)\npDP = plot!(DPClouds.binCenters.*3e5,DPClouds.binSums.*rsDay,c=:crimson,lw=2,label=\"cloud component\",ls=:dash)\npDP = plot!(xlabel=\"v [km/s]\",ylabel=\"delay [days]\",title=\"delay profiles\",xticks=[0.5e4*i for i=-2:2],\n    foreground_color_legend=nothing,background_color_legend=nothing,xlims=(-1e4,1e4),ylims=(0,16))\n\nplot(pLP,pDP,layout=@layout([a;b]),size=(500,800),tickdirection=:out,minorticks=true,margins=5*Plots.Measures.mm)\n\nWhich should produce something like the left panel in the comparison below (with the right panel being the result published in Long+2025):\n\n(Image: line and delay profiles for combined + submodels)\n\nNote that there is some inherent randomness in the clouds and the width here is slightly larger than shown in the original paper to better demonstrate the two component nature, but you can improve the smoothness of the profiles by increasing the number of model points and fine tuning the width of the cloud profile if you so desire, and we've clearly reproduced the main features in the plot just fine even at this lower resolution and non fine-tuned prescription.\n\nYou can of course visualize your combined models with the same standard plotting recipes shown earlier to generate fun animations like the one below showcasing the intensity, delay, and velocity structure of this combined model: \n\n(Image: Sample combined model animation for parameters in Long+2025)","title":"Reproducing the combined model line and delay profiles shown in Long+2025"},{"category":"section","location":"usage_examples/#Defining-your-own-custom-models","page":"Usage and Examples","text":"BroadLineRegions.jl makes it easy to define your own models, either by modifying one of the existing classes of models or starting entirely from scratch. \n\nFor example, say you wanted to use the cloudModel as a base but wanted the intensities to be completely randomized. You could define your own custom intensity function and pass that to the cloudModel constructor like so:\n\nmyCustomIntensityFunction(;_...) = rand() #note the _... to handle unsupported kwargs that may be passed to your intensity function\nmyCloudModel = BLR.cloudModel(100,I=myCustomIntensityFunction) #100 clouds with random intensities using defaults for all other parameters\n\nIt is possible to pass custom intensity, velocity, response, and optical depth functions to either cloudModel or DiskWindModel.\n\nIf you already have some precalculated result for any of the above quantities which you would like to use in your model, you can also pass that array to the model constructor so long as it matches the size requirements (i.e. if you have 100 clouds must pass an array of 100 intensities). \n\nYou can also modify models after creation, as shown in the Long+2025 example above where we rescaled the total intensity in the disk submodel. \n\nnote: Note\nThe model and ring structs are mutable, meaning any operations you perform on them overwrite previous values. This includes submodels in larger models, i.e. if you overwrite a field in a combined model the submodel struct will also be mutated. \n\nOf course you are not limited to using BroadLineRegions.jl's predefined model constructors. The most general way to make a model is to simply pass an array of ring structs, a dictionary of profile structs, a camera struct, and an array of submodel start indices. \n\nEach ring represents a \"ring\" on a \"camera\" observing the BLR, and this mutable structs holds all the interesting quantities about the BLR we might wish to model. The camera struct holds the alpha (x) and beta (y) camera coordinates for all points in the model, and in each corresponding ring we must supply the relevant physical information on the BLR associated with that camera pixel. See the full documentation of ring for all available options, but at a minimum in each ring we must define phi, I, v, i and r for the BLR. \n\nFor example, say we were looking top down at an imaginary BLR whose bespoke structure happened to spell out the friendly message \"don't panic\" with random intensities and rotating out of the page. We could implement this custom BLR like:\n\n# Generate a matrix with 1s for letter pixels and 0s for background\nfunction fortytwo() #Full disclosure: Claude helped write this one...\n    mat = zeros(Int, 8, 53)\n    \n    # d\n    mat[1,1] = 1; mat[1,2] = 1; mat[1,3] = 1; mat[1,4] = 1\n    mat[2,1] = 1;                                      mat[2,5] = 1\n    mat[3,1] = 1;                                      mat[3,5] = 1\n    mat[4,1] = 1;                                      mat[4,5] = 1\n    mat[5,1] = 1;                                      mat[5,5] = 1\n    mat[6,1] = 1;                                      mat[6,5] = 1\n    mat[7,1] = 1;                                      mat[7,5] = 1\n    mat[8,1] = 1; mat[8,2] = 1; mat[8,3] = 1; mat[8,4] = 1\n    \n    # o\n    mat[1,7] = 1; mat[1,8] = 1; mat[1,9] = 1; mat[1,10] = 1; mat[1,11] = 1\n    mat[2,7] = 1;                                            mat[2,11] = 1\n    mat[3,7] = 1;                                            mat[3,11] = 1\n    mat[4,7] = 1;                                            mat[4,11] = 1\n    mat[5,7] = 1;                                            mat[5,11] = 1\n    mat[6,7] = 1;                                            mat[6,11] = 1\n    mat[7,7] = 1;                                            mat[7,11] = 1\n    mat[8,7] = 1; mat[8,8] = 1; mat[8,9] = 1; mat[8,10] = 1; mat[8,11] = 1\n    \n    # n\n    mat[1,13] = 1;                                           mat[1,17] = 1\n    mat[2,13] = 1;                                           mat[2,17] = 1\n    mat[3,13] = 1; mat[3,14] = 1; mat[3,15] = 1;             mat[3,17] = 1\n    mat[4,13] = 1; mat[4,14] = 1; mat[4,15] = 1; mat[4,16] = 1; mat[4,17] = 1\n    mat[5,13] = 1;                               mat[5,16] = 1; mat[5,17] = 1\n    mat[6,13] = 1;                                           mat[6,17] = 1\n    mat[7,13] = 1;                                           mat[7,17] = 1\n    mat[8,13] = 1;                                           mat[8,17] = 1\n    \n    # ' (apostrophe)\n    mat[1,19] = 1\n    mat[2,19] = 1\n    \n    # t\n                   mat[1,22] = 1\n                   mat[2,22] = 1\n    mat[3,21] = 1; mat[3,22] = 1; mat[3,23] = 1; mat[3,24] = 1; mat[3,25] = 1\n    mat[4,21] = 1; mat[4,22] = 1; mat[4,23] = 1; mat[4,24] = 1; mat[4,25] = 1\n                   mat[5,22] = 1\n                   mat[6,22] = 1\n                   mat[7,22] = 1;                                           mat[7,25] = 1\n                   mat[8,22] = 1; mat[8,22] = 1; mat[8,23] = 1; mat[8,24] = 1\n\n    # space between t and p\n    \n    # p\n    mat[1,29] = 1; mat[1,30] = 1; mat[1,31] = 1; mat[1,32] = 1; mat[1,33] = 1\n    mat[2,29] = 1;                                           mat[2,33] = 1\n    mat[3,29] = 1;                                           mat[3,33] = 1\n    mat[4,29] = 1; mat[4,30] = 1; mat[4,31] = 1; mat[4,32] = 1; mat[4,33] = 1\n    mat[5,29] = 1\n    mat[6,29] = 1\n    mat[7,29] = 1\n    mat[8,29] = 1\n    \n    # a\n    mat[1,35] = 1; mat[1,36] = 1; mat[1,37] = 1; mat[1,38] = 1; mat[1,39] = 1\n    mat[2,35] = 1;                                           mat[2,39] = 1\n    mat[3,35] = 1;                                           mat[3,39] = 1\n    mat[4,35] = 1; mat[4,36] = 1; mat[4,37] = 1; mat[4,38] = 1; mat[4,39] = 1\n    mat[5,35] = 1; mat[5,36] = 1; mat[5,37] = 1; mat[5,38] = 1; mat[5,39] = 1\n    mat[6,35] = 1;                                           mat[6,39] = 1\n    mat[7,35] = 1;                                           mat[7,39] = 1\n    mat[8,35] = 1;                                           mat[8,39] = 1\n    \n    # n\n    mat[1,41] = 1;                                           mat[1,45] = 1\n    mat[2,41] = 1;                                           mat[2,45] = 1\n    mat[3,41] = 1; mat[3,42] = 1; mat[3,43] = 1;             mat[3,45] = 1\n    mat[4,41] = 1; mat[4,42] = 1; mat[4,43] = 1; mat[4,44] = 1; mat[4,45] = 1\n    mat[5,41] = 1;                               mat[5,44] = 1; mat[5,45] = 1\n    mat[6,41] = 1;                                           mat[6,45] = 1\n    mat[7,41] = 1;                                           mat[7,45] = 1\n    mat[8,41] = 1;                                           mat[8,45] = 1\n    \n    # i\n    mat[1,47] = 1\n    mat[3,47] = 1\n    mat[4,47] = 1\n    mat[5,47] = 1\n    mat[6,47] = 1\n    mat[7,47] = 1\n    mat[8,47] = 1\n    \n    # c\n    mat[1,49] = 1; mat[1,50] = 1; mat[1,51] = 1; mat[1,52] = 1; mat[1,53] = 1\n    mat[2,49] = 1\n    mat[3,49] = 1\n    mat[4,49] = 1\n    mat[5,49] = 1\n    mat[6,49] = 1\n    mat[7,49] = 1\n    mat[8,49] = 1; mat[8,50] = 1; mat[8,51] = 1; mat[8,52] = 1; mat[8,53] = 1\n    \n    return mat\nend\n\nmat = reverse(fortytwo(),dims=1) #reverse top/bottom\nα = zeros(sum(mat)) #camera x coordinates in units of rs\nβ = zeros(sum(mat)) #camera y coordinates in units of rs\nαj = range(-size(mat)[2]/2,stop=size(mat)[2]/2,length=size(mat)[2])\nβi = range(-size(mat)[1]/2,stop=size(mat)[1]/2,length=size(mat)[1])\ncounter = 0\nfor i=1:size(mat)[1]\n    for j=1:size(mat)[2]\n        if mat[i,j] != 0 #fill in camera coordinates where matrix non-empty\n            counter += 1 #which point are we on?\n            α[counter] = αj[j]\n            β[counter] = βi[i]\n        end\n    end\nend\n\ncam = BLR.camera(α,β,false) #initialize the camera for the model, set raytraced flag to false\n\nrings = Array{BLR.ring}(undef,sum(mat)) #initialize array of rings\n\nfor j=1:length(rings) #define the rings with minimum physical quantities of our model BLR\n    r = √(α[j]^2+β[j]^2) #radius in physical space = camera radius for i = 0.0\n    ϕ = atan(β[j],α[j]) #angle in physical space = camera ngle for i = 0.0\n    i = 0.0 #inclination\n    I = rand()\n    v = sqrt(1/r)*sign(α[j]) #rotating in/out of the page and proportional to √1/r\n    ΔA = 1.0 #rₛ²\n    rings[j] = BLR.ring(r=r,ϕ=ϕ,i=i,I=I,v=v,ΔA=ΔA) #define ring with minimum physical quantities using defaults for everything else\n    #note that 3d plotting will be wrong for this, as the 3D plotting routine assumes you start with point in x-y plane and then rotate into z direction\n    #do things in this order if you care about 3d visualizations of your custom models (but otherwise it does not matter)\nend\n\n# Create the custom BLR model\nmyCustomModel = BLR.model(rings,Dict{Symbol,BLR.profile}(),cam,[1]) #no profiles (yet), only one submodel so subModelStartInds = [1] \n\n#visualize it\nusing Plots\nimgI = BLR.image(myCustomModel,:I,markersize=2.,background_color=:black,cmap=cgrad([:red,:lime],[0.0,0.3,1.0]))\nimgv = BLR.image(myCustomModel,:v,markersize=2.,background_color=:black,cmap=cgrad([:blue,:red],[-1.,0.,1.]))\np = BLR.getProfile(myCustomModel,:line,bins=10)\nBLR.setProfile!(myCustomModel,p)\np = BLR.getProfile(myCustomModel,:r,bins=10)\nBLR.setProfile!(myCustomModel,p)\np = BLR.getProfile(myCustomModel,:ϕ,bins=10)\nBLR.setProfile!(myCustomModel,p)\nP = BLR.profile(myCustomModel)\nplot(imgI,imgv,P,layout=@layout([grid(2,1) P{0.4w}]),size=(800,400),margins=5*Plots.Measures.mm)\n\nWhich produces the following plot: (Image: don't panic BLR)\n\nnote: Note\nIf you want the default 3D visualizations to work correctly, define your points in the x-y plane and then rotate them into 3D space using rotate3D as this is what plot3d calls on points to make the 3D visualizations. This should not affect any science use case if you opt to initialize the points some other way (like in the quick example above).","title":"Defining your own custom models"},{"category":"section","location":"usage_examples/#Multi-line-models","page":"Usage and Examples","text":"Everything above models the BLR for a single broad emission line, but of course the BLR often has multiple broad emission lines we might want to model concurrently. This is easily accomplished in BroadLineRegions.jl given its flexibility, and we provide a nice wrapper to support this process with the CompositeModel type. This is simply a custom type that holds several models at once, with each BLR model given a unique name, central wavelength, and relative flux normalization to signify it represents a distinct emission line. The per-line models stay completely independent — combining lines this way changes nothing about how each individual model behaves, and importantly each line model can itself be a multi-component BLR model as described above, so as much complexity as you want is possible in this way. \n\nTo create a multi-line BLR model, simply start with an existing single line BLR model and add it to a CompositeModel:\n\nmHα = BLR.DiskWindModel(3000.,100.,1.,75/180*π,\n        nr=2048,nϕ=1024,scale=:log,f1=1.0,f2=1.0,f3=0.0,f4=0.0,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,τ=5.0,reflect=false) #say the Hα line is modeled by a simple DiskWind model with this parameterization\ncm = BLR.CompositeModel(mHα; line=\"Hα\", lineCenter=6563.) #initialize the composite model, labeling the Hα line and denoting its line center in whatever units you prefer -- but note BroadLineRegions.jl is not aware of these units, so each subsequent line added must use the same units. \n\nlineCenter units are arbitrary (Å above, but nm/μm/anything works) &mdash; the only requirement is that they are mutually consistent across all lines in one CompositeModel, since only the ratio of Δλ/λ matters. This is your responsibility to manage and is not checked by the code.\n\nOnce you have initialized your CompositeModel, there are two ways to add more lines with addLine!. You can pass an explicitly constructed separate model like:\n\nmCIV = BLR.cloudModel(1_000_000; μ=1000.0, β=1.0, F=0.5, θₒ=30/180*π, i=30/180*π, γ=1.0, ξ=1.0,\n        I=BLR.cloudIntensity, v=BLR.vCircularCloud, τ=5.0, rng=:philox, seed=42) #completely independently constructed BLR model with its own distinct parameters\nBLR.addLine!(cm, mCIV; line=\"CIV\", lineCenter=1549., fluxRatio=0.8) #now the CompositeModel cm holds both the DiskWindModel from before for Hα and this new CIV cloud model\n\nAlternatively, you can also reuse the recorded construction parameters of any existing BLR model that has already been added to the CompositeModel (via rebuild on model.params), overriding only what should differ:\n\nBLR.addLine!(cm; line=\"Hβ\", lineCenter=4861., fluxRatio=0.35, from=\"Hα\", r̄=2000.) #all parameters the same as the Hα line model, except move r̄ in from 3000 -> 2000 rₛ\n\nHere fluxRatio is the line's velocity-integrated flux relative to the first line in the CompositeModel (in this example \"Hα\" ≡ 1.0), so the semantics are independent of each intensity function's arbitrary units. Line models are identical except for the central wavelength and flux normalization in this way unless an override parameter is specified (like we did in this example for r̄), and if one is specified the new model is built internally the same way you would build it yourself on the outside &mdash; this wrapper just provides more convenient syntax/saves a bit of typing. \n\nA note on seeds: cloud models built with rng=:philox, seed=... (like mCIV above) rebuild bit-identically under parameter reuse &mdash; the stored seed is reused unless you override it, so an addLine!(cm; from=\"CIV\", ...) line samples \"clouds\" identically as the first line. Pass a new seed to draw fresh (statistically equivalent but different) cloud points. Models built with the legacy AbstractRNG path cannot be reproduced exactly (the RNG state was consumed) and warn accordingly.\n\nWe can obtain the combined wavelength-space spectrum for our new multi-line BLR CompositeModel using getSpectrum,  which can be visualized directly with the spectrum plotting recipe (one series per line plus the total, with any overlapping line regions shaded):\n\nedges, centers, flux, total = BLR.getSpectrum(cm; bins=200) #the underlying data; sum(flux[line]) == fluxRatio\nBLR.spectrum(cm; bins=200) #plot it (pass z=... to shift to the observed frame, but note here we already used the line central wavelengths at z=0)\n\nWhether lines overlap in wavelength space can be checked with lineOverlap, which returns one (lineA, lineB, λlo, λhi) entry per overlapping pair (an empty vector means no overlaps):\n\nBLR.lineOverlap(cm) #NamedTuple[] here -- Hα/Hβ/CIV are far apart compared to their velocity widths\n\nOf course all the usual single-model entry points work per line on this new CompositeType as you would expect, i.e. BLR.getProfile(cm, :line; line=\"Hβ\"), BLR.getVariable(cm, :r; line=\"CIV\"), BLR.raytrace!(cm), BLR.image(cm, :I; line=\"Hα\"), BLR.plot3d(cm), and BLR.profile(cm) (which overlays every line's profile).\n\nVelocities map to wavelengths at first order, λ = lineCenter*(1+v) (see wavelength), with the stored line-of-sight velocity v in units of c. ","title":"Multi-line models"},{"category":"section","location":"usage_examples/#Velocity-resolved-line-ratios","page":"Usage and Examples","text":"With several lines in one CompositeModel we can also model the velocity-resolved ratio of any two of them, i.e. the Balmer decrement BD(v) = FHα(v)/FHβ(v), for example recently measured for a sample of \"Little Red Dots\" in Chen et al. 2026 (§5.3). This quantity is simply the ratio of the two continuum-subtracted broad-line profiles interpolated onto a common velocity grid, which we can calculate from our CompositeModel with the getProfile(cm, :ratio; lines=(a, b)) method. This bins the fluxes from both lines onto shared velocity bins and divides, returning a profile struct named Symbol(\"a/b\").\n\nAs a simple example, consider two Balmer series lines modeled as DiskWindModels in BroadLineRegions.jl with different radial emissivity profiles:\n\nmHα = BLR.DiskWindModel(7500., 100., 1., 75/180*π; nr=2048, nϕ=4096, scale=:log,\n        I=BLR.DiskWindIntensity, v=BLR.vCircularDisk, f1=1.0, f2=1.0, f3=0.0, f4=0.0,\n        τ=5.0, reflect=false) # a simple disk-wind model that will have a singly peaked line profile, power-law source function α = 1\ncm = BLR.CompositeModel(mHα; line=\"Hα\", lineCenter=6563.) #initialize the CompositeModel \n\nBLR.addLine!(cm; line=\"Hβ\", lineCenter=4861., fluxRatio=1/2.86, from=\"Hα\", α=1.25, r̄=5000.) #Case B integrated decrement\n# Hβ reuses all the parameters from Hα but with a steeper power-law and different intensity-weighted mean radius to compensate for this \n# steeper power-law such that the radial grids are roughly the same size in both Hα and Hβ\nc_kms = 2.99792458e5\nvEdges = collect(-2500.:100.:2500.) ./ c_kms #use 100 km/s bins between -2500 and 2500 km/s, converted to units of c\nbd = BLR.getProfile(cm, :ratio; lines=(\"Hα\", \"Hβ\"), bins=vEdges) #the velocity-resolved Balmer decrement BD(v)\nBLR.profile(cm)   #both line profiles, overlaid\nBLR.profile(bd)   #BD(v) itself -- centrally peaked, declining toward the wings\n# equivalently, plot straight from the composite without computing `bd` first (the lines pair is\n# positional here because Plots reserves `lines` as a keyword): BLR.profile(cm, :ratio, (\"Hα\", \"Hβ\"))\n\nThis example was chosen to get the shape of the BD(v) profile to roughly match what was observed and described in Chen+ 2026, where they observed Balmer decrements with ~single peaks and decreasing roughly by a factor of two between the center and the wings. Of course this is a very simple example, and many further complications are possible, including passing your own custom intensity functions as described in earlier sections of the documentation.\n\nAn important subtlety of the integrated-flux semantics: each line's profile is normalized to unit integral before being scaled by its fluxRatio, so any constant prefactor on a line's intensity divides out &mdash; the per-line model sets only the velocity shape of BD(v), while the integrated decrement is pinned by fluxRatio alone (so you can scale it any way you like after generation). This means that there could be a discrepancy between the \"real\" ratio in between the two models and the ratio shown in the plot, but of course if you would rather let the per-line models themselves set the integrated decrement without pinning it by hand, you can compute the fluxRatio from them directly:\n\nIΔA(m) = sum(x for x in BLR.getVariable(m, :I, flatten=true) .* BLR.getVariable(m, :ΔA, flatten=true) if isfinite(x))\nmHβ = BLR.rebuild(mHα.params; α=1.25, r̄=5000.) #the same model addLine! builds internally\ncm2 = BLR.CompositeModel(mHα; line=\"Hα\", lineCenter=6563.)\nBLR.addLine!(cm2, mHβ; line=\"Hβ\", lineCenter=4861., fluxRatio=IΔA(mHβ)/IΔA(mHα)) #model-implied integrated decrement\n\nThe integrated ratio of any two registered lines is always available as lineRatio(cm, \"Hα\", \"Hβ\") (== fluxRatios[\"Hα\"]/fluxRatios[\"Hβ\"] by the semantics above).","title":"Velocity-resolved line ratios"},{"category":"section","location":"usage_examples/#Running-on-the-GPU","page":"Usage and Examples","text":"As of version 0.3.0, BroadLineRegions.jl can now generate/port models to the GPU (only tested on NVIDIA for now) for significant performance gains on some workflows. Below we repeat the CM96 example from above showcasing how to access these capabilities and the kind of  performance gains one might expect from using BroadLineRegions.jl on your GPU.  First, we must load CUDA.jl alongside BroadLineRegions to enable these features:\n\nusing BroadLineRegions, CUDA\n\nnote: Float32 by default\nThe GPU entry points default to Float32 (T=Float32) because consumer (GeForce) cards run Float64 at roughly 1/64 the Float32 rate. Float32 is plenty for most line-profile and transfer-function work; pass T=Float64 if you are on a data-center card or need the extra precision.\n\nFollowing the CM96 disk model from the top of this page, we can modify this slightly to showcase two ways to get this model onto the GPU instead of the CPU: \n\n(1) Build on the CPU, then transfer. Construct the model exactly as before and hand it to gpu, which returns a device-resident handle you can call the observable functions on directly:\n\nmCM96 = BLR.DiskWindModel(3000.,100.,1.,75/180*π,\n        nr=2048,nϕ=1024,scale=:log,f1=1.0,f2=1.0,f3=0.0,f4=0.0,\n        I=BLR.DiskWindIntensity,v=BLR.vCircularDisk,τ=5.0,reflect=false) #same as before\n\nmGPU = BLR.gpu(mCM96)                 #device-resident handle (ModelArrays on the GPU)\np = BLR.getProfile(mGPU, :line, bins=101)\ntEdges = collect(range(0.0,stop=20.0/rsDay,length=101)) #will generate 100 bins, convert 20 days upper limit to units of rs\nvEdges = collect(range(-0.04,stop=0.04,length=101)) #0.04c ~12*1e8 cm/s\nΨ = BLR.getΨ(mGPU, vEdges, tEdges)   #same vEdges/tEdges as the CM96 example\n\nThe handle is built once and reused across as many observable calls as you like — nothing is re-flattened or re-copied per call. Results come back as ordinary host arrays/profiles, identical in shape to the CPU versions. You can call any observable/raytrace! on the model and will see a performance gain  for each of these common function calls that can exploit the GPU. But note that you do pay a penalty to  transfer the model from the CPU to the GPU, thus to see performance gains in this kind of scenario requires that  the models be large (such that calculating observables/raytracing takes a long time on the CPU) or that you calculate  many quantities after model creation (i.e. calling getProfile many times).\n\n(2) Build directly on the device. For the built-in DiskWind/cloud model physics you can skip the host model entirely and construct everything instead directly on the GPU with gpuDiskWindModel (note there similarly exists a gpuCloudModel function as well).  This is the fastest path for fitting where models are constantly reconstructed from new parameters, since it never allocates host rings, flattens, or transfers. To generate the same example as before but on GPU directly we thus can write:\n\nmGPU = BLR.gpuDiskWindModel(3000.,100.,1.0,75/180*π;          #(r̄, rFac, α, i) as in CM96\n        nr=2048,nϕ=1024,scale=:log,f1=1.0,f2=1.0,f3=0.0,f4=0.0,τ=5.0)\np = BLR.getProfile(mGPU, :line, bins=101)\ntEdges = collect(range(0.0,stop=20.0/rsDay,length=101)) #will generate 100 bins, convert 20 days upper limit to units of rs\nvEdges = collect(range(-0.04,stop=0.04,length=101)) #0.04c ~12*1e8 cm/s\nΨ = BLR.getΨ(mGPU, vEdges, tEdges) #same vEdges/tEdges as in the CM96 example\n\nIf for some reason you want the model back on the CPU, this can easily be accomplished with:\n\nmCPU = BLR.cpu(mGPU)\n\nBut note that in doing so this model object will hold the flattened array shape required to build it on the GPU and not the standard ring structs used  on models created on the CPU. \n\nHow much of a performance gain does this get us? We can time the end-to-end \"build a model and get its line profile\" for each approach (use CUDA.@sync so the GPU work actually completes before the timer stops):\n\n#CPU only: build host model + profile\n@time begin\n    m = BLR.DiskWindModel(3000.,100.,1.,75/180*π,nr=2048,nϕ=1024,scale=:log,\n            f1=1.0,f2=1.0,f3=0.0,f4=0.0,τ=5.0)\n    BLR.getProfile(m, :line, bins=101)\nend\n\n#GPU, build-on-device: build resident model + profile\n@time CUDA.@sync begin\n    mGPU = BLR.gpuDiskWindModel(3000.,100.,1.0,75/180*π;nr=2048,nϕ=1024,scale=:log,\n            f1=1.0,f2=1.0,f3=0.0,f4=0.0,τ=5.0)\n    BLR.getProfile(mGPU, :line, bins=101)\nend\n\nOn a laptop with an AMD Ryzen 9 HX 370 CPU and RTX 5070 Ti GPU for this scenario (nr=2048, nϕ=1024, total grid size ≈ 2.1M points, 101-bin :line profile, Julia 1.12), the measured wall-clock times are:\n\napproach time vs. CPU-only\nCPU only — build + getProfile(:line) ~360–720 ms 1×\nGPU, build-on-device (gpuDiskWindModel) + profile, Float32 ~2.2 ms ~160–330×\nGPU, build-on-device + profile, Float64 ~7.5 ms ~50–95×\n\nSo building directly on the device is well over two orders of magnitude faster for this build-dominated work (Float32), and the gap grows with nr/nϕ and with the number of clouds — most of the win is eliminating the host build + flatten + transfer, not just the kernel compute. (The CPU time varies run-to-run by roughly 2× here, which is where the spread in the speedup column comes from; regenerate on your own hardware. All times are warm/steady-state — the first call in a fresh session additionally pays one-time kernel compilation.)\n\nnote: When the transfer path `gpu(m)` pays off\ngpu(m) (approach 1) is not a shortcut if you are only going to take a single profile of a newly built model — building the host model, flattening it, and copying it to the device costs more than just evaluating the profile on the CPU (≈ 4 s here, slower than CPU-only). Its value is reuse: once a model is resident on the GPU, each additional observable call is at least ~15× faster than the equivalent CPU implementation. Thus use gpu(m) when you will make many observable calls on one model, or when trying to  implement custom physics that are not easily ported to the GPU; otherwise just use gpuDiskWindModel/gpuCloudModel when you rebuild \"standard\" geometries every iteration.","title":"Running on the GPU"},{"category":"section","location":"usage_examples/#Custom-intensity-/-velocity-on-the-GPU","page":"Usage and Examples","text":"gpuDiskWindModel accepts custom physics, as long as it can run inside a kernel — pass intensity (or velocity) as a GPU-safe scalar callable (r, ϕ, inc) -> value that closes only over isbits values. For example, for a disk with uniform emissivity (every point set to 1):\n\nmGPU = BLR.gpuDiskWindModel(3000.,100.,1.0,75/180*π;nr=2048,nϕ=1024,scale=:log,\n        intensity = (r,ϕ,inc) -> 1.0f0,    # GPU-safe scalar I(r,ϕ,inc); use 1.0f0 for Float32\n        τ=5.0)\np = BLR.getProfile(mGPU, :line, bins=101)\n\nA general Function that cannot run in a kernel (closures over arrays, calls into non-GPU libraries, etc.) should instead be used to build the model on the CPU and then moved over with BLR.gpu(m). For a more detailed worked example of the (r, ϕ, inc) -> value form to model your own physics on, see the built-in GPU-safe closures: _rt_disk_intensity_fn (the device intensity, a direct port of the CPU DiskWind_I_) and _rt_disk_velocity_fn / _rt_disk_radial_velocity_fn for an example velocity calculation.","title":"Custom intensity / velocity on the GPU"},{"category":"section","location":"usage_examples/#Summary-of-everything-else-that-works-on-the-GPU","page":"Usage and Examples","text":"The device-resident handle is a drop-in for the observables described above: getProfile (:line, :delay, :r, :ϕ, :phase, :moment2), getΨ, getΨt, phase, and secondMoment all have methods on it, which work automatically just as they would on a CPU hosted model and with the same syntax.  Combined models work too: build each submodel on the device (or copy them there) and add them with + (e.g. BLR.gpuDiskWindModel(...) + BLR.gpuCloudModel(...)) just as before, then raytrace! the combined model if desired — all without leaving the GPU. Bring any handle back to the host with BLR.cpu(mGPU). Two caveats on the resident GPU paths: they require uniform bin edges (pass an Int bin count or uniformly spaced edges), and do not accept a custom dx integration element — use the host model for those. See gpu, gpuDiskWindModel, gpuCloudModel, and residentDiskWindModel/residentCloudModel (CPU-backend builds, handy for testing the pipeline without a GPU) in the API for the full set of options.","title":"Summary of everything else that works on the GPU"},{"category":"section","location":"usage_examples/#Additional-(selected)-helpful-tools-from-the-[API](@ref)-not-described-above","page":"Usage and Examples","text":"After combining submodels, access their constituent parts by simply writing:\n\nmCombined = m1+m2 #combine two models\nm1 = mCombined[1]; m2 = mCombined[2] #get access to the submodels with indexing \n\nIf you are annoyed with the output statements after things, suppress them by placing a semicolon after:\n\nmCombined; #suppresses output\n\nOther functions: ","title":"Additional (selected) helpful tools from the API not described above"},{"category":"function","location":"usage_examples/#BroadLineRegions.getVariable","page":"Usage and Examples","text":"getVariable(m::model, variable::Union{String,Symbol,Function}; flatten=false)\n\nRetrieve elements from model object and stack them into matrices for easy manipulation.\n\nArguments\n\nm::model: Model object to extract variables from\nvariable::Union{String,Symbol,Function}: Variable to extract from model\nIf String, will be converted to Symbol\nMust be a valid attribute of model.rings (e.g. :I, :v, :r, :i, :ϕ) or a function that can be applied to model.rings\nExample: Keplerian disk time delays could be calculated like t(ring) = ring.r .* (1 .+ sin.(ring.ϕ) .* ring.i)\nflatten::Bool=false: If true, flatten the result to a vector\n\nReturns\n\nArray{Float64,}: Matrix or vector of extracted variable from model.rings, created by stacking the output variable for each ring\nFor example, if variable given is :I, result will have shape (length(r), length(ϕ)) as at each r and ϕ there is a value of I\nIf flatten=true, result will be a flattened vector\n\n\n\n\n\ngetVariable(m::model, variable::Symbol; flatten=false)\n\nRetrieve model variable when specified as a Symbol. See main docstring for details.\n\n\n\n\n\ngetVariable(m::model, variable::Function; flatten=false)\n\nRetrieve model variable when specified as a Function. See main docstring for details.\n\nNote\n\nResults are memoized in m.cache only for the package's own pure delay functions (t, tDisk, tCloud) — user-supplied functions are always re-evaluated, since they may capture state the cache cannot see.\n\n\n\n\n\ngetVariable(rm::ResidentModel, variable::Union{String,Symbol}) -> Vector\n\nHost copy of a single flat ModelArrays column by name (e.g. :I, :v, :α). This is the recipe-facing counterpart of getVariable(::model, …); the columns are already flat so it takes no flatten keyword, and it does not accept a Function (compute custom quantities on the host model). When the columns live on the GPU the result is copied back to the host.\n\n\n\n\n\ngetVariable(cm::CompositeModel, variable; line::String, flatten=false)\n\nPer-line forwarding of getVariable: gather variable from the model registered under line.\n\n\n\n\n\n","title":"BroadLineRegions.getVariable"},{"category":"function","location":"usage_examples/#BroadLineRegions.raytrace!","page":"Usage and Examples","text":"raytrace!(cm::CompositeModel; line=nothing, kwargs...) -> cm\n\nPer-line forwarding of raytrace!. line=nothing (the default) raytraces every line; otherwise only the named line.\n\nraytrace! returns a new model (it does not mutate its argument), so this reassigns cm.models[line] = raytrace!(cm.models[line]; kwargs...). Lines that raytrace! would warn about and hand back unaltered – a single-submodel model (subModelStartInds == [1]) or an already-raytraced one (camera.raytraced) – are skipped silently so a multi-line loop does not warn-spam.\n\nRaytracing happens within each line's own submodels only. Cross-line occlusion is deliberately not modeled: different lines are at different wavelengths and each line's τ is that line's own optical depth (see the Workstream-4 plan, \"Out of scope\").\n\n\n\n\n\nraytrace!(m::model; IRatios::Union{Float64,Array{Float64,}}=1.0,\n        τCutOff::Float64=1.0, raytraceFreeClouds::Bool=false,\n        backend=nothing, T=Float64)\n\nPerform raytracing for a model, combining overlapping components along line of sight.\n\nThis function should be called after combining all relevant models (i.e. mCombined = m1 + m2 + m3...). Points are binned by pixel, sorted front-to-back along the line of sight, and combined with a segmented exp(-τ) scan: each point attenuates those behind it, points past τCutOff are dropped, and IRatios is applied as a global per-submodel weight. A new model object is returned with the extraneous points removed. Note that this function will mutate the input model objects.\n\nnote: Algorithm / performance\nThe bin→sort→segmented-scan implementation is far faster than the original brute-force raytracer (and can also run on a device via backend). If you only need simple disk obscuration removal rather than full attenuation, removeDiskObscuredClouds! is a lighter alternative.\n\nArguments\n\nm::model: Model to raytrace\nIRatios::Union{Float64,Array{Float64,}}=1.0: Global emissivity weights for each submodel\nIf Float64, applies to all submodels equally\nIf array, applies to each submodel individually (must match number of submodels)\nUsed when combining models with different intensity functions if they aren't properly normalized\nτCutOff::Float64=1.0: Maximum optical depth to raytrace to (stops when τ > τCutOff)\nraytraceFreeClouds::Bool=false: Whether to raytrace free clouds (cloud-cloud raytracing)\nIf false, clouds are only raytraced if they overlap with a continuous model\nIf true, clouds will be checked for overlap with other clouds and raytraced accordingly\nbackend=nothing: Optional KernelAbstractions backend to run the bin→sort→scan on a device.\nIf nothing, uses the CPU implementation.\nPass CUDABackend() (with CUDA.jl loaded) to run on the GPU; see also gpu.\nT=Float64: Element type for the device arrays when backend is set (use Float32 on GeForce GPUs for speed).\n\nReturns\n\nm::model: Model with raytraced points\n\n\n\n\n\nraytrace!(rm::ResidentModel; IRatios=1.0, τCutOff=1.0, raytraceFreeClouds=false) -> ResidentModel\n\nDevice-resident dispatch of raytrace!: run the full bin→sort→segmented-scan→ compact combine on rm's backend with no host round-trips, returning a freshly raytraced ResidentModel (drop-in for the observable methods). rm must carry raytrace metadata — build it with gpu(m) (CUDA loaded) or resident(m; raytrace=true).\n\nLike raytrace!(::model) this returns a new combined handle rather than mutating in place (the point count changes: one combined point per active output pixel + surviving free clouds), so write rm = raytrace!(rm). Semantics match the host raytrace!: IRatios is a global per-submodel weight, τCutOff the optical-depth cutoff, raytraceFreeClouds enables cloud–cloud attenuation.\n\n\n\n\n\n","title":"BroadLineRegions.raytrace!"},{"category":"function","location":"usage_examples/#BroadLineRegions.reset!","page":"Usage and Examples","text":"reset!(m::model; profiles=true, img=false)\n\nErase existing profiles/raytrace status and invalidate the model's getVariable cache.\n\nCall this after mutating ring fields directly (e.g. m.rings[1].I .= ...) so that subsequent getVariable/getProfile calls see the new values instead of memoized ones.\n\nParameters\n\nm::model: Model object to reset\nprofiles::Bool=true: If true, reset profiles\nimg::Bool=false: If true, reset raytracing boolean (does not change existing model but allows model to be raytraced again after combining other new models)\n\n\n\n\n\nreset!(cm::CompositeModel; line=nothing, profiles=true, img=false) -> cm\n\nPer-line forwarding of reset!. line=nothing (the default) resets every line; otherwise only the named line. Extra keywords (profiles, img) forward to the per-line model method.\n\n\n\n\n\n","title":"BroadLineRegions.reset!"},{"category":"function","location":"usage_examples/#BroadLineRegions.phase","page":"Usage and Examples","text":"phase(m::model; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, kwargs...)\n\nCalculate differential phase signal for a model based on specified baselines, model orientation, and BLR angular size.\n\nArguments\n\nm::model: Model object to calculate phase for\nU::Vector{Float64}: U component of complex visibility in [Mλ]\nV::Vector{Float64}: V component of complex visibility in [Mλ]\nPA::Float64: On-sky position angle of the model in radians\nBLRAng::Float64: Characteristic size of the BLR model in radians (conversion from r_s to radians)\nreturnAvg::Bool=false: If true, returns the average phase across all baselines\noffAxisInds::Union{Nothing,Vector{Int}}=nothing: If provided, only calculates phase for baselines at specified indices\n\nReturns\n\nIf returnAvg=true: Tuple{Vector{Float64},Vector{Float64},Vector{Float64}} containing:\nBin edges for velocity\nBin centers for velocity\nAverage differential phase (in radians)\nIf returnAvg=false: Vector{Tuple{Vector{Float64},Vector{Float64},Vector{Float64}}} containing:\nFor each baseline, a tuple of bin edges, bin centers, and differential phase\n\nThe differential phase is calculated by integrating the phase over the model at each velocity bin, weighted by the intensity and area of each ring element.\n\nA device-resident method (phase(::ResidentModel, …)) runs this on the GPU without re-flattening the model each call; see gpu and ResidentModel.\n\n\n\n\n\nphase(rm::ResidentModel; U, V, PA, BLRAng, returnAvg=false, offAxisInds=nothing, bins=100, kwargs...)\n\nGPU-resident differential phase, matching phase(::model, …).\n\n\n\n\n\n","title":"BroadLineRegions.phase"},{"category":"function","location":"usage_examples/#BroadLineRegions.DRW","page":"Usage and Examples","text":"function DRW(;t::Array{Float64}=collect(range(0,stop=100,length=1001)), μ::Float64=0.0, τ::Float64=1.0, σ::Float64=0.1)\n\nGenerate a damped random walk continuum lightcurve following MacLeod+2010\n\nArguments:\n\nt::Vector{Float64}`: time array that returned lightcurve will be sampled at\nμ::Float64: mean value of random walk\nτ::Float64: characteristic time scale of random walk\nσ::Float64: standard deviation of random walk\n\nReturns:\n\nC::Vector{Float64}: damped random walk array corresponding to times t\n\n\n\n\n\n","title":"BroadLineRegions.DRW"},{"category":"function","location":"usage_examples/#BroadLineRegions.syntheticLC","page":"Usage and Examples","text":"function syntheticLC(Ct,CLC,Ψτ;tStart=0.0,normalize=false)\n\nGenerate a synthetic lightcurve from a continuum lightcurve and a 1D transfer function.\n\nArguments:\n\nCt::Vector{Float64}: time array for the continuum lightcurve\nCLC::Vector{Float64}: continuum lightcurve values at times Ct\nΨτ::Vector{Float64}: transfer function values at times Ct\ntStart::Float64: time after which the lightcurve should be normalized to zero\nnormalize::Bool: whether to normalize the lightcurve to the extent of variations in the continuum\n\nReturns:\n\nΔlc::Vector{Float64}: synthetic lightcurve sampled at times Ct with zero point ~at tStart and optionally normalized\n\n\n\n\n\n","title":"BroadLineRegions.syntheticLC"},{"category":"section","location":"#BroadLineRegions.jl","page":"Home","text":"A fast and flexible toolkit for modeling the broad-line region (BLR) in Julia.\n\n(Image: Sample combined model animation for parameters in Long+2025)","title":"BroadLineRegions.jl"},{"category":"section","location":"#Installation","page":"Home","text":"","title":"Installation"},{"category":"section","location":"#Julia","page":"Home","text":"using Pkg\nPkg.add(\"BroadLineRegions\")\n\nOr install from the GitHub repo directly:\n\nusing Pkg\nPkg.add(\"https://github.com/kirklong/BroadLineRegions.jl\")","title":"Julia"},{"category":"section","location":"#Python","page":"Home","text":"To access BroadLineRegions.jl from within your Python installation, first you must install JuliaCall. After successfully installing JuliaCall you can add BroadLineRegions.jl to your new Julia installation in Python with: \n\nfrom juliacall import Main as jl\nfrom juliacall import Pkg as jlPkg\njlPkg.add(\"BroadLineRegions\") #or use the github link","title":"Python"},{"category":"section","location":"#Quickstart","page":"Home","text":"While this code is designed to be very flexible and modular such that you can implement your own bespoke models of the BLR easier, two popular models of the BLR are included as default models which one can play with. \n\nTo generate a \"cloud\" model similar to that of Pancoast+ 2011 and 2014, use syntax like:\n\nusing BroadLineRegions #exports itself as both BroadLineRegions and the shorter acronym BLR\nmClouds = BLR.cloudModel(100_000,μ=500.,β=1.,F=0.5,θₒ=30/180*π,γ=1.,ξ=1.,i=0.,\n        I=BLR.IsotropicIntensity,v=BLR.vCircularCloud,rescale=1e-5,τ=0.0)\n\nTo generate a \"disk-wind\" model similar to that of Chiang and Murray 1996 and 1997 following the prescription laid out in Long+ 2023 and 2025 use syntax like:\n\nmDisk = BLR.DiskWindModel(500.,5.,1.,30/180*π,nr=24,nϕ=48,scale=:log,\n        f1=1.0,f2=1.0,f3=1.0,f4=1.0,reflect=false,τ=5.)\n\nYou can of course define fully custom models by passing your own \"rings\" (on a camera) and optionally any default profiles, a camera struct, and submodel start indices:\n\nmCustom = BLR.model(myCustomRings,nothing,myCustomCamera,[1]) #a custom model with no profiles, a user-defined camera for raytracing/visualization, and with no submodels\n\nModels can be combined simply by writing mCombined = m1 + m2. \n\nGenerate profiles (i.e. line, phase, delay, or whatever else your heart desires) for models with syntax like:\n\np = BLR.getProfile(m,:line) #generate line profile with default parameters\nBLR.setProfile!(m,p) #optinally store the profile in model data structure \n\nA few default visualization recipes exist as well:\n\nprofiles = BLR.profile(m) #plot all of the model's stored profiles \nimg = BLR.image(m,:I) #make an image of the model intensity, can pass any other parameter as well to \"image\" them\ngeometry = BLR.plot3d(m) #visualize the geometry of the system in a 3d plot, can also color points according to any parameter\n\nNote that no functions are exported by default into the global namespace to prevent overlap with other modules, and you must prepend the module name to all methods to access them. BroadLineRegions.jl exports itself as both BroadLineRegions and BLR, so both of these prefixes are equivalent, i.e. BroadLineRegions.model == BLR.model. If adding this prefix annoys you, you can avoid this by specifying the functions you want added to the global namespace in your import/Using statement. For example:\n\nusing BLR: DiskWindModel, cloudModel\nmDisk = DiskWindModel(parms...) #instead of BLR.DiskWindModel\n\nFor more detailed examples, see the Usage and Examples page.\n\nFull documentation is available in the API section.\n\nAlso note that if you are using BroadLineRegions from within Python you will need to modify all the code examples with the corresponding name that you imported julia with, i.e. the simple cloud example in the quickstart above in Python would become: \n\nfrom juliacall import Main as jl\nimport numpy as np\njl.seval(\"Using BroadLineRegions\") #load BLR into Main\n\nmClouds = jl.BLR.cloudModel(100_000,μ=500.,β=1.,F=0.5,θₒ=30/180*np.pi,γ=1.,ξ=1.,i=0.,\n             I=jl.BLR.IsotropicIntensity,v=jl.BLR.vCircularCloud,rescale=1e-5,τ=0.0)\n\nYou may also consider starting Julia with a sysimage to improve performance (see pure Julia example here, and instructions for enabling the functionality in juliacall here).","title":"Quickstart"},{"category":"section","location":"#Referencing","page":"Home","text":"If you find this code useful in your work, please cite it as:\n\nlorem ipsum","title":"Referencing"},{"category":"section","location":"#Contributing","page":"Home","text":"If you would like to contribute to the package, please open a pull request on the GitHub. For bug reports and feature requests, please open an issue on the GitHub. ","title":"Contributing"}]
}
